# BRANCHES — every branch not master lives here until merged+deleted

Status is the four-state vocabulary — DONE / BLOCKED / OWED / UNSTARTED — per the shared
loop-rules block in `CLAUDE.md`. No fifth state. BLOCKED must name a blocker that holds
*now* and an owner; where the evidence does not settle barrier-vs-trigger, the row is
UNSTARTED.

| Branch | Purpose | Status | Detail | Blocker / outstanding (owner) |
|--------|---------|--------|--------|-------------------------------|
| `gov/session-closure-sweep` | The #90 vocabulary-adoption sweep (completes #88's adoption, does not amend it): relabel every `BRANCHES.md` row + `OPEN_QUESTIONS.md` entry in health-app to the four states (DONE/BLOCKED/OWED/UNSTARTED); push 3 previously local-only branches with dedicated rows; adjudicate 2 invalid-state rows + 5 OWED loops against evidence; strike the stale ROADMAP "CLAUDE.md deferred to laptop" row. Governance-only — no `backend/`, `frontend/`, or `alembic/`; test count must not move. DECISIONS_LOG #90 + FEEDBACK §13 at merge. | **DONE** — ff-merged to master at `05f0282`, pushed; local branch deleted (was local-only, never pushed to origin). 7 concern-split commits, `e99c09e` → `05f0282`. Governance-only: 6 files, zero `backend/` / `frontend/` / `alembic/`; backend suite **206 passed**, unchanged from #88's 206. DECISIONS_LOG #90 + FEEDBACK §13 landed. | — (complete). | **Phase 2 remains OWED (separate repo, separate session):** `health-connect-app`'s `BRANCHES.md` (4 rows) and `OPEN_QUESTIONS.md` (Q1/Q2/Q4/Q5 on `PENDING`) are still on superseded labels — this sweep could not reach them under the single-repo rule. Owner: Luke, in an HCA-rooted session. |
| `feat/feedback-ledger` | `FEEDBACK.md` §12 — append-only, status-mutable integrity ledger for analysis-loop failures, typed `HUMAN`/`MODEL`/`COUPLED`, seeded 12 rows from the 15 Jul calf investigation; `prevention` mandatory (inclusion test as hard gate), `signed` records error direction as a bound, `status` mutable so phantoms are struck-not-deleted, `caused_by` = inverse of `caused`. CLAUDE.md §12 pointer (repo-specific section); OPEN_QUESTIONS Q28. On-branch DECISIONS_LOG #85–#88. Governance-only: 4 files, 264 insertions / 0 deletions — no feature code, no migration, no schema, no test delta. | **OWED** — governance fully built and verified pre-commit (§1–§11 byte-identical to master after CRLF-normalisation; shared block unchanged, so nothing propagates to `health-connect-app`); pushed to `origin/feat/feedback-ledger` this session (#90 sweep step 1). **Renumber debt at merge (consolidated, verified #91 sweep) — DECISIONS ×4, FEEDBACK ×1, QUESTIONS ×2:** DECISIONS `#85`–`#88` → next-free (master #85–#91 now taken); FEEDBACK `§12` → next-free (master's §12 is #88's unseeable-surface rule, §13 taken by #90, §14 by #91), and that heading's own `#85–#88` back-reference follows the renumber; OPEN_QUESTIONS `Q28`/`Q29` → next-free (master now occupies both). Owner: Luke. | Merge deliberately deferred ("not rushing master"); merging is out of scope for the #90 sweep, which pushes but does not merge. | Luke's merge decision. |
| `feat/interpretation-view-skeleton` | Increment 1 of 5 of the lab-interpretation view: static three-section render (What Moved → Stable → Mechanisms) of the contract v0.4 §2 worked example, committed verbatim as `frontend/src/fixtures/interpretationExample.json`; new `/interpretation` route behind `RequireAuth`; hybrid weighted-inline levers; colour-as-fact. New `frontend/src/components/interpretation/*` + `pages/InterpretationView.jsx`. Frontend-only — no backend, no migration, no schema. On-branch DECISIONS_LOG `#NEXT`. | **OWED** — increment-1 render complete and verified (`npm run build` green; colour-as-fact checked by computed style over every element — neutral ramp except the FSH line; gate-2 spine per-member traced: `hpg_axis` moves on `fsh` alone); pushed to `origin/feat/interpretation-view-skeleton` this session. Ships **INERT by design** (renders a fixture; the `context_builder` AI pointer swaps only at increment 5). Open loop = ff-merge; entry is `#NEXT` so it renumbers automatically — **no renumber debt**. **CONTRACT DRIFT — verified this sweep, must be resolved at merge:** the committed fixture's top-level keys are `['groups','meta']` and `ungrouped` appears NOWHERE in the branch's `frontend/src`, but master's #86 producer emits `{meta, groups, ungrouped}`. This branch was cut against the pre-#86 shape, so wiring the view as-is would silently DROP every ungrouped marker — the branch looks complete by merge position while consuming a superseded contract. Increment 2+ must consume the flat `ungrouped[]`. Owner: Luke. | No external precondition to land; merge deferred, out of scope for the sweep. Pushing it unblocks O3 as a side effect — closure, not licence to start it. | Luke's merge decision; increments 2–5 are separate future work. |
| `feat/checkin-injury-probe` | Probe tier of the injury-aware check-in: `backend/injury_probes.py` — a versioned, provenance-stamped scaffold for injury-type-specific probe questions, seeded with ONE worked example (gastroc strain); two structural gates (elicitation-only questions, referral-only escalation) enforced in tests and both mutation-tested; cadence from injury type + entry set-date. Strikes the stale "future" note in `docs/checkin-schema.md`. OPEN_QUESTIONS Q30/Q31. No migration, no schema, no frontend. 170 backend tests green (137 + 33 new). | **OWED** — scaffold built, both gates mutation-tested (elicitation gate fails a seeded imperative; referral gate fails a seeded clearance; negative controls in both directions); pushed to `origin/feat/checkin-injury-probe` this session. Ships **INERT by design** (no ledger entry carries `injury_type`; `evaluate_escalation` is a pure function with no caller). **Renumber debt at merge (consolidated, verified #91 sweep) — DECISIONS ×2, FEEDBACK ×0, QUESTIONS ×2:** DECISIONS `#89`/`#90` → next-free (both taken on master), and the `#89`/`#90` strings in `injury_probes.py` + `tests/test_injury_probes.py` docstrings must move with them; OPEN_QUESTIONS `Q30`/`Q31` → next-free (master now occupies both — Q30 `.gitattributes`, Q31 Known-issues table). **No FEEDBACK debt:** the branch references §8/§10/§11 but adds no section of its own. Owner: Luke. | No external precondition to land; merge deferred, out of scope for the sweep. | Luke's merge decision; feature goes live only when a real injury entry carries `injury_type: gastroc_strain`. |
| `feat/interpretation-producer-foundation` | Interpretation producer FOUNDATION (4a): `backend/interpretation/` (gates + `build_foundation`) over a new `labs_reads.marker_series` seam. Emits `{meta, groups[], ungrouped[]}` with 4a fields only — every 4b field absent. DECISIONS_LOG **#86**. | **DONE** — landed on master at `f02b4a2`. Backend-only, no migration. 3 concern-split commits: `012ef1d` (feature: series + gates + producer + tests) / `b249a60` (debt: declared-state #85 mint) / 4a governance. **206 tests green** (184 pre-existing incl. #85's 47, + 22 new). | — (no external precondition to land) | **Branch RESET this session:** its prior 3 commits (superseded groups-of-one design, never merged) were discarded — this brief re-specced ungrouped markers as a FLAT `ungrouped[]` list (not synthesised groups-of-one), a lab_flag-only gate 2, and an explicit-panel signature. **#86 provisional:** if `feat/feedback-ledger`/`feat/checkin-injury-probe` merge before this is pushed, they take #86+ in merge order and this renumbers. **Oracle caveat:** `INTERPRETATION_OUTPUT_CONTRACT.md` (corrected §2, UI-maintained per #63) is NOT in the repo; the fixture `backend/tests/fixtures/interpretation_s2.json` is the §2 worked example from `feat/interpretation-view-skeleton`, mechanical 4a fields stable across the 4b correction. That divergence risk has now occurred ONCE and been resolved (#87): the corrected contract file / asset carries `hepatocellular` display_name "Hepatobiliary enzymes + bilirubin", the fixture was re-synced to match, and the G3 oracle now asserts group display_name — so the re-sync mechanism is exercised, not theoretical. Re-sync again if it diverges on any further field. Gates G5/G6 mutation-tested; G1/G2 boundary greps clean (no current_state/declared_state import, no `levers[]` read); `latest_lab_results` byte-unchanged. **Unblocks 4b** (verdict/relations/levers, now phase-aware via #85). **Still dangling (not this session's concern):** `feat/interpretation-view-skeleton` (increment 1 render layer) remains an unmerged local branch — it consumes this producer's shape once wired. |
| `feat/protocol-declaration` | The prerequisite 4a spawned before 4b: a structured, queryable declaration of the user's stack. `backend/declared_state.py` (continuity-aware value schema + pure `derive_phase` + `is_assumable_present` + the `current_state` lift) and `_seed_declared_state` in `seed_engine.py` seeding the confirmed real stack under three new `user_knowledge_entries` types (protocol/supplement/behavioural). Closes the "no structured protocol" gap 4a surfaced; corrects the unreliable Clinical_Protocol.md. DECISIONS_LOG `#NEXT`. | **OWED** — landed on master at `b249a60` (declared_state + seed + governance); prod seed live (operator-confirmed, 23 rows written). Open loop = confirm `current_state.declared_state` READS BACK 23 factors in prod — the seed WRITE is attested, the read-back is not. Owner: Luke. DECISIONS entry **minted #85** (was an unminted `#NEXT` in master — the number-at-merge debt, reconciled 2026-07-18: it was already in master past #84, so it took the lowest free number; `feat/feedback-ledger` #85–88 and `feat/checkin-injury-probe` #89–90 are unmerged hardcodes that renumber at their own merges). Backend-only, **no migration**. **184 tests green** (137 + 47). | — (no external precondition to land) | **Numbering:** headed `#NEXT`; master max is **#84**, #85–#90 are local-only hardcodes on `feat/feedback-ledger` / `feat/checkin-injury-probe`. **MERGED ≠ LIVE:** `current_state.declared_state` reads EMPTY until the Railway seed runs — a Luke operator step post-merge (`seed_engine.py`, injury-ledger precedent). **Design call refining the brief, mutation-proven:** the row's `active` column ("this declaration is current", always True) is SEPARATED from `value["active"]` ("the user takes this"). The brief's literal reading — write the row inactive when the factor is untaken — was rejected because it (a) drops tirzepatide/glow/hgh/ultra_muscleze_night out of `current_state` (which loads active=True rows only), making their phase underivable, and (b) breaks idempotency (skip keys on active=True, so untaken rows duplicate every re-run — the mutation fails `assert 4 == 0`). `derive_phase` orders by CONTINUITY, never by value.active, so a stopped factor still resolves washout/stopped. **Phases verified end-to-end through current_state** against the brief's table: trt→steady, tirzepatide→washout, cjc_ipamorelin/igf1_lr3→episodic (active yet `assumable_present=False`), glow→stopped, hgh→None, cbt_i→re_entering, ultra_muscleze_night→None. Derivation mutation-proven non-vacuous 4 ways. **Seed corrects Clinical_Protocol.md** (HGH never used; peptides episodic-not-discontinued; GLOW = BPC-157/TB-500/GHK-Cu not KPV); tirzepatide date flagged triangulated-not-a-dosing-log. **Unblocks 4b** (phase-aware verdict/relations/levers). **Follow-on, separate concern:** surface `declared_state` into the context_builder system prompt (this branch deliberately does NOT touch prompt rendering). **Incidental:** untracked `backend/gate_test.py` (operator stray) fires a live paid Anthropic call at pytest COLLECTION and 400s a bare `pytest` — scope to `tests/`; not this session's to delete. **Loop (partially closed):** the Railway seed HAS run (23 rows, operator-confirmed). Still owed: confirm `current_state.declared_state` reads back 23 factors in prod. Pairs with `feat/interpretation-view-skeleton` (inc.1) and `feat/interpretation-producer-foundation` (4a), both in flight; none touch each other. |
| `fix/probe-harness-fidelity` | Make `probe_resolver.py` able to reach the code it measures, and unable to report success when it doesn't (FEEDBACK §11): per-probe subject expectations + non-zero exit, turns that drive to a block, constraint-neutral subjects, and the injury-refusal probe split onto its own pinned fixture. | **OWED** — merged (`adb67e8`, 2026-07-15; ff to master, local branch deleted, never pushed to origin) but the loop is NOT closed: the outstanding check is `/opt/venv/bin/python probe_resolver.py 1` in the container, which has never been run (operator-only). 2 commits: `5c5b43f` (instrument) / `adb67e8` (governance). B1–B4 rode one commit deliberately — B4's probe depends on B1's expectation machinery, so splitting would have landed a red intermediate. **Test instrument only — no production code path changed** (diff touches `backend/probe_resolver.py` alone). No migration. 137 backend tests green. | — (no external precondition to land) | **Verified under `--synthetic`, exit 0, both probes reach their subject:** resolver forced 3 guessed titles (`Calf Raise`, `Preacher Curl`, `Pullover`) in 2 turns with `suggest_candidates` actually called; injury probe confirmed no forbidden id reaches `create_routine`. B1 proven by reproducing the 2026-07-15 null result with a never-confirming probe → loud non-zero. **Finding (behaviour, not a defect):** the injury gate does not refuse wholesale — it refuses the MOVEMENT and substitutes a safe one ("Lower A (Injury Modified)": leg press + trap bar deadlift). A `provisioned == 0` predicate failed on the platform working correctly, so the expectation is now "forbidden template ids never provision". Loop closes on Luke: `/opt/venv/bin/python probe_resolver.py 1` in the container — the **live** run, which still has NOT been executed by Code (the container is operator-only). The injury probe is synthetic-only by design and will be skipped there. |
| `feat/resolver-candidate-suggestions` | Ranked catalogue candidates on an unresolved title (#83) — resolution stays EXACT, a unique candidate is never auto-resolved, fail-closed unchanged; plus `backend/probe_resolver.py` landed as an operator-run model-facing probe (#84). | **DONE** — all 5 concern-split commits landed on master (ff-merged, branch since deleted): `35f4d1c` (suggest_candidates) / `b21ce7f` (chat candidate warning) / `62bf248` (probe harness) / `40c8b0d` + `3feaa44` (#83/#84 governance, incl. ratio-tier exercised live). No migration, no schema change. 137 backend tests green. *Adjudicated by content, not `git cherry`: the branch ref no longer exists (neither local nor `origin`), so patch-id was unrunnable — instead confirmed #83/#84 present in `DECISIONS_LOG.md` and `suggest_candidates`/`probe_resolver.py` present in the tree, all five commits on `origin/master`'s history. The prior "IN FLIGHT / local-only, never pushed" label was stale — the work merged and the branch was deleted.* | — (no external precondition to land) | **C3 verified CLOSED** (the chat brief's hard stop): the warning DOES reach the model — actions appended at `chat.py:540` → `ChatResponse.response` → stored as the assistant message (`ChatPanel.jsx:82`) → echoed back as `conversation_history` (`ChatPanel.jsx:77-80`). Loop confirmed live: a 1/3 routine went 3/3 and provisioned on the turn after the warning. **LOOP CLOSED — verified in prod 2026-07-15**, live 494-row catalogue, real model, real user state: the model emitted `exercise_template_id` for in-history movements and a bare title (`Calf Raise`) for the out-of-history one — #82's contract followed exactly. Exact match missed; `suggest_candidates` returned 5 candidates, **all 5 genuine, zero noise** — `Seated Calf Raise`, `Standing Calf Raise`, `Standing Calf Raise (Smith)`, `Standing Calf Raise (Barbell)`, `Standing Calf Raise (Machine)`. Fail-closed held: nothing written until the user disambiguated ("standing machine"), after which the routine provisioned. **`_SUGGEST_MIN_RATIO = 0.5` — OWED ITEM NOW CLOSED (2026-07-15, later run):** the repaired probe forced three guessed titles live and the ratio tier fired for the first time — `Preacher Curl` → `Rope Cable Curl` (0.643) / `Drag Curl` (0.636); `Pullover` → `Pull Up` (0.533). The floor admits noise; containment-first ranking put every genuine candidate above every ratio-tier one, and `limit=5` bounds the tail. Measured, and it held — but ranking is what carries it, not the threshold. The floor must NOT be raised on this evidence: 0.512 (a real candidate) sits below 0.533 (noise), so ratio does not separate signal from noise in either direction. See #83's ratio-tier addendum. |
| `feat/hevy-resolver-activation` | Fire the landed-but-dormant #60/#61 title→id resolver: history renders CATALOGUE titles via an upstream `canonical_title` join (#81), the provisioning contract permits title emission with EXACT matching retained (#82), and the #43 context-builder parity guard is narrowed rather than retired (#80). Strikes the ROADMAP "Hevy resolver activation" NEXT row. | **OWED** — code landed + deployed at `9193453` (ff-merged, pushed; local branch deleted). 4 concern-split commits: `bc81911` (#81 catalogue titles) / `8ca6108` (#82 title emission + #80 guard narrowing) / `320eedf` (B3 pin) / `9193453` (governance). No migration, no schema change. 124 backend tests green. Limb 1 of the loop is prod-proven; **limb 2 is outstanding** = a live chat request naming a nonsense movement, confirming the routine is refused AND that the unresolved title is named back to the user. Owner: Luke. | — (no external precondition to land) | **B0 finding:** the "byte-parity guard" named in ROADMAP/BRANCHES prose is `test_context_builder_output_unchanged_pre_post_refactor` (`tests/test_current_state.py:138`) — a full-string old-vs-new assertion against pinned SHA `3360ed5`. It is UNRE-BASELINEABLE (the SHA cannot move without going old-vs-old, per the test's own comment), hence #80's narrowing. **B3 was a no-op:** `_process_routine_actions` already surfaced unresolved titles and refused the routine; no silent-failure path existed. **Loop CLOSED — evidence, not expectation (adjudicated #90 sweep).** "The resolver has never fired in prod" is now DISPROVEN, on master-canonical evidence independent of `feat/resolver-candidate-suggestions`'s (stale) row: DECISIONS_LOG **#83 "SCALE ADDENDUM (2026-07-15)"** and the immutable-dated governance commit **`40c8b0d`** (2026-07-15) both record a live 494-row prod run, real model, real user state — the model emitted `exercise_template_id` for in-history movements and a bare title (`Calf Raise`) for the out-of-history one (#82's contract exactly), exact match missed, fail-closed held until the user disambiguated, **after which the routine provisioned**. **Limb 1 — CLOSED on that evidence.** **Limb 2 — OUTSTANDING, which is why this row is OWED and not DONE:** "nonsense movement → routine refused with the title named" is a *distinct code path*, not a corollary of limb 1. `suggest_candidates → []` (refuse and name the unresolved title) is a different branch from `suggest_candidates → [5]` (fail-closed pending disambiguation, which is what the 15 Jul run exercised), and the refusal string is the user-visible surface — so it is provable only by running it. Outstanding: a live chat request naming a nonsense movement; confirm the routine is refused and the unresolved title is named back. Owner: Luke. |
| `fix/exercise-tag-coverage` | Fix the one unresolved tag title (`Bulgarian Split Squat` → `Bulgarian Split Squat (Dumbbell)`, keyed to the catalogue not the workout log) + new read-only `backend/audit_exercise_tag_coverage.py` measuring the ID-keyed fallback hit-rate; extracts the three-state rule to `selection.classify_coverage` so the audit and the read path share one definition (DECISIONS_LOG #79) | **DONE** — landed on master at `8a29b1f`, pushed; local branch deleted (never pushed to origin). 3 concern-split commits: reference fix (`68e3c9e`) / audit+extraction (`727adb3`) / governance (`8a29b1f`). No migration, no schema change. 105 backend tests green. | — (audit `--strict` exit-1 path still NOT live-exercised: the prod window had zero untagged movements, so the branch never fired. Unit-tested only.) | **LOOP CLOSED 2026-07-14.** Both operator steps ran in the Railway backend container (`root@3752627012b6:/app`), both verified: (1) `seed_exercise_region_tags.py 1 --confirm` → `titles_resolved: 56`, `titles_unresolved: 0`, `tags_written: 37` (36 + the now-resolving BSS row), `no_pattern_adjudicated: 21`, `confirmed: True`; (2) `audit_exercise_tag_coverage.py 1 --days 28` → **fallback hit-rate 0.0%** over 38 distinct movements (25 TAGGED / 13 ADJUDICATED NO-PATTERN / **0 UNTAGGED**), 12/45 workouts in window. The keyword fallback no longer fires on the active window — the behavioural claim the seed alone could not make. **#79 confirmed in prod, not merely argued:** `B5D3A742` resolves TAGGED as `Bulgarian Split Squat (Dumbbell)` while still `logged as: Bulgarian Split Squat` — a title the catalogue does not contain, which a title-keyed audit would have scored as a miss. (The earlier title-keyed pass scored 38/38 and the id-keyed pass also reports 38 distinct; the figures coincide, but only the id-keyed one decomposes 25/13/0 and only it is sound.) Hevy fetch + `user_hevy_key` now live-verified (5 pages, all HTTP 200) — the invalid-key failure mode did not materialise. |
| `fix/hevy-template-sync-activation` | Activate the Hevy template sync at the OPERATOR layer: new `sync_hevy_templates.py` CLI (`--user-id` safety valve, non-zero exit on empty/partial), per-user error isolation + loud empty-list signal in `sync_exercise_templates`, empty-substrate precondition gate on the seeder (DECISIONS_LOG #77) | **DONE** — landed on master at `811ef6e`, pushed; local branch deleted (never pushed to origin). No migration. 94 backend tests green. | — (wiring/resilience only; the three template features #60/#61, #65, #74-#76 were inert on an unpopulated prod substrate) | **LOOP CLOSED 2026-07-14.** Both operations ran against Railway, both verified: (1) `sync_hevy_templates.py` → `hevy_exercise_templates` = **494 rows** (non-zero; the precondition gate the branch shipped is what made (2) meaningful rather than a silent 0-row no-op); (2) `seed_exercise_region_tags.py 1 --confirm` → **55/56 titles resolved**, 36 `exercise_region_tags` rows, 55 templates adjudicated, alembic head `c3a2d8e5f109`. The single unresolved title (`Bulgarian Split Squat`) was a reference-data defect, not a sync defect — fixed on `fix/exercise-tag-coverage` (DECISIONS_LOG #79). |
| `feat/tag-adjudication-three-state` | Three-state tag coverage (tagged / adjudicated-no-pattern / untagged) via `hevy_exercise_templates.adjudicated_at`; reject calf→ankle_df; corrected tag file moves four strength-lift families to `no_pattern` (DECISIONS_LOG #76, resolves Q26, opens Q27) | **DONE** — landed on master at `1bd4793`, pushed; local branch deleted (never pushed to origin). Migration `c3a2d8e5f109` (head); Railway auto-runs `alembic upgrade head` on deploy. 87 backend tests green. | — (feature INERT until seeded, as with #74) | **LOOP CLOSED 2026-07-14** — both checks verified ((1) and (2) below). (1) DONE: `seed_exercise_region_tags.py 1 --confirm` against Railway → 36 tag rows, **55 templates adjudicated** — the three-state substrate is live, so `adjudicated_at` now distinguishes no-pattern from never-looked in prod. (2) **DONE — G5 CONFIRMED IN PROD 2026-07-14** (operator run): 3× `sync_hevy_templates.py --user-id 1` (1,482 upserts) left `exercise_region_tags` at 36 rows / tag fingerprint `5ed53a9f018b13c5288a4ac6342a090e` unchanged, and `hevy_exercise_templates` at 55 adjudicated / 55 lateralised / meta fingerprint `d1979d0b8326de4bef7f790c30667062` unchanged. `_upsert_template` provably never assigns `laterality` or `adjudicated_at`. **Loop closed.** *Baseline caveat: those fingerprints are pre-BSS (36 tags / 55 adjudicated); prod is now 37/56 after the #79 re-seed, so they are a dated, SUPERSEDED baseline — not a reproducible current check. The result stands regardless: the clobber mechanism is indifferent to row count.* Q27 (v1 strength-ratio axis) remains a separate future design pass. |
| `feat/exercise-catalogue-taxonomy` | Replace the `_LOADED_KEYWORDS` substring matcher with a real taxonomy join: new `exercise_region_tags` table + `hevy_exercise_templates.laterality`; `infer_loaded_regions` rewritten to a table lookup with the keyword map demoted to an instrumented fallback (exercise-catalogue sequence step 1 of 4; DECISIONS_LOG #74/#75) | **DONE** — landed on master at `814890a`, pushed; local branch deleted (never pushed to origin). Migration `b2f1c9a4d7e8` (head); Railway auto-runs `alembic upgrade head` on deploy. 86 backend tests green. | — (feature code is INERT until tags are seeded — with no tag rows, `infer_loaded_regions` falls back to the keyword path = pre-land behaviour, so the land ships no behavioural change) | **LOOP CLOSED 2026-07-14** — (1), (2) and (3) all verified. (1) DONE: reference confirmed and seeded with `--confirm` (human adjudication stamped). (2) DONE: `seed_exercise_region_tags.py 1 --confirm` against Railway → 55/56 resolved, 36 tag rows over a 494-row substrate. The feature is **no longer inert** — tagged templates now take the authoritative join instead of the keyword fallback. (3) **DONE — G5 CONFIRMED IN PROD 2026-07-14** (operator run): 3× `sync_hevy_templates.py --user-id 1` (1,482 upserts) left tags and template meta byte-identical — `exercise_region_tags` 36 rows / fp `5ed53a9f018b13c5288a4ac6342a090e`; `hevy_exercise_templates` 55 adjudicated / 55 lateralised / fp `d1979d0b8326de4bef7f790c30667062`. `_upsert_template` provably never assigns `laterality` or `adjudicated_at`. The S1 table split holds under a real resync. *Fingerprints are pre-BSS (36/55) and thus a dated, SUPERSEDED baseline — prod is now 37/56 — but the mechanism is row-count-indifferent, so the result stands.* **Loop closed.** The separate question — whether the fallback stopped firing — was answered by `audit_exercise_tag_coverage.py` (DECISIONS_LOG #79): **hit-rate 0** in prod, same date. |
| `feat/constraint-consumption` | Injury constraints consumed, not decorative: AM soreness items ← active injury ledger; injury `trajectory` in JSON `value` + divergence / symptom-gated review surfacing in `get_readiness_snapshot`; `naive_baseline` soreness term = max across reported sites (constraint-consumption brief, Steps 1–4; DECISIONS_LOG #72/#73) | **OWED** — landed on master at `e70437b`. Open loop = browser-verify `CheckInAM.jsx` renders the injury-derived AM soreness items (logic is unit-verified only). Owner: Luke | — (backend + frontend, no migration, no external precondition) | Code side complete. **Railway seed DONE 2026-07-13** — `seed_engine` run against prod Postgres (service `health-app-DB`) via `DATABASE_PUBLIC_URL`: `injury ledger rows written: 2` (right hamstring + pes anserine, each with trajectory; the 3 pre-existing injuries skipped unchanged, add-only seed). Remaining (organic, not a code gap): trajectory divergence/review flags surface in `get_readiness_snapshot` only once (a) the backend redeploys with the Step-3 code and (b) an AM check-in records soreness under the derived keys. Frontend `CheckInAM.jsx` not browser-verified (logic unit-verified). Note: `get_readiness_snapshot` via the MCP connector appears to read a non-prod DB (it showed the new injuries before any prod seed succeeded) — verify prod directly with a Postgres query, not the tool. |
| `feat/hevy-exercise-template-resolver` | Hevy exercise-template synced store + title→id resolver (DECISIONS_LOG #60/#61) | **DONE** — ff-merged to `master` at `41a8998`, pushed; local branch deleted. Loop CLOSED on evidence: prod is 3 migrations past the resolver's own stamp. | — (prod-stamp precondition `217dce22fbc5` confirmed before land) | **Loop CLOSED — evidence, not expectation (adjudicated #90 sweep).** The loop watched for prod `alembic_version = 3497ab483935` — which IS this feature's own migration (`3497ab483935_add_hevy_exercise_templates`, the migration that CREATES the `hevy_exercise_templates` table). Repo chain: `3497ab483935` → `f4e1a2b3c6d7` → `b2f1c9a4d7e8` → `c3a2d8e5f109` (head), so the stamp is a direct ancestor of head, 3 steps back. Prod reached it: a master-recorded operator run (2026-07-14, `sync_hevy_templates.py`) shows `hevy_exercise_templates` = **494 rows** — a table `3497ab483935` creates, so its population is direct proof the migration applied — and reports alembic head `c3a2d8e5f109`. Prod is past `3497ab483935`; the stamp check is superseded. |
| `feat/hevy-create-loop` | `create_and_resolve` — app-originated custom exercise via create→sync→list-back (DECISIONS_LOG #65, resolves Q14) | **DONE** — landed on master at `e13e3a2`, pushed; local+remote branch deleted | — (backend-only, no migration, no external precondition) | Code side complete. `create_and_resolve` is a library function; resolver-activation wiring stays dormant (unchanged this session). |
| `feat/connector-error-policy` | Connector failures decouple from session auth (401→424, read handlers → 502, global 500 CORS guard) + "See all" true pagination (DECISIONS_LOG #66, #67) | **OWED** — code landed + deployed at `51c7091` (ff-merged, pushed; local branch deleted). Open loop = live "See all" E2E through the deployed backend against a valid Hevy connector key: an account with >1 page of workouts, confirm pagination reads past page 1. Owner: Luke. | — (backend + frontend, no migration, no external precondition) | **Stays OWED (adjudicated #90 sweep) — no evidence to close on.** git log on master shows only the landing commit `51c7091` and its BRANCHES governance — no later commit records a live See-all verification; the row's own history says NOT E2E-verified (connected key was invalid). The brief's expectation ("key replaced 12 Jul, See-all verified live") has no master/prod artifact behind it. A Hevy-MCP probe cannot vouch: it uses its own `HEVY_API_KEY`, not health-app's stored connector key, and does not exercise health-app's See-all endpoint. Worst case still degrades to page 1, guarded by the 502 helper. |
| `fix/hevy-exercise-history-path` | Correct `get_exercise_history` path to `/v1/exercise_history/{id}` (DECISIONS_LOG #69, resolves Q16) | **DONE** — landed on master at `2fc21e2`, pushed; local branch deleted (never pushed to origin) | — (backend-only, no migration, no external precondition) | Code side complete. Doc-evidence basis (official docs + 3 clients); live `exercise_history` corroboration optional belt-and-braces later (local Hevy MCP hung this session). Method currently unwired — zero call sites — so no downstream behaviour-shift. |
| `fix/hrv-sleep-integrity` | Ingest bounds guard + deep-sleep excluded from daily readiness (DECISIONS_LOG #70, #71 — HRV & Sleep Data Integrity brief Tasks 3, 4) | **OWED** — code landed at `dccca41` (ff-merged, pushed; local branch deleted). Open loop = Task 3 Railway history sweep: apply the ingest-bounds guard as a backfill over historical HRV rows (sweep SQL supplied for Railway, never run — the local DB is dev SQLite). Owner: Luke. | — (backend-only, no migration, no external precondition) | **Stays OWED (adjudicated #90 sweep) — genuinely outstanding.** The bounds-guard + deep-sleep-exclusion code is DONE on master; the Task 3 historical sweep has no master/prod artifact and cannot be run from this session (Railway is operator-only). Both fixes are independent of the HRV-metric question (Q17, resolved → #89). |
| `chore/markitdown-mcp` | Adopt MarkItDown as the document→markdown ingestion path: `markitdown` MCP at **user scope** (`uvx markitdown-mcp`) + `markitdown[pdf,docx,pptx,xlsx,xls]==0.1.6` CLI; CLAUDE.md repo-canonical Tooling section + DECISIONS_LOG #78 (deterministic + cheap on born-digital prose; table structure flattens, scanned/broken-font → cid garbage — native-vision fallback recorded) | **DONE 2026-07-14** — ff-merged to `master` at `de38820`, pushed; local branch deleted. Machine-local tooling only, no repo code, no migration. Nothing outstanding: the durable Claude Desktop registration was declined as low-value (it only buys the weaker in-context path), so no open loop remains. | Machine-local: the MCP registration + CLI install do not replicate across machines — re-run the setup on any new machine. Durable Claude Desktop registration declined as low-value; if ever wanted, quit Desktop FIRST then edit `claude_desktop_config.json` (the running app rewrites it and drops edits), or Settings → Developer → Edit Config + restart. | — (complete; no open loop). |
| `chore/process-notes` | Session process notes off the #78 work: FEEDBACK.md #9 (Bash tool is Git Bash/POSIX — use `<<'EOF'`, never PowerShell `@'...'@`) + CLAUDE.md Tooling note loud-vs-silent axis appended to the MarkItDown Limits (refines DECISIONS_LOG #78, does not edit it) | **DONE** — landed on master at `cd87e64`, pushed; local branch deleted (never pushed to origin). Docs only, no repo code, no migration, DECISIONS_LOG untouched. | — (governance notes only; no code precondition) | — (complete). |
| `feat/recovery-metrics-rhr` | Expose RHR as a series in `get_recovery_metrics` (HRV & Sleep Data Integrity brief Task 2) | **UNSTARTED** — 1 commit at `a4e1887`, pushed to `origin/feat/recovery-metrics-rhr` 2026-07-21 under #98's push-while-holding rule (it was the last local-only branch, and a local-only branch is unreadable to chat); rowed here, not in undefined limbo. Its blocker (the Q17 (A)/(B) gate) is **resolved** (→ #89): Q17 closed on **(A) instrumentation**, so no physiology gate remains to hold it. | Prior rationale (scraper RHR shares HRV's *device family*) is superseded by a stronger one: `last_shr`/`sleep_hr_bpm` was **itself phantom-affected** — HCA #19 fixed it in the same commit as HRV — so scraper RHR is not a clean (A)/(B) discriminator either; it moved for the same instrumentation reason. Health Connect `resting_heart_rate` (`health_connect_syncs`) remains the only independent path. | — (gate resolved; pick up as a standalone RHR-series feature whenever wanted. DECISIONS entry numbered at merge.) |
| `feat/cbti-module` | CBT-I data substrate (phase 1, brief Steps 1–4): migration `e5f2a9c7b104` adds 9 nullable AM-moment sleep-diary columns to `daily_records` + two append-only ledgers (`cbti_blocks`, `cbti_prescriptions`, append-only as a model+application invariant with `ck_cbti_prescription_decision` the one DB-enforced constraint); `import_cbti_block.py` loads the completed 2026-03-19→05-13 block (VA CBT-I Sleep Diary Calculator export) to Railway with ±0.001 SE reconciliation + negative control. DECISIONS_LOG #107/#108/#109, OPEN_QUESTIONS Q42. | **OWED** — Gates 1–4 done; 5 commits, **rebased onto `origin/master`** (pre-rebase range `f0899eb`→`5fb625d`, recovery ref `backup/cbti-pre-rebase`); held for chat review, **not merged**. 223 backend tests green (was 206; +10 substrate, +7 import). Migration verified up/down/up on SQLite in isolation AND applied to Railway (Postgres now at `e5f2a9c7b104`). Import verified against the DB: 1 block / 9 prescriptions / 53 daily_records, DB-stored `diary_se_pct` reconciles 0/53, exit aggregates (TST 428m / SE 89.6%) reproduce #107's decline figures. Open loop = **ff-merge after review**. Owner: Luke. | — (no external precondition to land; held for review per #98 push-while-holding). | **PROD SCHEMA AHEAD OF MASTER:** the migration is applied to Railway (prod at `e5f2a9c7b104`) but the migration FILE lives only on this branch — master's chain still ends at `c3a2d8e5f109`. Merging reconciles this; abandoning the branch would strand a prod migration with no master file. **Renumber debt — RESOLVED at the rebase:** the branch was rebased onto `origin/master` after master took #104/#105/#106 (safety-threshold gate) and Q41 (haematocrit citation capture). The CBT-I entries were renumbered by anchored pattern to **#107/#108/#109** and **Q42**, and the brief-provisional `#101`/`#102` left dangling in `models.py` / `import_cbti_block.py` were corrected to #107/#108 in the same pass. Master max at rebase: **#106 / Q41**. If `feat/feedback-ledger` (#85–88) or `feat/checkin-injury-probe` (#89–90) ff-merge before this one, these renumber again above them. **Phase 2 (brief Steps 5–7) is a separate future brief:** titration engine, AM/PM surfaces + the 12h-clock prefill gate (Q42), ISI capture — none started. |
