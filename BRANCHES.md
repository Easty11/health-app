# BRANCHES — every branch not master lives here until merged+deleted

Status is the four-state vocabulary — DONE / BLOCKED / OWED / UNSTARTED — per the shared
loop-rules block in `CLAUDE.md`. No fifth state. BLOCKED must name a blocker that holds
*now* and an owner; where the evidence does not settle barrier-vs-trigger, the row is
UNSTARTED.

| Branch | Purpose | Status | Detail | Blocker / outstanding (owner) |
|--------|---------|--------|--------|-------------------------------|
| `gov/248-gate3-deploy-verified` | Post-land record for Gate 3 (`#248`): the merge SHA (`e289c12`) and the live deploy verification (deploy `bfc1a5e2` SUCCESS, boot log `1341a2cf6938 -> 334526269006, add_load_metrics` + `Application startup complete`) that `feat/q6-gate3-load-metrics`'s own row could not name pre-merge. `#248` Status + the gate-3 BRANCHES row. Governance-only. | DONE | Self-merged on green per the standing rule. `#176`(b): `DECISIONS_LOG` #248 Status + BRANCHES; no code, no `#NEXT`. `#176`(c): removed lines are the declared replacements (the "cannot name its landing SHA" clause; the #248 Status recompute-order sentence, re-emitted with the deploy line). Row asserts DONE ahead of merge. The `load_metrics` rollup (populating rows) remains operator-run in-container. | — (Luke) |
| `feat/q6-gate3-load-metrics` | Q6 gate 3 — `load_metrics` daily rollup of `load_events`: per-window Banister Fitness/Fatigue/Form (`#32` τ: mech 10 / nm 6 / metab 4; fit τ=42; form=fitness−fatigue, k=1) + `#33` ΔLoad acute(7d):chronic(28d) ratio. Migration `334526269006` (chained on single head `1341a2cf6938`), `LoadMetric` model, `backend/load_metrics.py` (reads load_events only, D-B; delete-and-reinsert per (user, formula_version, metrics_version); `metrics_version 'banister-v1'`; fail-closed psychological — Q122; metabolic provisioned), unit + reconciliation tests. Day grain = user-local AEST via `_local_day`. `DECISIONS_LOG` `#248` (gate-3 rollup) + `#249` (ΔLoad), `OPEN_QUESTIONS` Q122, SCHEMA 028, ROADMAP row-79 gate-3 DONE + two supersession corrections (`#247` NM, `#246` rename). | DONE | Built, tested, and RELEASED (operator, 2026-08-28) — merged via **PR #121** (merge commit `e289c12`; SHA recorded in this follow-up governance touch). **Live-verified:** deploy `bfc1a5e2` SUCCESS, boot log `1341a2cf6938 -> 334526269006, add_load_metrics` + `Application startup complete`. Gates discharged with real execution (deps in a venv): full backend suite **1225 passed / 1 skipped** (sole failure = the pre-existing `3360ed5` shallow-clone artifact, unrelated); `load_metrics` tests incl. the reconciliation oracle **54 passed**; alembic scratch **upgrade `1341a2cf6938 → 334526269006` + downgrade clean**, `load_window` present, no bare `window`. `load_metrics.py` reads `load_events` only (grep-clean). Concern-split commits: migration (S2) · model+transform+tests (S3/S4/S5) · governance (S6) · test-fix. `#NEXT`/`#NEXT+1` resolved → `#248`/`#249` at land (master max re-read `#247`, no advance); Q122 concrete. S1 CONFIRMED (true UTC → `astimezone`, no flip). | **Nothing outstanding pre-merge.** Post-merge (operator-side, in-container): run the `load_metrics` rollup (`railway ssh --service health-app-backend` → `cd /app` → `/opt/venv/bin/python load_metrics.py`) and face-validity the mechanical/neuromuscular curves. Standing rule: any later `bw_fraction`/template change → BOTH recomputes in order, `load_events` then `load_metrics`. Owner: Luke. |
| `gov/247-closing-figures-correction` | Supersede `#243`'s "`#245` = 0 / all `bw_fraction` NULL at recompute" closing-figures claim — the operator's reversible prod experiment (2026-08-28) showed nulling Push Up (`392887AA`) raised Mechanical +821.1 (924,082.5234 → 924,903.6234) and restoring reverted it exactly; the 17-template tagging pass was live at the 27 Aug recompute (`audit_bodyweight_templates.py --strict` exit 0). So both figures reflect `#243`+`#244`+`#245`, and the NM movement is recorded as "≥ +41.66 `nm_au` from `#244`" (net of `#245` through `I` on mixed-logging templates), not "exactly". Root cause cited as `FEEDBACK` §18 (state inferred from an adjacent attestation — `#245`'s closeout — is not measured state). `DECISIONS_LOG` **#247** (append, supersede-not-amend) + CLAUDE.md Recent-landings pointer. Governance-only. | DONE | Self-merged on green per the standing rule (governance PRs ready-for-review, `--merge`, delete branch). `#176`(b): `DECISIONS_LOG` #247 (pure append) + the CLAUDE.md pointer; no code, no `#NEXT`. `#176`(c): the only removed line vs master is the declared Recent-landings pointer replacement — `#243`'s block is left as-landed (superseded by `#247`, not edited). Row asserts DONE ahead of merge (a row riding its own branch cannot name its landing SHA). | — (Luke) |
| `gov/246-load-window-landed` | Post-land housekeeping for the `#246` `load_window` rename: flip the `feat/load-window-rename` row BLOCKED→DONE with merge/deploy evidence, update `#246` Status to LANDED + live-verified (deploy `7fa6a32e` SUCCESS + the boot-log migration line), and roll the Recent-landings cap-3 (`#246` on, gate-1 `#239/#240` off). Governance-only. | DONE | Self-merged on green per the operator's standing rule (governance PRs open ready-for-review, `--merge`, delete branch — never held for review). `#176`(b) batch: `BRANCHES` + `DECISIONS_LOG` #246 Status + CLAUDE.md Recent-landings; no code, no `#NEXT`. `#176`(c): every removed line is a declared replacement — the stale `feat/load-window-rename` status/detail cells, the `#246` Status paragraph, the tier0-v1 landings tail clause, and the rolled-off gate-1 pointer. Row asserts DONE ahead of merge (a row riding its own branch cannot name its landing SHA). | — (Luke) |
| `feat/load-window-rename` | Rename `load_events.window` → `load_window` — `window` is a Postgres reserved word that forces `"window"` quoting in every hand query and would force it into gate 3's `load_metrics` rollup. Migration `1341a2cf6938` (`op.alter_column` rename on the single head `d4a1f8c609e2`, symmetric downgrade); model attribute + the `load_events.py` insert kwarg + six `test_load_events.py` sites + SCHEMA 027 DDL all → `load_window`; the unique-constraint and index NAMES kept (Postgres `RENAME COLUMN` rewrites their definitions, and the names never appear in a query). Resolves the `#243` window rename-vs-quote open item (operator adjudicated: rename). `DECISIONS_LOG` **#246**. Column-rename only — no data change, no recompute. | DONE | Merged via **PR #116** (merge commit `7bed138`, `--merge`), branch deleted — self-merged on green after the operator released the `#238` hold by explicit instruction. Deploy `7fa6a32e` **SUCCESS**; backend boot log carries `INFO [alembic.runtime.migration] Running upgrade d4a1f8c609e2 -> 1341a2cf6938, rename load_events.window to load_window` then `Application startup complete` — single head applied, no `Multiple head revisions` signature. `#246` stood at master max `#245` (no advance at the merge instant). Suite not run in the authoring sandbox (sqlalchemy absent) — tests build the schema from the model via `Base.metadata.create_all`, so the model rename is what they exercise. **Suite discharged (operator, 2026-08-28, `8c6b04e`): `test_load_events.py` 43 passed; full backend suite 1216 passed, 0 failed.** | — (Luke) |
| `claude/post-243-nm-delta-t5xrbv` | `#243` closing-figures recording — trace and record the post-`#243`/`#244`/`#245` operator recompute (deploy `9583a992`): Mechanical 924,082.523 `kg_reps` / Neuromuscular 522.480 `nm_au` over 54 sessions, ranking pass, and the +41.66 `nm_au` NM movement the `#243` "unaffected" line did not cover. Traced (a)→(b)→(c): (a) impossible (`e1rm_samples` filters non-rep/weight-NULL, so `#243`'s non-rep skip cannot shift the e1RM fit; NM Δ = 0), (b) = `#244`'s `floor(10−RPE)` RIR convention (owns +41.66 outright — `bw_fraction` all NULL so `#245` = 0 both windows), (c) ruled out by identical diagnostic shape. Plus CLAUDE.md prod-tooling (`railway ssh` recompute route, `window` reserved-word quoting, Windows psql `\encoding UTF8`) and the ROADMAP Gate-3 timezone-bucketing prerequisite. Governance-only. | DONE | Guard-green, LANDED via **PR #115** (merge commit, `--merge --delete-branch`). Under `#176`(b): feature-free governance batch, commit `6d756a2` + closing-figures refinement (`bw_fraction` all-NULL → `#245` = 0; Mechanical = `#243`+`#244`, not `#243` alone). **No `#NEXT`** — references concrete `#243`/`#244`/`#245`, so nothing to resolve at merge. `#176`(c): the only removed lines vs master are the two declared replacements (CLAUDE.md recompute sentence; the single-line ROADMAP row 79, re-emitted intact). Row asserts DONE ahead of merge — a row riding its own branch cannot name its landing SHA. Self-merged on operator instruction (the `claude/<hash>` ban is a branch-name convention, not the `#238`-removed merge gate; not server-enforced by `master-pr-gated`). | The `#243` window rename-vs-quote open item is **operator-resolved → rename to `load_window`**, sequenced as its own **held** migration PR (`#238` schema exception). `bw_fraction` tagging + recompute is operator-owned, pre-Gate-3. Owner: Luke. |
| `claude/q6-gate2-load-events-9kejqs` | `bw_fraction` (`#245`) — per-template bodyweight fraction, promoted from Q121 gap 4 to land ahead of gate 3. Migration `d4a1f8c609e2` adds `hevy_exercise_templates.bw_fraction`; transform scales only 0/NULL-weight rep sets (logged weight untouched); worklist `audit_bodyweight_templates.py`; 13 Jul fixture updated (WDB@0.25 → 26,207.575); `#245` + Q121 gap-4-resolved + SCHEMA + ROADMAP. `formula_version` stays `tier0-v1`. | DONE | Built + test-proven, LANDED. `#NEXT`→`#245` resolved at land (master max #244 re-read). Merged via PR #112 (merge commit) after operator released the `#238` schema-migration hold; branch deleted. `formula_version` stays `tier0-v1`; deploy verified + migration `d4a1f8c609e2` in boot logs. Follows on operator side: tagging pass + recompute + ranking re-read, then gate 3. | — (Luke) |
| `claude/q6-gate2-load-events-9kejqs` | Q6 gate 2 — the Tier-0 `load_events` transform (D-C/D-D). Migration `c7d9e2f14a86` adds the `load_events` store; `backend/load_events.py` derives per-session-window Mechanical/Neuromuscular load from `hevy_workouts.raw` (`formula_version 'tier0-v1'`, epoch-gated missing-RPE rule, e1RM rolling-60d fit, D-E halving, D-D non-rep bridging, `excluded_at` respected, gap-recording `provenance`); `LoadEvent` model + SCHEMA.md 027; `hevy_workouts._rpe_coverage` corrected (reps-based, excluded-aware). Governance: gate-2 DECISIONS_LOG entry + Tier-0-gaps OPEN_QUESTIONS item + ROADMAP. | DONE | Built + test-proven, **review-corrected**, LANDED. Review caught two blocking defects from stale ROADMAP-row-79 wording — epoch-gated RPE and laterality halving in the load path — both fixed: RPE is per-set/date-independent, load sums sets as logged, epoch is diagnostic-only. Feature `dd7193c` (store + transform + `_rpe_coverage` fix) · `41d0cdb` (review defect fixes) + governance `4402eae`/`14024de`; `#NEXT`→`#241`, `Q#NEXT`→`Q121` resolved at land (master max #240/Q120 re-read). Merged via PR #107 (merge commit, `--merge`) after operator released the `#238` schema-migration hold; branch deleted. **Live-verified on prod (`#242`): Mechanical 3,056,351.056 `kg_reps` / Neuromuscular 480.818 `nm_au` over 54 sessions; Q6 DONE.** Session close-out (`#242`, FEEDBACK §1 standing correction, container-tooling note) rode the restarted branch (same name, from `c36825d`) as a follow-up governance PR. | — (Luke) |
| `claude/hevy-strength-persistence-24nkkj` | Q6 gate 1 — the Hevy strength → four-window load persistence layer, plus its FK-ordering fix and the test-substrate hardening. `#103` (215435f) `hevy_workouts`/`hevy_sets` migration `f9a2c1d40b73` + ingestion + laterality pairing (D-E) + usage-joined coverage audit + Q6/Q115/ROADMAP governance (`#239`, D-A..D-G). `#104` (b42e32a) the `db.flush()` parent-before-sets FK-ordering fix. `#105` (d5875a3) suite-wide SQLite FK enforcement + 71 seeded fixtures. Close-out `#240`. | DONE | Merged 215435f (#103) · b42e32a (#104) · d5875a3 (#105); branch remote-deleted after each. Backend deploy 9b6ad5de SUCCESS on d5875a3; live backfill clean (54/1609 effective after dedup adjudication). | — (Luke) |
| `feat/hc-sync-contract-collapse` | Collapse `/health-connect/sync` from dual-name acceptance to HCA's mapped names alone and make a name break FAIL LOUD, superseding `#174` (Q5). Six dual-name branches deleted (`beatsPerMinute`/`bpm`, `heartRateVariabilityMillis`/`rmssd`, steps `startTime`/`date`, `exerciseType`/`type`, envelope `exercise`/`workouts`, `dataOrigin`/`sourcePackage`); `all_exercises()` deleted with branch 5 (the collapse breaks it), `get_kg()`/`get_meters()`/`sport_name_for` untouched (collapse doesn't reach them). Loudness built: required canonical fields + `extra="allow"` (missing→422, surplus→retained), `type: int` (a required `Any` accepts null), a shape-only reject diagnostic in a new `hc_sync_diagnostics.py` wired via `main.py`, and per-stream `received`/`aggregated`/`unattributed` counts on the response. `DECISIONS_LOG` **#234** (supersedes #174) / **#235** (loudness + response contract) / **#236** (E3 source-neutral contract); `OPEN_QUESTIONS` **Q118** (metadata persistence), **Q119** (windowed backfill recovery path), **Q5 → DONE → #234**. | **DONE — code, full human review (NOT the guard-only governance path).** Six commits, concern-split: fixture (`7ac3083`), Step-2 collapse (`579ca32`), Step-3 loudness (`ab88849`), Step-4 diagnostics incl. the `main.py` crossing as its own commit (`0e47999`), Step-5 counts (`ca2ef2d`), this governance commit. Backend **1113 → 1137**, frontend **47**, zero unadjudicated changes. Every gate exercised: mutation battery (§18) incl. a self-caught harness no-op; paired positive controls (§17); GATE 4's loc-parse bug caught by its own test; GATE 5 reconciliation asserted per-count, not by sum identity. `#176`(c): the only `.md` removals are declared-region Status/State edits (#174 supersession marker as a pure insertion; Q5 State). Numbers written as literals against master max re-read `#233`/`Q117`; **re-verify and re-resolve at the merge instant** if master advanced. | **LANDED ≠ LIVE — the counts are consumed by no client: `SyncScreen.js` discards the sync response at HCA `7a63b15`, so the operator surface is Railway logs + direct DB inspection until an HCA update reads them.** `workouts` aggregation into `DailyRecord` is deliberately **held at `#189`** — `aggregated.workouts` is honestly 0, not a drop. The **O3 client conformance check is deferred to session 2** behind `#236`'s source-neutral contract; session 2 also owes the HCA mapper de-duplication (`fetchSleepData`/`fetchHRVData` exist twice, only the `fetchAllData` inline copies on the sync path — the adapter extraction must de-duplicate, not move). No HCA file touched this session (one-repo rule; HCA cloned read-only for provenance only). Metadata persistence is `Q118`. **OWED status (owner Luke):** (1) number re-verify at the merge instant — **DONE at merge**: literals stood against master max `#233`/`Q117`, no advance (the dropped-then-restored `Q119` reconciliation landed as PR #97). (2) O3 client conformance check + HCA mapper de-dup — **still OWED, session 2 behind `#236`**. (3) post-deploy verification — **DISCHARGED 2026-08-24** (precedent: the `feat/cbti-eval-trigger-v2` prod-verification discharge): one real sync against the deployed stricter endpoint at `10:43:37Z` **PASSED** — eight in-window dates upserted, pre-window `synced_at` untouched (non-destructive upsert), no 422 and no `HC sync rejected` log, `unattributed == 0` across `exercise` 75 / `sleep` 129 / `steps` 78 / `heart_rate` 49311. Structural finding carried as a cross-ref, no new Q: `health_connect_syncs.hrv_rmssd` never populated and no `hrv` `record_type` ever reached `record_sources` — Samsung Health does not write HRV to Health Connect (`samsung_hrv_readings` scraper is the sole HRV source), the arbitration `#236`/`Q83` own; `Q119` stays OPEN. Full evidence in `#235` Status. |
| `feat/schedule-item-schema` | Give `schedule_item` a closed, validated shape at write and stop the read path losing facts silently. `routers/knowledge.py::validate_schedule_item` refuses (422) an unknown key, a non-weekday in `days`, a truthy-string boolean, `sessions_per_week` outside 1–14, a missing required field and an out-of-set `expected_load`/`time_of_day`; an unacknowledged day overlap is a structured 409 naming every clashing row. Applied inside `upsert_knowledge_entry` — the shared path for `POST /knowledge/entry`, chat and `routers/health.py` — not only on the route, because a rejection the chat writer never sees is the silent drop this replaces. `hard` (scheduling) and `expected_load` (cost) separated as two axes; `supersedes` triggers on DAY OVERLAP ALONE, `distinct_from` accepted at write and never stored. Prompt template rewritten to teach the closed shape and the overlap retry. **Both `context_builder` silent-drop sites closed** — the day-map loop's `else`-less `if d_lower in day_map:` and the bare `except (ValueError, IndexError): pass` — now reported into THIS WEEK FLAGS. `DECISIONS_LOG` **#233**, `SCHEMA.md` **024**, `OPEN_QUESTIONS` **Q116**/**Q117**, `Q112` appended. | **PARTIAL — steps 1/2/3/5/6 landed, step 4 and GATE 4 did NOT run.** Backend 1087 → **1113**, frontend **47**, zero regressions, both suites green. GATES 1–3 exercised: validator proven **by mutation, not by passing** (`FEEDBACK` §18) — eight mutations applied one at a time, every one caught, including the v1 activity-matching rule which fails 4 tests; every negative paired with a positive control (`FEEDBACK` §17) — a conforming write accepted and stored verbatim, a non-overlapping row accepted, cross-user isolation asserted, so the refusals cannot be a validator that refuses everything. Parity guard narrowed between anchors (not by dropping `_section_user_profile`), with a control asserting the excised spans still differ so the narrowing cannot become a no-op that hides drift. `#176`(c): **zero removed lines in any `.md`** — pure additions; the 13 code removals are each a line the change declares it rewrites. `#NEXT` resolved by count-verified scoped replace per file, asserting the pre-existing prose tokens (35 in `DECISIONS_LOG`, 7 in `OPEN_QUESTIONS`, 1 in `SCHEMA`) were untouched — the `#175`/`#220` corruption is what that guards. | **The 18 active rows were NOT backfilled and the 5 prod assertions were NOT performed — carried in `Q116`, which is this branch's loop-close.** No route to the database from this session: no Railway CLI, `Health_app_data` MCP unauthorized, and every credential path closed by `#111` (the `env` deny pattern refused a call, as designed). The GUARD stop-condition — confirming the live row set still matches the 2026-08-23 chat read — is therefore also unexecuted, and `Q116` states that it runs FIRST. Split visible in the landed artefact, not only in a report: `SCHEMA.md` 024 states the validator is live and the rows are not backfilled. Owner: Luke, or any session with database access. **Not resolved, per GUARD:** `Q112` (appended only — its cited `phases` evidence goes with row 9's correction, which has not run), `Q105`, `Q106`. Clone-depth observation carried to close-out, not minted: `test_context_builder_output_unchanged_pre_post_refactor` fails on a fresh SHALLOW clone (the pinned `3360ed5` is absent), so master reports red until `git fetch --unshallow`. |
| `governance/q115-sprint-load-routing` | Transcribe the chat-side draft question on load routing into `OPEN_QUESTIONS` as **Q115**: `#28`'s routing clause dual-routes strength volume-load (Mechanical + Neuromuscular) and nothing else, so supramaximal / repeated-sprint work is HR/zone-derived and lands in **Metabolic alone** — the neuromuscular cost is absent from the channel that should carry it, and what is recorded carries Metabolic's faster τ (`#32`: Metabolic ≈4 d vs Neuromuscular ≈6 d). Scoped as a **routing amendment** inside the existing taxonomy, explicitly not a fifth window (`#32` orders τ before the split) and explicitly not a weighting claim from the prompting proteomics (a stimulus-quality finding, not a cost finding; source supplied to chat, not retrieved). The open substance is the discriminator — mean HR cannot separate intermittent from continuous work, which is the same blindness `get_training_load()`'s `hr_avg` has today. | **DONE** — reviewed and ruled; status asserted at the merge instant, as `814707f` did for PR #85, because a row riding its own branch cannot name its landing SHA. Governance-only under `#176`(b): five commits, one file each (transcription with `Q#NEXT` · number-at-merge resolution · this row · cross-ref amendment · this flip). `#176`(c) audit: the OPEN_QUESTIONS transcription is 90 insertions / 0 deletions, and every removed line since falls inside a declared replacement region — the `Q#NEXT` heading token, the Polar bullet's cross-ref, and the Next-action tail. **Branch renamed** from the `claude/`-prefixed, hash-suffixed auto-name the session brief pinned, which `CLAUDE.md` bans for in-flight work; a brief does not outrank the operating contract, and accepting it would have recorded a banned name as landed practice. The rename could not carry the original PR — this session's GitHub toolset has no branch-rename call and a PR's `head` is not editable — so the original draft PR was closed with a pointer and the branch re-opened under its concern name. | **The `#10` cross-ref is corrected in Q115 and does NOT ride this row.** Q115 now cites `#17` (AccessLink v4) / `#46` (per-second exercise-HR) for Polar retrieval, and records in-entry that `#28`'s own Status line carries the same wrong pointer; `#28` is locked, so that correction rides the routing amendment Q115 already schedules rather than costing a decision entry. Residual, per `#123`: closed questions leave the scan surface, so if Q115 closes before the routing amendment lands the correction needs relocating at that moment — named in Q115's Next action. **Two open items for Luke, neither minted here.** (1) `OPEN_QUESTIONS.md:1123` cites `#112` for the below-the-fold rule; the rule is `#123` — `#112` is cross-repo propagation debt. Found while writing Q115's residual clause, flagged rather than silently corrected. (2) A session brief specified a branch name `CLAUDE.md` bans and nothing caught it until PR review — one instance, a gap in the brief-writing path rather than this branch; a `FEEDBACK` entry on brief preconditions only if it recurs. |
| `governance/feedback-30-amend` | Correct a FEEDBACK entry that instructed future sessions NOT to investigate a proven write-side data-loss path, and record the questions the last session left unwritten. `§30` was minted from a read-side symptom and closed with *never "fix" data that reads fine over HTTP elsewhere* — true for the read path, false for the write path, where the same missing `charset` makes PowerShell 5.1 downgrade the request body before it is sent (`POST /knowledge/entry` ids 75–78 persisted em-dashes as hyphens, loss AT REST). `§30` amended to name both directions; `§31` added (auditing a vocabulary's members does not audit its coverage). `DECISIONS_LOG` **#229** (FEEDBACK is amended IN PLACE, never superseded — the opposite convention to `DECISIONS_LOG`, deliberately, because FEEDBACK is a live instruction set read at session start); `FEEDBACK_ARCHIVE` **§30 essay**; `OPEN_QUESTIONS` **Q111** (injury coupling as a relationship the store cannot hold), **Q112** (standing intent vs current phase), **Q113** (`source` vocabulary — `api` channel vs `operator` authority), **Q114** (`CHECKS` has no `FEEDBACK.md` arm). | **DONE** — PR #86 (ruleset `master-pr-gated`, `placeholder guard (POSIX)` green), human review taken before the merge per `#176`. Governance-only batch under `#176`(b): six commits, one file each except the essay+counters commit and the resolution commit; branch deleted at merge. Status asserted ahead of the merge, as `814707f` did for PR #85 — a row that must ride its own branch cannot name its landing SHA. | No code, no schema, no migration, no tests — `git diff master --stat` touches only `DECISIONS_LOG`, `FEEDBACK`, `FEEDBACK_ARCHIVE`, `OPEN_QUESTIONS`, `BRANCHES`, `CLAUDE.md`. `#176`(c) audit: five removed lines, each inside a region the change declares it replaces — the `§30` rule line and four stale counters. **Two brief premises were verified FALSE before acting**: the archive was said to hold no essays for `§28`/`§29` (it holds both, so `§30` was the sole essay-less one-liner — the essay was written rather than skipped), and `FEEDBACK.md`'s header block carried two further stale indices (`§§7–28` and the literal word `22`) that no counter list had named. Encoding proved by explicit UTF-8 decode, not console read, since a console read reproduces the very defect documented; the `§30` tripwire is void for `FEEDBACK_ARCHIVE.md` (the essay deliberately contains one `U+00E2`) and was replaced by a codepoint inventory plus the real double-encode signature — zero sequences across all four stores. Numbers re-read from master at resolution: `#228`/`Q110`/`§30`, resolved `#229`/`Q111`–`Q114`/`§31` uncontested. | The `charset=utf-8` fix on the default response class is **NOT landed** and is not this branch's work — it ships behind a live `µ` round-trip, a prod verification per `§8`, not a repo write. Owner: Luke. `Q114` records that `§31` had no guard behind it: `CHECKS` covers `DECISIONS_LOG` and `OPEN_QUESTIONS` only, so a `§N` collision has nothing catching it. |
| `feat/assertion-provenance` | Record WHO asserted a profile entry, on what basis, and when. `hard_stops` and `live_signals` entries gain a provenance block as keys INSIDE the entry — `asserted_by` (`user`\|`engine`\|`clinician`), `asserted_on`, `basis`, `review_on` — so assertion and attribution stay atomic and no migration is needed. Writes require provenance; reads backfill its absence, so a legacy profile keeps working but the next edit must state who and when. `review_on` is a PROMPT, never an expiry. `DECISIONS_LOG` **#227** (provenance inside the entry; the write-strict/read-tolerant asymmetry; `asserted_by` is an authority axis distinct from the existing `source` channel axis, aligned with #222's `resolved_by`; `weekly_template` takes no block) and **#228** (`review_on` never expires — #223's rule restated in a second store); `OPEN_QUESTIONS` **Q110** (what surfaces a due review — bound to Q107) and **Q108 NARROWED IN PLACE** rather than duplicated: the bind-both-stores half is now answered, only the identity fork remains. | **DONE** — landed to master via PR #85 (ruleset `master-pr-gated`, `placeholder guard (POSIX)` green); human review taken before the merge per `#176`. Feature commit `8181d9e`; governance commit rides the same branch per `#176`(b); branch deleted at merge. | Backend **1069 passed** (1018 baseline +51), frontend untouched. THE regression gate runs the same hard stop in BOTH shapes through the real `is_contraindicated` path in one comparison — same verdict AND same reason string — and a second asserts `compute_probe_queue` -> `select_next` identical for two users differing only in the presence of provenance keys; running both shapes in one test is what makes it a comparison rather than an assumption that added keys are inert. Four GUARD gates prove nothing auto-expires: a 2020 `review_on` still blocks at both layers, past/future/null/absent are asserted IDENTICAL in one comparison, and a source-level gate asserts `engine/selection.py` contains no reference to `review_on` at all. **Gates mutation-tested** against three seeded defects, each caught by the intended gate — seeding an expiry check into `is_contraindicated` fails all four guards including the structural one; a backfill that clobbers real provenance fails the verbatim, mixed-profile and round-trip gates; skipping `live_signals` validation fails only that arm. Two `#221` tests updated: they wrote `hard_stops` without provenance, which the new write contract refuses. | None on this branch. Known consequence, pinned as a test not prose: GET -> PUT of a pre-provenance profile 422s because the read surfaces `asserted_on: null` — intended friction, and the message names the fix. Owner: Luke. |
| `feat/soreness-prefill-carry` | AM soreness prefill carries the last reported value instead of the hardcoded `1`. Since `1` is the scale FLOOR, every morning opened at "asymptomatic" regardless of yesterday — a sore injury had to be re-entered daily or the record understated it, and an unedited day silently reported recovery. `derive_soreness_items` now carries that injury key's last reported value within `_PREFILL_LOOKBACK_DAYS` (7, named constant), falling back to **3** — the module's existing no-information value from `calc_naive_baseline`'s `max(vals) if vals else 3` — so the prefill stops contradicting the scorer it feeds and `1` stops doing double duty as floor and default. No migration, no new field. `DECISIONS_LOG` **#224** (bounded carry), **#225** (the `3` fallback and its two recorded consequences), **#226** (a reported-vs-defaulted flag considered and DECLINED, with its residual stated); `OPEN_QUESTIONS` **Q109** (`review_when.metric` is decorative — verified in-tree, never read by `_review_message`); `FEEDBACK` **§30** (PowerShell 5.1 mojibake on charset-less JSON). | **DONE** — landed to master via PR #84 (ruleset `master-pr-gated`, `placeholder guard (POSIX)` green); human review taken before the merge per `#176`. Feature commit `cdffa1e`; governance commit rides the same branch per `#176`(b); branch deleted at merge. | Backend **1018 passed** (980 baseline +38), frontend untouched (verified, not assumed: `CheckInAM.jsx` seeds state from `/prefill` and submits it unchanged, and `SORENESS_LABELS[3-1]` renders "Moderate"). The window boundary is asserted at exactly 7 and exactly 8 days with a carried value of **2**, deliberately not the fallback 3 — a boundary test whose expected value equals the fallback cannot tell "carried" from "fell through". Prefill's read-only nature is asserted by a SQLAlchemy statement spy (no INSERT/UPDATE/DELETE) as well as by row-count and payload comparison, including for a user with no `daily_records` row. Query count asserted as a 1-injury vs 8-injury comparison, not a magic number. **Gates mutation-tested** against four seeded defects, each caught by the intended gate and no other — widening the window, discarding a key on the most recent RECORD rather than the most recent record containing it, removing the `bool` guard (`bool` is an `int` subclass, so `True` would read as a valid 1), and a per-key N+1 query, which fails ONLY the query-count gate since its behaviour is identical. One drafting correction: a hand-computed readiness literal of 6.5 was wrong (6.4); the term-by-term expression beside it was right and caught it. | None on this branch. Q109's specific instance (entry id 30) is recorded as a claim to CHECK, not a finding — it is a live prod row this session cannot read. Owner: Luke. |
| `feat/injury-resolution` | Close the WRITE end of the injury loop. An entry could be created and superseded-by-key but not RESOLVED, so `gather_active_injuries` kept returning healed injuries and `is_contraindicated` kept suppressing regions the user had recovered in, with no surface listing them. Adds `GET /knowledge/injuries` (`?include_resolved=true` for history) and `POST /knowledge/injuries/{entry_id}/resolve` — `active=False` plus a `resolution` block in the row's JSON `value`, `superseded_by` left null. No model, no migration. `DECISIONS_LOG` **#222** (resolution and supersession are distinct terminal states) and **#223** (resolution is an explicit operator write; `injury_trajectory` stays surfacing-only, restating #72); `OPEN_QUESTIONS` **Q107** (where a review flag should surface a resolve prompt) and **Q108** (whether `resolved_by:"clinician"` carries identity — to be answered once for this store and Brief B's). | **DONE** — landed to master via PR #83 (ruleset `master-pr-gated`, `placeholder guard (POSIX)` green); human review taken before the merge per `#176`. Feature commits `24e8bbc` (read surface) and `e7c2009` (resolution write); governance commit rides the same branch per `#176`(b); branch deleted at merge. | Backend **980 passed** (945 baseline +35), frontend untouched. The load-bearing gate names a SPECIFIC region — `single_leg_hop` right, suppressed by an active right-hamstring entry — and asserts it contraindicated before the resolve and selectable after with no other input changed; re-run one layer out through `compute_probe_queue`, and a third gate proves the lift is SCOPED by leaving a second live injury's block standing. GUARD gates prove nothing auto-resolves: a passed `resolve_by`, a soreness series at its exit condition, and a live `review` flag each leave `active` True and the region suppressed, plus a structural gate asserting `injury_trajectory`'s source contains no `.active =` assignment at all. Both trajectory flags pinned EXACTLY so a drift in `evaluate()` fails loudly. **Gates mutation-tested** against four seeded defects, each caught by the intended gate and no other — removing the `active` flip, mutating `value` in place rather than reassigning (the `JSON`-not-`MutableDict` silent-drop class), setting `superseded_by` on resolve, and dropping the user scope. One drafting correction recorded in #223: a first-draft expectation asserted a divergence the source correctly refuses (`resolving_by` diverges only while the last reading is >1) — the expectation was wrong, not the code. | None on this branch. The existing stale rows are NOT backfilled — that is a `ROADMAP` NEXT lane and is deliberately operator-driven per #223. Owner: Luke. |
| `feat/weekly-template-capacity-quota` | Add a sport-agnostic weekly dose allocator to the fortification profile: `fortification_profiles.weekly_template` (migration `b7c3e91f2a48`, nullable JSON) holding `{slots: [{capacity, sessions_per_week, minutes}]}` where `capacity` is a `engine.taxonomy.Capacity` member — never a sport, position, or exercise — plus a slot-constrained selection path (`select_next(..., capacity=)` and `GET /engine/next?capacity=`). Sport-specificity stays expressed through `vehicle_bias` / `horizon` / `primary_target`, which are already upsertable. `DECISIONS_LOG` **#221**, `OPEN_QUESTIONS` **Q105** (capacities stored verbatim, so consumers must route through `taxonomy.resolve_capacity`) and **Q106** (how a slot's `minutes` reaches the prescription — validated and stored, read by nothing). | **DONE** — landed to master via PR #82 (ruleset `master-pr-gated`, `placeholder guard (POSIX)` green); human review taken before the merge because the branch carries both code and a migration (`#176`). Feature commits `5048baa` (schema, serialisation, validation) and `14e5670` (slot-constrained selection + the gate suite), governance commit `f073462` rides the same branch per `#176`(b); branch deleted at merge. | Backend **945 passed** (890 baseline +55), frontend untouched. All six brief gates asserted at BOTH the engine and HTTP layers. The gate that matters — a hard-stopped region must not return through a matching capacity slot — is proven by before/after (the region a STABILITY slot DOES select, then stopped, then absent) rather than by reading filter order off the source, and reinforced by a subset property over every capacity: the slot filter narrows an already-stop-filtered queue, so it can only remove. Round-trip compared on the raw wire substring `"capacity":"STABILITY"`, not dict equality, which would pass a silent re-casing. The refusal battery carries four negative controls so a validator that refused everything could not report green. Migration verified by stamping the prior head, upgrading one step on a scratch SQLite, then downgrading. **Caught pre-commit:** the first pass chained the migration onto `e2d5c7a1b9f3` because the head-detection regex required the annotated `revision: str =` form while the true head `a7c3f19d5e28` uses bare `revision =` — `alembic heads` reported two heads and the chain was corrected before anything was committed (#113: read the matches, do not trust the pattern). | None — the consuming lane (weekly resolver / "what's due") is a `ROADMAP` NEXT row, not an outstanding item on this branch. Owner: Luke. |
| `feat/canonical-map-db-bind` | Move the canonical marker map from a startup-loaded `marker_canonical.json` dict to the `marker_canonical_entries` table (seeded from that JSON, 70 rows, migration `a7c3f19d5e28` self-checks the inserted count against the file), and add `POST /labs/canonical/bind` — a guarded runtime bind offered inline at the CONFIRM stage that writes a canonical entry and backfills that marker's historical unmapped `LabResult` rows. Implements `#50`'s confirmation-populated half and supersedes its stale "Not implemented" status. `DECISIONS_LOG` **#220**, `OPEN_QUESTIONS` **Q104** (the two interpretation readers left on the JSON seed). | **DONE** — landed to master via PR (ruleset `master-pr-gated`); feature commits `93cd187` (backend + schema) and `2c4b0fa` (frontend), governance commit rides the same branch per `#176`(b); branch deleted at merge. | Backend **890 passed** (882 baseline +8), frontend **47 passed** (41 baseline +6, `BindControl.test.jsx` drives the guard refusals through the DOM). Over-collapse proven at BOTH new runtime points — bind-time (canonical already established at a disagreeing unit) and backfill-time (a historical row carrying a disagreeing unit) — with the refused bind promoting **zero** rows, asserted, since a half-migrated series is harder to see and harder to undo than a refusal. `conftest` seeds the fixture from the same JSON the migration uses; the two tests importing `_CANONICAL_MAP` re-point at the JSON, which survives as the seed. **Step-1 finding corrected the brief:** its phasing hypothesis (ungrouped markers invisible to interpretation) is FALSE — `producer._ungrouped()` emits them and `presentation.py:237` builds rephrase fragments from them, so a bound-ungrouped marker DOES reach `rephrase`. Phasing held on a different mechanism: `_KNOWN_ENTITIES` is a *detector* allowlist (staleness = missed detection, never a false rejection) and `gates._UNIT_ESTABLISHED` is gated behind `safety_thresholds.json` authorship with a `value_plausibility` fallback. Migration cost decided it — `rephrase` has a `db` at its call site, `_resolve_band` would need a session threaded through ~6 signatures of the pure `#86` producer. | — |
| `feat/cbti-eval-trigger-v2` | `#118`'s PM evaluation trigger, **reworked onto the 4-night hunting engine** — offer (`GET /checkin-v2/cbti/evaluation`) + witnessed accept (`POST .../accept`) surfaced in `NightlyCloseOut.jsx`, both through `cbti.replay.evaluate_live_cycle` (#128 reuse). Cut fresh from master (`629e2c7`); supersedes the obsolete `feat/cbti-eval-trigger @ fec0324` (reference-only, never merged). Mints **#213** (the rework decision). | **DONE — #213, merged via PR #66 → master.** Branch commits: backend `853bed1`, frontend `ee9b2eb`, gov `chore: session close-out` + the `#NEXT → #213` resolution. Code + tests took full human review (no guard-only land, #176(c)). The whole close arm (`acceptable` flag, accept 409, dead-control UI) is DELETED as unreachable — the engine emits a `converged HOLD`, never `close`. Master features the obsolete branch would have dropped are preserved (`centre_minutes`/`dither_minutes` on `CBTIContextOut`, `DeepSleepLevers` + centre estimate in `NightlyCloseOut.jsx`). | Backend suite **838 = 828 + 10** (pre/post measured on fresh master via `.venv`, not derived); frontend vitest **+12** (32 total). Gates G1–G5 verified: G1 no KeyError on master's series dict; G2 offer/GATE-1-nap-guard parity pinned in a test; G3 close-reasoning grep clean in trigger+endpoint+UI; G4 eligibility gates on `days >= CYCLE_NIGHTS`; G5 placeholders resolved at merge (**#NEXT → #213**, master max re-read #212 at that instant), baselines reported. **Verified in prod 2026-08-13** (post-merge): offer + accept exercised against live block-2 data, accept minted rx 12 (re-confirmed by read-only Railway query — `hold`/n=2/390/22:30, rx 11 `superseded_by`=12, block open); OWED verification discharged. Two follow-ups filed — Q101 (elapsed-vs-sufficient selection) and #214 + a ROADMAP NEXT fix-row (accept-confirm defect). | **Nothing outstanding.** `#NEXT` resolved to **#213** at the merge instant (master max re-read #212, no advance). Merged `--merge --delete-branch`; obsolete `feat/cbti-eval-trigger @ fec0324` deleted (local + remote). |
| `chore/oq-q100-cooper-hevy-empty-account` | Session close-out for the user-5 (Cooper) zero-customs investigation — the #211 follow-up chip. Adds `OPEN_QUESTIONS` **Q100** (Cooper's stored Hevy key authenticates to an account holding 0 customs AND 0 workouts; H1 user-id-mapping and the copied-key form of H2 ruled out by read-only prod probe; stale-belief vs wrong-account indistinguishable from our side — no Hevy whoami, 0 workouts = no in-band identity signal), strikes the ROADMAP NEXT "investigate" row to **DONE → Q100**, overwrites `closeout.md`, prepends this session to `CLAUDE.md` Recent-landings (3-cap, dropping the ROADMAP-hygiene line), and rows this branch. No `DECISIONS_LOG` change — no decision minted (an `OPEN_QUESTIONS` finding, not a `;cc` queue). | **DONE** — written at land per the self-row convention #176(b); merged via PR, branch deleted. Governance/docs-only (`OPEN_QUESTIONS.md` Q100, `ROADMAP.md` row strike, `closeout.md`, `CLAUDE.md` Recent-landings, this row); guard-gated per #176(c) — Q100 is a pure append, the ROADMAP strike and the Recent-landings swap are confined declared-region replacements, `closeout.md` a wholesale ritual overwrite; no removed line outside a declared region; placeholder guard green (exit 0). | **Nothing outstanding on the branch.** Branch gate at close: only this branch was touched — the `claude/sleepy-hofstadter-8b5c6a` worktree auto-name carried no unique commits (ancestor of master) and was abandoned per the auto-name ban; `feat/cbti-eval-trigger` pre-existing, untouched, pushed, rowed OWED below; `claude/vibrant-khorana-86acde` + `master` are other-worktree checkouts, untouched. **Q100 resolution needs Cooper** (confirm the key's account; re-issue if wrong). The handoff names this a multi-user-FINDING session (read-only investigation, no product code moved) and the product lanes that stood still — interpretation increment 2 (rephrase), increment 3, `feat/cbti-eval-trigger` rework, Banister, the #116/#121 deploy probe. Number-at-merge: master max **#211 / Q99** re-read at the merge instant; **Q100** stood, no decision to resolve. |
| `chore/session-closeout-0812b` | Session close-out for the 2026-08-12 dispatch brief (C4 governance-view regen · §19 ledger row · interpretation training-wheels verify): overwrites `closeout.md` (real commits — none mid-session, the §19 row rides this commit; pending-brief reconciliation; cold-resume handoff), lands the `FEEDBACK_ARCHIVE.md` §19.6 ledger row id-20, prepends this session to `CLAUDE.md` Recent-landings (3-cap, dropping #207), and rows this branch. No `DECISIONS_LOG` / `OPEN_QUESTIONS` content changed; no decision minted (dispatch brief, not a `;cc` queue). | **DONE** — written at land per the self-row convention #176(b); merged via PR, branch deleted. Governance/docs-only (`FEEDBACK_ARCHIVE.md` §19 row, `closeout.md`, `CLAUDE.md` Recent-landings, this row); guard-gated per #176(c) — the §19 row and this self-row are pure additions, `closeout.md` is a wholesale ritual overwrite and Recent-landings a capped trim, both declared replacement regions, no removed line outside one. | **Nothing outstanding on the branch.** Branch gate at close: no feature branch touched this session; `feat/cbti-eval-trigger` pre-existing, untouched, pushed, rowed OWED below. The handoff names this the **third recent INSTRUMENT/governance** session and the product lanes that stood still — interpretation increment 2 (rephrase, on-ramped by training-wheels review #1 now verified in-DB), increment 3, lab small-lanes, `feat/cbti-eval-trigger` rework, Banister, the #116/#121 deploy probe. Clearest next action: **Q45** (dated NOW head), then interpretation increment 2. C4 output (`build/CONSOLIDATED_GOVERNANCE_VIEW.md`) uncommitted by design — Luke places it via the claude.ai UI. |
| `chore/session-closeout-0812` | Session close-out for 2026-08-12 (ROADMAP-hygiene brief): overwrites `closeout.md` (real commits, no `;cc` PENDING queue carried in, cold-resume handoff), prepends the ROADMAP-hygiene landing to `CLAUDE.md` Recent-landings (trimmed to the 3-cap, dropping #194), and rows this branch. No `DECISIONS_LOG` / `OPEN_QUESTIONS` content changed — the ROADMAP strike + adaptive-programming lane landed by PR #59 (`chore/roadmap-adaptive-lane`, merged + deleted); no decision minted by `;cc` adjudication (closeout-class re-triage, provenance row-carried). | **DONE** — written at land per the self-row convention #176(b); merged via PR, branch deleted. Governance/docs-only (`closeout.md`, `CLAUDE.md` Recent-landings, this row); guard-gated per #176(c) — Recent-landings is a pure insert + capped trim, no removed line outside a declared region. | **Nothing outstanding on the branch.** Branch gate passed at close: `chore/roadmap-adaptive-lane` merged + remote-deleted (PR #59); `feat/cbti-eval-trigger` pre-existing, untouched, pushed, rowed OWED below. The handoff names this an **INSTRUMENT/governance** session (ROADMAP re-triage only) and the product lanes that stood still — interpretation increment 2 (rephrase, operator-confirmed needed by #194), increments 3 / go-live small-lanes, `feat/cbti-eval-trigger` rework, Banister. Clearest next action: **Q45** (dated NOW head), then interpretation increment 2. Batched nit owed to next ROADMAP touch: "live end-to-end" → "wired and live-fired; round-trip proof owed (Q77)". |
| `chore/session-closeout-0811b` | Session close-out for the reader-unification thread (`#207`): pins PR #56 / merge `e4c7dba` into #207's Status, prepends #207 to `CLAUDE.md` Recent-landings (3-cap, dropping #189), adds the ROADMAP status-tooling NEXT row + date bump, overwrites `closeout.md`, and rows this branch. Feature + governance content landed by PR #56 (`feat/status-reader-channels`, merged + deleted). | **DONE** — self-row written at land per #176(b); merged via PR, branch deleted. Governance/docs-only (`DECISIONS_LOG` #207 Status pin, `CLAUDE.md` Recent-landings, `ROADMAP.md` insert + date, `closeout.md`, this row); guard-gated per #176(c) — the #207 Status pin replaces its own declared placeholder region, Recent-landings is insert + capped trim, ROADMAP a pure insert; no removed line outside a declared region. | **Nothing outstanding on the branch.** Branch gate at close: `feat/status-reader-channels` merged + remote-deleted (PR #56, #207); `feat/interp-inc2-rephrase` (increment-2 rephrase, #202) pre-existing, untouched, pushed, unrowed — the queued next PRODUCT lane, named in the handoff; `feat/cbti-eval-trigger` pre-existing, untouched, pushed, rowed OWED below. |
| `chore/session-closeout-0811` | Session close-out for 2026-08-11 (interpretation go-live, increment 5): overwrites `closeout.md` (real commits, no `;cc` queue carried in, cold-resume handoff), prepends `DECISIONS_LOG` **#194** to `CLAUDE.md` Recent-landings (trimmed to the 3-cap, dropping #188), adds the ROADMAP two-marker canonical small-lane, and rows this branch. Asset/feature content (#194 promotion, Q92) landed by PR #52. | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. Governance/docs-only (`closeout.md`, `CLAUDE.md` Recent-landings, `ROADMAP.md` small-lane insert, this row); guard-gated per #176(c) — Recent-landings is a pure insert + capped trim and the ROADMAP row a pure insert, no removed line outside a declared region. | **Nothing outstanding on the branch.** Branch gate passed at close: `feat/interpretation-go-live` merged + remote-deleted (PR #52, #194); `feat/cbti-eval-trigger` pre-existing, untouched, pushed, rowed OWED below. The handoff names this a **PRODUCT** session (first live run of the interpretation surface against real data); clearest next action is **increment 2 (rephrase)**, now operator-confirmed by Luke's vernacular feedback. |
| `chore/session-closeout-0810` | Session close-out for 2026-08-10 (status-parser-gate brief): regenerates `closeout.md` (real commits, no `;cc` queue carried in, cold-resume handoff), prepends `DECISIONS_LOG` **#190–#193** to `CLAUDE.md` Recent-landings (trimmed to the 3-cap, dropping #187), and rows this branch. No store content changed here — #190–#193 / Q90–Q91 landed by PR #50. | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. Governance/docs-only (`closeout.md`, `CLAUDE.md` Recent-landings, this row); guard-gated per #176(c) — Recent-landings is a pure insert + capped trim, no removed line outside a declared region. | **Nothing outstanding on the branch.** The handoff names this an **INSTRUMENT/governance** session (status-reporting tooling) and the product lanes that stood still: HC exercise ingestion (#189 held), `feat/cbti-eval-trigger` (OWED), #116/#121 deploy probe (OWED). Scheduling brief (S1/S2) is next and must resolve **S4** (a HALT must reach Luke off the artifact surface) before the model is scheduled. Cross-repo: **Q90** is an HCA-side store edit, owner Luke. |
| `status-parser-gate` | **Status reporting: parser gate + snapshot baseline (Steps 1–2 of the status-reporting plan).** Repairs the live `**State:**` extraction bug in `gen_governance_view` (89 health-app questions rendered blank); centralises both repos' store grammar to `scripts/gov_dialects.py`; adds a new gated machine status model (`scripts/gen_status_model.py`) with count-parity + sequence + non-empty-extraction gates and an off-vocab/drift tally; seeds the first cross-repo snapshot (`Projects/_status/`, outside both repos, baseline-flagged + provenance-stamped). Mints **#190–#193**; opens **Q90** (HCA question-vocab drift), **Q91** (brief-authoring verify-checkpoint). | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. Code + governance — full human review via PR, not the guard-gated governance-only path. | Number-at-merge re-read at the merge instant — master max **#189 / Q89**, unchanged since open — so **#190–#193 / Q90–Q91** stood; guard green. Step 1 hard gate: the `### State vocabulary` block is byte-identical across repos (SHA `27337630fca6db03`), selecting accept-and-flag-as-drift. Step 2 gate proven on the real positive (count parity `89==89` passes, extraction gate HALTs all-89-empty, exit 1). Snapshot #1 seeded `2026-08-10T115614Z`, `baseline:true`. Diff engine, ageing backfill, dashboard, scheduling are out of scope (own briefs). | **Two items this brief did not resolve, carried forward: (S1/S2)** scheduling option + cadence; **(S4)** how a HALT reaches Luke when the artifact is the only output surface — a blocker on the scheduling brief, not this one. The gate reports to stderr + non-zero exit: sufficient for manual runs, explicitly NOT sufficient once scheduled (a silent scheduled failure is indistinguishable from a quiet week). Cross-repo: **Q90** is an HCA-side store edit, owner Luke. |
| `chore/session-closeout-0809k` | 2026-08-09 close-out (Brief K, HC identity-cutover findings): regenerates `closeout.md` (real commits, no `;cc` queue carried in, cold-resume handoff), prepends `DECISIONS_LOG` **#188** to `CLAUDE.md` Recent-landings (trimmed to the 3-cap, dropping #185), and rows this branch. No store content changed here — the findings landed by PR #45 (`#188`, `Q83` premise, `ROADMAP` F1 gate, `models.py`/migration docstrings). | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. | Branch gate passed at close: `gov/hc-identity-cutover` merged + remote-deleted (PR #45); `feat/cbti-eval-trigger` pre-existing, untouched, pushed, rowed OWED below. Governance/docs-only (`closeout.md`, `CLAUDE.md` Recent-landings, this row); guard-gated per #176(c) — Recent-landings is a pure insert + capped trim, no removed line outside a declared region. | **Nothing outstanding on the branch.** The handoff names this a **PRODUCT-adjacent** session (real HC data defect) that continues off the instrument-over-product streak #187 broke; `#188` records the ~10,881-row fork without touching it (dry-run-gated remediation deferred). Clearest next action: the #116/#121 backend deploy probe, still OWED. |
| `chore/session-closeout-0809b` | Second 2026-08-09 close-out (Briefs A + B session): regenerates `closeout.md` (real commits, no `;cc` queue carried in, cold-resume handoff), appends `DECISIONS_LOG` **#187**, prepends it to `CLAUDE.md` Recent-landings (trimmed to the 3-cap), and rows this branch. | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. | Branch gate passed at close: `feat/labs-shell-dedupe-and-mcp-reads` merged + remote-deleted (PR #43); `feat/cbti-eval-trigger` pre-existing, untouched, rowed OWED below. Governance/docs-only (`closeout.md`, `DECISIONS_LOG` #187, `CLAUDE.md` Recent-landings, this row); guard-gated per #176(c) — #187 is a pure append, Recent-landings a pure insert + capped trim, no removed line outside a declared region. | **Nothing outstanding on the branch.** The handoff names this a **PRODUCT session** that breaks the four-session instrument-over-product streak: Briefs A + B landed (#187) = two of #186's three moratorium items; one more from {interpretation 4b, Polar→chat} lifts it. Backend deploy probe (#116) OWED — `railway deployment list --service health-app-backend` SUCCESS + exercise `latest_only`. |
| `chore/session-closeout-0809` | Session close-out for 2026-08-09 (governance-prune brief + chat’s §§1–6 extension): regenerates `closeout.md` (real commits, pending-queue reconciliation, cold-resume handoff) and rows this branch. No store content changed here — the prune landed by PR #41 (`#186`) and the HCA return trip by PR #25 (`#34`). | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. | Branch gate passed at close: `chore/governance-prune` merged + remote-deleted (PR #41); HCA `gov/shared-block-prune` merged + deleted (PR #25). Governance/docs-only (`closeout.md` + this row); guard-gated. | **Nothing outstanding on the branch.** The handoff names the **fourth consecutive** instrument-over-product session and the moratorium-gated product lanes (interpretation increments 2/3/5, `feat/cbti-eval-trigger`, #116/#121 deploy probe, Polar→chat, lab-confirm Briefs A/B). Owed to chat: §6→`Clinical_Protocol`, §5→`Athlete_Profile` (project knowledge). |
| `chore/governance-prune` | **Governance prune (this brief): shift the contract from auditing to shipping.** Compresses `CLAUDE.md`’s shared block to invariants-only (97 lines; whole file 453→249). Rebuilds `FEEDBACK.md` 1328→73 lines: §§7–28 → one-line index under a new §7; §§1–3 (behavioural corrections) → archive verbatim; §4 → §1 Project principles; six items → §2 Design principles; §3.5 → `CLAUDE.md` Tooling; §5 injury snapshot → archive tombstone (truth = Postgres / `Athlete_Profile`); §6 CPAP removed (folds into project-knowledge `Clinical_Protocol`). Adds two shared standing rules — severity gate on review, governance batching — and a moratorium on new governance until three product items land. Mints **#186**. | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. Governance/docs-only (`CLAUDE.md`, `FEEDBACK.md`, new `FEEDBACK_ARCHIVE.md`, `DECISIONS_LOG`, `BRANCHES`); no code, no migration. | **Diff-shape gate (#176c): HUMAN REVIEW required, not guard-only.** The shared block and `FEEDBACK.md` are wholesale replacements, not confined-region edits, so the guard’s placeholder anchor cannot certify them. Every stripped line is relocated verbatim to `FEEDBACK_ARCHIVE.md`, proven byte-identical to the 4bd99cc original (§§1–3 = 21,689 B, §§7–28 = 75,166 B; full pre-prune `CLAUDE.md` embedded). §6 intentionally not archived (clinical data → project knowledge per the kill-rule). | **Nothing outstanding on the branch.** Read load (CLAUDE.md + FEEDBACK.md, both read at open) 21,325→3,316 words. §5 injury truth remains the Postgres declared-state ledger / `Athlete_Profile`; §6 CPAP folds into `Clinical_Protocol` — both chat-maintained in project knowledge post-land. |
| `chore/session-closeout-0808b` | Session close-out for 2026-08-08 (Brief J): regenerates `closeout.md` (real commits, pending-queue reconciliation, cold-resume handoff) and rows this branch above. No store content changed — `DECISIONS_LOG` #185, `OPEN_QUESTIONS` Q87, the `CLAUDE.md` strike + Recent-landings, and `.gitattributes` all landed by PR #39. | **DONE** — written at land per the self-row convention. | Branch gate passed at close: `gov/cross-repo-sweep` merged + remote-deleted (PR #39); `feat/cbti-eval-trigger` pre-existing, untouched, rowed OWED below. Governance/docs-only; guard-gated. | **Nothing outstanding on the branch.** The cold-resume handoff names the **third consecutive** instrument-over-product session and the still-open product lanes (interpretation increments 2/3/5, `feat/cbti-eval-trigger` rework, CBT-I surface, Banister). Held finding: `backend/models.py:224` stale wire-contract claim (`Q83`/`#175`) — background task, owner Luke. |
| `gov/cross-repo-sweep` | **Repo-wide application of #184's no-cross-repo-claims rule + the `.gitattributes` `-text` foreclosure (Brief J).** Strikes the third instance of the enforcement-state claim #184 targeted: `CLAUDE.md`'s merge-path section justified being repo-specific with a present-tense "HCA has no ruleset / no branch protection / no `.github/workflows` at all" sentence — all three clauses false (ruleset `20573455` active, `.github/workflows/governance-guard.yml` present, verified `gh api`); replaced with the structural reason (enforcement config is per-repo, outside the tree). Runs #184's test over every tracked `*.md`/`*.py` (263 lines / 17 files), classifying each cross-repo reference; only the live-state-claim class is struck. Adds `.gitattributes` (`*.md text`). Mints **#185**; feeds **Q87** (stays OPEN). | **DONE** — written at land per the self-row convention; merged via PR, branch deleted. Governance/docs + a new dotfile; no code, no migration. Guard-gated per #176(c): the one `CLAUDE.md` replacement is a declared region (6 ins / 3 del), no removed line outside it, no whole-file EOL churn. | Number-at-merge re-read at the merge instant — master max **#184**, unchanged since open — so **#185** stood; guard green. Sweep result: **1** live claim struck (`CLAUDE.md:293`), **1** wire-contract claim held for separate human review (`models.py:224`, `Q83`/`#175`), the rest append-only history / structural grammar / cross-repo task-pointers. `.gitattributes` is preventive — `ls-files --eol BRANCHES.md` reads `i/lf w/crlf` after `dc023a1`'s heal; `--renormalize` staged no `.md` content. | **Nothing outstanding on the branch.** Flagged for separate review (not this batch): `backend/models.py:224` still carries the stale "current HCA builds send no dataOrigin" wire-contract claim its sibling docstring already corrected (`#175`/`Q83`) — a code-file edit. Owner: Luke. |
| `chore/session-closeout-0808` | Session close-out for 2026-08-08: regenerates `closeout.md` (real commits, pending-queue reconciliation, cold-resume handoff), prepends #183/#184 to `CLAUDE.md` Recent-landings (trimmed to the 3-cap), and rows this branch + `gov/checker-exit-contract` above. | **DONE** — written at land per the self-row convention. Branch gate passed at close: `gov/checker-exit-contract` merged + remote-deleted (PR #36); `feat/cbti-eval-trigger` pre-existing, untouched, rowed OWED below. Governance/docs-only, guard-gated per #176(c) — pure insertions plus a capped-block trim, no removed line outside a declared region. | **Nothing outstanding on the branch.** The live thread is cross-repo: **Brief E** — HCA mirrors the `read()` fix to md5 `154e1871fab988fda9ce72170db4071f` (33 lines LF), after which the docstring-stripped executable bodies differ by exactly one hunk (main()'s advisory string); not runnable from health-app. Within health-app the next feature pick is interpretation increment 2 (rephrase) or the `feat/cbti-eval-trigger` rework. |
| `gov/checker-exit-contract` | **Checker exit-contract repair + docstring de-propagation (Brief D).** `check_governance_placeholders.py` `read()` returned git's stdout after checking only the return code: under text-mode decode a non-UTF-8 byte died in a subprocess reader thread (returncode 0, non-string → `finditer` TypeError, a traceback exiting 1 where the contract reserves 2), and an empty blob passed silently at exit 0. Now captures bytes, decodes explicitly, routes every non-run to exit 2. Same PR strikes two docstring sentences asserting another repo's enforcement state. Mints **#183**/**#184**; opens **Q87** (cross-repo-parity artefact register). | **DONE** — merged to master via PR #36 at `b4cf1ac`; local + remote branch deleted. Code + governance — full human review via PR, not the guard-gated governance-only path. | **Nothing outstanding on the branch.** Both defects reproduced on scratch refs before the fix (#170); four controls after — clean 0 / placeholder 1 / non-UTF-8 2 / empty blob 2, exit codes asserted, scratch torn down (`git ls-remote` clean). Number-at-merge re-read at the merge instant — master max **182 / Q86**, unchanged since open — so **#183**/**#184**/**Q87** stood; guard green on PR #36 (4s). **Handoff for Brief E:** docstring-stripped executable body **97 lines / md5 `ca648f466a30f6b7a6704e83a2bce490`** (LF); mirror `read()` to **33 lines / md5 `154e1871fab988fda9ce72170db4071f`**, then expect exactly one hunk (main()'s advisory string). |
| `chore/session-closeout-0807` | Session close-out for 2026-08-07: regenerates `closeout.md` (real commits, pending-queue reconciliation, cold-resume handoff), prepends #182 to `CLAUDE.md` Recent-landings (trimmed to the 3-cap), and rows this branch + `gov/writer-claim-correction` above. | **DONE** — written at land per the self-row convention. Branch gate passed at close: `gov/writer-claim-correction` merged + remote-deleted; `feat/cbti-eval-trigger` pre-existing, untouched, rowed OWED below. Governance/docs-only, guard-gated per #176(c) — pure insertions plus a capped-block trim, no removed line outside a declared region. | **Nothing outstanding on the branch.** The live thread is cross-repo: **Brief B** (HCA re-mirror against md5 `552728ade81e90edcbc8f12bbbc02a80`) then **Brief C**, both HCA-rooted, not runnable from health-app. Within health-app the next feature pick is interpretation increment 2 (rephrase) or the `feat/cbti-eval-trigger` rework. |
| `gov/writer-claim-correction` | **Shared-block writer-claim correction (Brief A).** Strikes the `@claude` GitHub Action clause from the shared block's writer line — it named a per-repo surface, not an invariant, and no `@claude` Action exists on any ref of health-app (`.github` holds only `governance-guard.yml`, a `contents: read` CI guard that cannot write). Line now reads "Code is the only writer"; any Action wiring is stated below `END SHARED LOOP RULES` in the repo that has it. Also appends `FEEDBACK` §14 occurrence 5 (77 vs 80). Mints **#182**. | **DONE** — merged to master via PR #34 at `236bc47`; local + remote branch deleted. Governance/docs-only, guard-gated per #176(c): the one removed line (CLAUDE.md:55) is inside the declared replacement region. | **Nothing outstanding on the branch.** Number-at-merge re-read at the merge instant — master max **#181**, unchanged since open — so **#182** stood; placeholder guard green on PR #34 (8s). **Handoff for Brief B:** the shared block on master is now **259 / 18717 / md5 `552728ade81e90edcbc8f12bbbc02a80`** (git LF blob, lines 20–278, trailing newline excluded) — Brief B mirrors against that hash. The #176(b) self-row was missed on its own branch; rowed here by the close-out instead. |
| `feat/mcp-get-lab-results` | **New MCP tool `get_lab_results(marker, limit)`** — a thin text formatter over the existing REST read-back `routers.labs.get_lab_results` (#59/#47), so the interpretation withhold is inherited at the projection, not re-implemented. Filter + limit applied over the returned Pydantic snapshots (no second query); `_marker_matches` mirrors `find_marker`'s rule in the FILTER direction. Mints `#181`. | **DONE** — landed on master via PR #32. Backend **765 passed** vs a 755 baseline (+10, `test_mcp_lab_results`); frontend untouched. Withhold guarded at string level (test c). Control per `#103`: net-new, cannot import against master. Guard green, `mergeStateStatus: CLEAN`. | **Nothing outstanding on the branch.** Number-at-merge re-read at the merge instant: master a466b7e, max 180 / Q86, cbti unlanded, so #181 stood. Concern-split honoured: code+test in one commit, governance in another, no unrelated changes. |
| `feat/canonical-urine-acr` | **Canonical-map data addition (no code path changed).** Adds the three urine-ACR markers stored raw from the SNP upload — `R U-Creatinine`->`creatinine_urine` (mmol/L), `R U-Albumin`->`albumin_urine` (mg/L), `R U-Albumin/Creat`->`albumin_creatinine_ratio_urine` (mg/mmol). Sets the analyte-first specimen-suffix key convention (first specimen-typed markers). Mints `#180`, `FEEDBACK` §28; hand-edited per `#98`. | **DONE (fully — branch + prod).** The map landed on master via PR #31; the operator ran the `#55`-sibling backfill against prod on 2026-08-07 and confirmed it with a query. Backend **755 passed** vs a 749 baseline (+6). Control per `#103`: all six new tests fail against master's map. §6 guard exercised both ways (mmol/L maps; umol/L refused) — and now confirmed on live rows. | **Nothing outstanding — both prod OWEDs closed with evidence 2026-08-07.** The Railway CLI could not reach the DB from the operator laptop (the injected `DATABASE_URL` uses the private `*.railway.internal` host, which resolves only inside Railway), so the backfill was run in the Railway dashboard SQL. Confirming query: all three urine rows now carry their canonical keys (`R U-Creatinine`->`creatinine_urine`, `R U-Albumin`->`albumin_urine`, `R U-Albumin/Creat`->`albumin_creatinine_ratio_urine`), none NULL. **Unit precision-check CLOSED:** stored `unit_canonical` byte-matches the map (`mmol/L` / `mg/L` / `mg/mmol`), so no future upload trips the §6 guard; serum `Creatinine`/umol/L and `Albumin`/g/L are untouched, confirming the specimen split holds on live data (`FEEDBACK` §28). Note: `#180`'s locked entry still reads OWED — that was true at authoring; this row carries current state per the DECISIONS-records-decision / BRANCHES-records-work-state split. |
| `fix/labs-fieldconfidence-optional` | **Move 3 — closes the `#177`/`#178` null-on-sparse-row family at its last row-level instance.** `FieldConfidence` sub-fields -> `float | None`; four arms ship together (contract, `min()`-drops-None derivation, frontend `isSuspect`/`confidencePct` null-guards, prompt). LOOSEN not coerce — a confidence has no safe default, unlike `#178`'s bools. Row-classification helpers extracted to `labRowClassification.js` (react-refresh: a component file exports only components). Mints the decision + `FEEDBACK` §27; opens **Q86** (the report-level scalars, left fail-closed by design). `field_confidence` shipped PRE-CAPTURE — proven pattern + real trigger row, not a captured 422. | **DONE** — written at land per the self-row convention, via PR #30. Backend **749 passed** vs a 737 baseline (+12: contract, derivation, route-level); frontend **20** vs 10 (+10). Positive control per `#103`: against master the contract/derivation tests reproduce the `float_type` 422 and the `min()` hazard, and 5/10 frontend cases fail (incl. `confidencePct` 74% vs 98%), while every genuine-signal case passes both sides. Lint unchanged at 5 pre-existing. | **Nothing outstanding on the branch.** Number-at-merge re-read at the merge instant — master still `3041839`, max 178 / Q85, `feat/cbti-eval-trigger` unlanded — so `#179` stood without re-resolution; guard green, `mergeStateStatus: CLEAN`. **Owed elsewhere, not here:** the backend Railway service must redeploy — this is a backend contract change, so `#121`'s served-bundle probe does not cover it; use `railway deployment list` for SUCCESS (`#116`). The urine-ACR canonical mapping is a separate track, not owed here. |
| `fix/labs-null-exclusivity-bool` | **Move 2 — the CURE for the fault `#177`'s instrument made legible.** A ref-less row nulls a non-Optional exclusivity bool, so Pydantic refused the body before `confirm_lab_report` ran. `ResultItem.ref_low_exclusive`/`ref_high_exclusive` gain a `mode="before"` validator coercing `null → False`; type stays strictly `bool` (matches the non-nullable column — no migration). Extraction prompt gains the absent-ref exclusivity case. Mints the decision + `FEEDBACK` §26; resolves **Q85**. `field_confidence` deliberately untouched. | **DONE** — written at land per the self-row convention, via PR #29. Backend **737 passed** vs a 722 baseline (+15: 10 contract, 5 end-to-end); frontend 10 unchanged (no frontend file touched). Positive control per `#103`: against master's `labs.py` the e2e suite reproduces the captured 422 byte-for-byte (`loc: [body, results, 0, ref_high_exclusive]`, `Input should be a valid boolean`) and 4/10 contract tests fail, while the 6 anti-regression cases pass both sides. | **Nothing outstanding on the branch.** Number-at-merge re-read at the merge instant — master still `5421af2`, max 177 / Q85, `feat/cbti-eval-trigger` unlanded — so `#178` stood without re-resolution; guard green, `mergeStateStatus: CLEAN`. **Owed elsewhere, not here:** the backend Railway service must redeploy before the live re-upload proves the cure — this fix is backend, so the `#121` served-bundle check does not cover it; use `railway deployment list` for SUCCESS per `#116`. **Known residual, not a blocker:** `FieldConfidence`'s four floats are still non-Optional — the last members of the null-on-sparse-row class, left untouched because they validated on this report. |
| `fix/labs-confirm-422-detail` | **Move 1 of the lab-ingest confirm fault.** Both `Metrics.jsx` catch blocks (`/labs/extract`, `/labs/confirm`) render list-form FastAPI 422 `detail` via a new shared pure helper `frontend/src/lib/apiError.js`; the constant fallback is reserved for a genuinely absent `detail` (transport). Adds the repo’s first frontend test runner (`vitest`, node env, no jsdom) + 10 cases. Mints **#177**, `FEEDBACK` §25, and the held **Q85**. | **DONE** — written at land per the self-row convention, via PR #28. Backend suite 722 passed, identical to baseline (no backend file touched); frontend 10 new tests pass; lint unchanged at 5 pre-existing errors, none in a touched file. Positive control per `#103`: the pre-fix expression returns exactly `"Failed to save report"` on the G1 payload. **Merge was blocked for one cycle by an external cause worth recording:** GitHub Actions sat in `major_outage` on 2026-08-07 with webhooks throttled to ~15%, so no `governance-guard` run was ever created for the head SHA (zero check-runs) and `master-pr-gated` read the never-reporting required context as *pending*, not failed — `mergeStateStatus: BLOCKED`, exactly the failure the workflow header warns of. A dropped webhook is **not replayed** when Actions recovers: closing and reopening the PR after recovery re-fired `pull_request` and the guard reported `success` on the same SHA. `--admin`/`--no-verify` were not reached for; the ruleset has no bypass actors. | **Nothing outstanding on the branch.** Number-at-merge re-read at the merge instant per the window rule — master still `dd15f7c`, max 176 / Q84, so `#177`/`Q85` stood without re-resolution. Move 2 (the `FieldConfidence` tolerance question) is deliberately NOT here: it is held at **Q85**, gated on re-uploading the SNP Albumin/Creat Ratio PDF once this deploys and reading the real `loc` off the now-working banner (owner: Luke — needs the operator). Assertion boundary is the helper + a source-level wiring check, not a rendered DOM. |
| `chore/session-closeout-0806` | Session close-out for 2026-08-05/06: regenerates `closeout.md` (real commits, pending-queue reconciliation, cold-resume handoff) and rows this branch. No store content changed — `DECISIONS_LOG` and `OPEN_QUESTIONS` were already landed by PRs #21–#26. | **DONE** — written at land per the self-row convention. Branch gate passed at close: all six session branches merged + remote-deleted; `feat/cbti-eval-trigger` pre-existing, untouched, rowed OWED below. Guard green. | **Nothing outstanding on the branch.** Three live threads carried in `closeout.md`, none blocking: the Withings source-side revoke (operator, phone — the one item needing no keyboard), `#175` OWED on the `'unknown'` identity policy + era-split coverage query, `#174` OWED on the field-name contract test. `CLAUDE.md` Recent landings deliberately unchanged — it already holds exactly this session's three landings at the 3-cap. |
| `gov/175-precondition-narrowed` | Resolves `#175`'s identity precondition **in the safe direction** and narrows it. Identity DOES arrive — HCA's mappers thread `sourcePackage: r.metadata?.dataOrigin ?? null` and the live `record_sources` rows carry real packages; the `WriterIdentity` docstring was the stale artifact. Withdraws the fail-closed-on-everything scenario; keeps the real risk (`'unknown'` legitimately exists and must not be silently dropped). Corrects the docstring — the source of the confusion. `Q83` carries the live detail + the two discharging moves; `#175` gets a marked rider. **Second banked concern:** mints **#176** — governance edits bank to one batched PR per checkpoint, gated by **diff shape, not file class** — and inserts the rule into `CLAUDE.md`'s repo-specific merge-path section. | **DONE** — written at land per the self-row convention. Docstring edit proven behaviour-neutral: the `.py` diff touches no line at column 0 and no field/class/def declaration, so it is inside the triple-quoted string only; file re-parses. Guard green; state-label scan clean. | **Nothing outstanding on the branch.** `#175` stays **Status OWED** — decided, not implemented. The narrowed precondition is dischargeable with data, not a blocker: (1) one bounded `health_connect_record_sources` coverage query when Railway is reachable — `'unknown'` vs real per record type, **split by era** — and (2) decide `'unknown'`'s policy explicitly (admit-with-flag / pre-`#175` max-pick fallback / log-and-count per `#74`) before the filter is written. Tracked at `Q83`. **#176's invariant (c) is MANUAL today** — no script asserts "no removed line outside a declared replacement region"; the revisit trigger names `Q80`'s uniqueness arm plus a removed-lines check as what would make it guard-enforced. This PR is the first application of the rule to itself: two banked concerns, one PR, human-gated because it touches a code file. |
| `gov/175-source-admission` | Mints **#175** — source **admission** replaces source **priority** for HC sleep: `_aggregate_day` admits only from an allow-list of registered measuring sources, any other writer excluded by default rather than ranked. Narrows `#35`/`#36`/`#37`. Names the accepted trade (a genuinely new legit device contributes nothing until deliberately admitted) and discharges the `Q83` OWED entry-note. Governance only; no code. | **DONE** — written at land per the self-row convention. Placeholder resolved on-branch after re-reading master's max at that instant; guard green post-resolve; state-label scan clean; diff confirmed to touch only this branch's own additions. | **#175 is Status OWED — decided, not implemented**, and carries a **precondition on the code, not the decision**: `WriterIdentity` documents *"current HCA builds send no dataOrigin"* and `_capture_record_sources` coalesces missing identity to `'unknown'`, yet `Q83`'s evidence shows real package names on 2026-08-03. Both cannot be current. **Resolve which before writing the filter** — an allow-list over `'unknown'` admits nothing and fails closed on every night, which is worse than the fail-silent it replaces. Then `Q83` → `DONE → #175`. Sequencing unchanged: admission before `Q82`'s fragment-merge. |
| `gov/q83-mirror-loop-resolution` | Records that `Q83`'s identical-`record_start` sub-finding is **confirmed** as a Withings Health-Mate mirror of Samsung's own sleep — an echo, not a second sensor — and reframes the fix from "prefer Samsung" to **default-untrust** (select from a registered measuring source; treat any other writer as derivative unless known independent). Adds the source-side half: revoking Withings' HC **write** permission for Sleep kills the duplicate pre-ingest and needs no repo work. `OPEN_QUESTIONS` Q83 only; no decision minted, no code. | **DONE** — written at land per the self-row convention (`chore/branches-self-row-convention`). Guard green; state-label scan clean (one `**State:**` per question, no `**Status:**`). | **Two things deliberately NOT done here.** (1) The **source-side revoke is Luke's to make** — a phone setting, higher leverage than the code half, and it demotes the code half from live bug fix to robustness. (2) **No `DECISIONS_LOG` entry was minted**, and one is OWED if the reframe is to bind: default-untrust *narrows* `#35`/`#36`/`#37` from source **priority** to source **admission**, which changes a landed decision rather than detailing it — that is an append the user should sanction, not a side effect of a question edit. Flagged in-row at `Q83`. |
| `chore/post-landing-pointers` | Post-landing pointers for PR #21, both out of that brief's stated two-file scope. `CLAUDE.md` Recent landings gains **#173**/**#174** at the top and **#169** drops off the 3-entry cap; the `gov/open-questions-sweep` row below moves OWED -> DONE with its landing SHA. No store content, no code — pointers only. | **DONE** — merged to master via PR #22 (merge `9ebd583`, commit `e10e639`, on top of `51f19c1`); local + remote branch deleted, `git ls-remote` returns 0 refs. Guard green on master post-landing. Merge was held for Luke and released after he verified the PR from GitHub against its **actual parent** (`51f19c1`) rather than a stale local `master` — the diff is two files, +4/−2. | **Nothing outstanding.** Row corrected on `chore/branches-self-row-convention`: it was written in-flight as OWED and was therefore false the moment the branch landed. |
| `chore/branches-self-row-convention` | Corrects the `chore/post-landing-pointers` row above, which was written **in-flight** (`OWED` / "PR #22 open" / "merge held") and so asserted a false state from the instant that branch merged — a canonical store lying about exactly the thing the terminal-state gate exists to police. Also records the convention that prevents it, which the repo already followed but never stated. | **DONE** — written at land per the convention below. | **Nothing outstanding.** **The convention, now explicit: a branch's own row records its TERMINAL state, written from within the branch, not its in-flight state.** `feat/capability-observations` already did this (`DONE` — ff-merged, branch deleted) and it is the only form that terminates: an in-flight row can only be corrected by a later branch, whose own in-flight row then needs correcting, without end. It is not optimism — **the row can only reach master by being merged, so a row visible on master is true by construction.** The in-flight facts (PR number, held-or-not, who signed off) belong in the Detail column as history, never in Status. |
| `gov/open-questions-sweep` | Applies the 2026-08-03 chat OPEN_QUESTIONS sweep. Governance only, no code change. Appends **#173** (deep-confidence and Banister inherit `#71` — device deep-sleep is not a readiness input) and **#174** (`/health-connect/sync` field contract single-named on HCA's mapped names, pinned by a contract test rather than codegen). Closes **Q3** (`DONE → #173`, superseded not answered), **Q4** (`DONE → #64`, G4 passed) and **Q7** (`DONE → #72`, 5 injury rows live); re-scopes **Q5** (capture precondition struck), **Q6** (unbuilt work, not an unverified one — `load_metrics` does not exist) and **Q20** (decoupled from Q7). Mints **Q81** (deep-sleep-stage validity), **Q82** (fragmented-session undercount), **Q83** (source-blind Samsung/Withings selection) and **Q84** (backend accepts record types HCA never posts). Fixes the Gate-3 footnote pointer. | **DONE** — merged to master via PR #21 (`51f19c1`, commit `9bd31b9`); local + remote branch deleted, `git ls-remote` returns 0 refs. Guard green on master post-landing (`check_governance_placeholders.py` exit 0). `#NEXT+N` / `Q#NEXT+N` placeholders resolved on-branch after re-reading master at that instant, and **re-read again immediately before the merge** — master was unchanged at **#172** / **Q80** (head `f68c73c`) both times, so no re-adjudication was owed. Headings verified unique and contiguous; the added-lines diff grepped clean for stale `#168`/`#169`/`Q79`/`Q80` literals. Independently verified from GitHub by Luke before the merge, not taken on Code's self-report. | **Nothing outstanding on the branch.** Two follow-ons landed separately on `chore/post-landing-pointers` (`CLAUDE.md` Recent landings, this row). **Two remain open and are tracked in their own stores, not here:** the `#174` code collapse — five dead-branch deletions in `backend/routers/health_connect.py` — **must not ship without the field-name contract test** (its evidence's load-bearing half is a cross-repo read; without the test the deletion breaks production silently on the next sync), tracked at `#174` Status OWED and `Q5`; and `Q83`→`Q82` in `_aggregate_day` (source priority before fragment merge). |
| `feat/capability-observations` | Give capability a HISTORY: new append-only `capability_observations` table (migration `b6f3d92a4e17`) recording measured QUANTITIES per `(region, side, measure)` with the date measured, plus a declared measure registry on `taxonomy.Region.measures` (`Measure(key, unit, higher_is_better, per_side)`, 7 of 31 regions seeded), `engine/observations.py` (`record_observation` / `series` / `latest` / `symmetry`), and two routes on the EXISTING `/engine` router. Scopes the wearable-metric invariant to the VERDICT, not the measurement — `capability_state.status` stays self-reported, an observation may be device-derived — with both prose assertions amended so the narrowing is on the record. Purely additive: `capability_state`, `engine/selection.py` and the Probe queue are untouched and nothing existing reads the new table. DECISIONS_LOG **#161**, OPEN_QUESTIONS **Q72**. | **DONE** — ff-merged to master, pushed; local + remote branch deleted (`git cherry origin/master` clean). Backend suite **578 passed** (545 baseline +33). Migration chained on verified head `d7c4b1a90e35`, single head confirmed via `ScriptDirectory.get_heads()`; applied and reversed against a scratch SQLite DB from a stamped parent. `#NEXT` resolved to **#161** ON-BRANCH before the ff (G5); master max was **#160** at that instant, re-read rather than reused from session open. **Renumber scope per #148 — classified, not counted:** 13 branch-own tokens resolved across `DECISIONS_LOG.md` (heading), `OPEN_QUESTIONS.md` Q72 (×2), `SCHEMA.md` §020, this row, and **seven source files** — `backend/models.py` (×2), `backend/routers/engine.py` (×2), `backend/engine/__init__.py`, `backend/engine/taxonomy.py` (×2), `backend/engine/observations.py`, migration `b6f3d92a4e17`, `backend/tests/test_capability_observations.py`. **10 residual `#NEXT` deliberately left**, each classified: 7 in `DECISIONS_LOG.md` are rule-text ABOUT the convention (#41, #148); 2 in `models.py` and 1 in `SCHEMA.md` §016 are pre-existing `ExerciseRegionTag` debt from an earlier branch. A bare `s/#NEXT/` would have minted this decision's number onto that debt — plus tokens in `selection.py` (×2), `hevy_templates.py`, `connectors/hevy.py`, `seed_exercise_region_tags.py`, two test files and migration `b2f1c9a4d7e8`. Every substitution was CONTENT-anchored and asserted to match exactly once before writing; a line-indexed plan was abandoned after the working tree changed underneath it mid-session. | **TWO BRIEF FACTS WERE FALSE and are corrected in-branch, not worked around.** (1) §3 claimed "no capability endpoints exist / the engine currently has no HTTP surface" and §9 accordingly put the routes in `main.py` — `backend/routers/engine.py` has existed with seven `/engine` endpoints incl. `GET /engine/capability-state` and `POST /engine/response`; the two new routes went there. (2) §8 assigned `deceleration_landing` / `change_of_direction` the side `bilateral`, but both are `per_side=True` in `taxonomy.py`, so the seeded battery would have been REFUSED by the fail-closed side guard the brief itself specified. Resolved by moving sidedness onto `Measure`, bounded by the region's `sides()` — a trunk-mounted GPS records one figure for the athlete and cannot attribute it to a leg. **Deliberately NOT built** (brief §10): the Catapult `.gt` max-velocity backfill (zip → brotli → msgpack, mm/s), MED / dose-response computation, and any change to `capability_state`, `selection.py` or the Probe queue. **A model-vs-migration DDL diff caught a real drift** — `created_at` NOT NULL on the model, nullable in the migration — fixed, re-verified clean; the visual read of the DDL had missed it. The full migration chain does NOT replay on SQLite (pre-existing `da60d2b93599` uses Postgres-only `ALTER COLUMN ... DROP DEFAULT`), so the new migration was exercised in isolation, not via full replay. | **Q72 opened, not guessed:** the eighth measure the brief listed — `max_velocity_ms` (hamstring velocity proxy, Catapult) — has no home on the v0 axis list. Verified: 31 regions, and the only velocity/speed/sprint token is `gait_speed`, a §G longevity axis (`queue_eligible=False`, probing test "Timed walk") whose norms are geriatric-referenced. Left unseeded per the brief's own instruction to flag rather than guess. Resolve before the `.gt` backfill runs. Owner: Luke. |
| `feat/frontend-readback` | Close the input-first read-back/nav gaps (three modules invisible in the UI: labs URL-only, check-in not viewable once done, CBT-I no surface). **A1:** Dashboard nav links → Labs (`/metrics`) + History (`/checkin-history`). **A2:** one-line `Session` import in `mcp_server.py` (correctness). **A4 backend:** `GET /labs/results` (#59 consumer), report-grouped, user-scoped (#42), #47-bounded at the projection. **A4 frontend:** read-only stored-results table in `Metrics.jsx`. **A3:** `CheckInHistory.jsx` (new `/checkin-history`) over the existing `GET /checkin-v2/history` + PM done-state value read-back. Investigation first: several brief claims were stale vs master (checkin-v2/history already REST; the `Session` "crash" is a no-op local annotation; CBT-I engine is built). OPEN_QUESTIONS **Q60** (CBT-I interim surface — gated on #47, not the firewall). No DECISIONS entry — follows locked #47/#49/#59/#42. | **DONE** — ff-merged to master; local + remote branch deleted (`git cherry` clean). Commits `e01ed01` (fix: Session) → `473461e` (feat: /labs/results + tests) → `6264c8a` (feat: frontend surfaces + nav) → `38ab3d0` + `75607cc` (governance) → renumber-and-land governance (tip). **Renumber on-branch before ff (G5):** `Q-NEXT` → **Q60**; no DECISIONS integer (no entry). Also folded a review correction: the CBT-I Q was re-led on #47 (the earlier draft led on I1/display hygiene). Backend suite **464** (460 baseline +4; commit `473461e`'s message says "464→468" — an arithmetic slip, the real total is 464, left in the pushed message rather than rewriting history). Frontend builds clean (99 modules), app boots + login renders with zero console errors. | **Deliberately NOT built:** the interpreted lab view (stays 4b-ii, #49 — A4 is the raw #47 precursor, a different surface); the CBT-I user surface (gated on #47 — may the MOVE/REVERSE verdict be surfaced as a directive at all — with the I1 firewall a secondary constraint; OPEN_QUESTIONS Q60, ROADMAP NEXT); check-in **edit + audit trail** (the ROADMAP NOW item — A3 is display-only); `/interpretation` left UNLINKED (renders an inert fixture, #135 — linking would show fabricated data). Also note: the AM read-back is the history page, not an AM done-state recap (the morning form is re-enterable) — a reasonable substitution for Q2's "done-state", Luke-confirmed at review. | **— (merge complete).** Q60 minted on-branch before the ff. **OPEN LOOP — post-deploy operator checks (authed surfaces unverifiable from a Code session: no login, `/labs/results` was branch-only):** (1) the Labs table renders the ingested results — this **doubles as discharging the #42 `user_id`-binding item** (an empty table = the 27 rows are bound to a different user_id than the app login); (2) `/checkin-history` renders; (3) the nav links work. Deploy is not verification — record the results. Owner: Luke. |
| `fix/pin-mcp-sdk` | **Production-down fix.** Pin `mcp[cli]` `>=1.0.0` → `==1.28.1` in `requirements.txt` — the only unpinned line, which resolved to the same-day mcp 2.0.0 and removed `mcp/server/fastmcp`, taking the app down at import since 2026-07-28 19:49 (migrations had already applied, so the DB advanced past the code and the stale container kept serving). One line, no code change. Findings recorded (`OPEN_QUESTIONS` Q59): no CI, and no check observes that the app boots. DECISIONS #149. | **DONE** — ff-merged to master as `c4e5da2`; local + remote branch deleted. Two concern-split commits: `91dc2c9` (fix: the pin) → governance close. Suite **460**, unchanged. **Renumber was MISSED at merge and repaired by a follow-up commit on master, not on-branch (the branch was already gone):** `c4e5da2`'s message claimed `#149`/`Q59` but the commit contained **only** `.claude/launch.json` — the governance edits were never staged. Cause: an unstaged working tree + `git add -A`, where an untracked `.claude/launch.json` gave `git commit` something to commit and masked the `nothing to commit` failure that would otherwise have surfaced the no-op immediately. Resolved here (`### 149.` / `## Q59.` substituted; staged by name). | **Deliberately NOT attempted:** the mcp 2.0 migration (`FastMCP` → `MCPServer`, a redesign not a rename; auth/transport paths survive, blast radius one file) — its own increment, because the release was hours old, nothing verifies the app starts, and production was stale during the decision. No CI added, no boot check (Q59). Verified in a **clean venv** (not the session venv, which already holds a working SDK): fresh install resolves mcp 1.28.1 and imports all six SDK paths the app reaches. | **— (merge complete + deploy verified).** #149 / Q59 resolved by the follow-up master commit above. **Deploy confirmed healthy (operator, post-merge):** deployment `1778471d` **Online**, healthcheck passed; `GET /health` → `{"status":"ok"}`; database at `c1e8b4d70f92`, both migrations applied and confirmed by operator query (no `overall_confidence` zeros; `marker_canonical IS NULL` count = 0). **Still outstanding (operator-only):** the last-good pre-failure build log's `Successfully installed` line names the exact prior mcp version — if it differs from 1.28.1, prefer it. Owner: Luke. |
| `feat/ingestion-findings` | Close the three findings from the FIRST real ingestion run (2026-05-30 draw, 7 Sullivan Nicolaides reports, 27 results, ingested 2026-07-28). **Step 1 (feat):** `overall_confidence` becomes DERIVED at confirm as `min(row confidences)` — removed from `ReportExtractionMeta` + the prompt example (it defaulted to 0.0 and the example seeded it, so three reports stored 0.0 despite 0.92–0.99 rows); backfill migration `b7f3a1c92e40` recomputes every existing report. **Step 2 (asset):** `psa` canonical entry (`unit_established: "ug/L"`, byte-matching the stored unit) + rebind migration `c1e8b4d70f92` for the one unmapped row. **Step 3 (decision):** the interpretation trigger is a DRAW (newest `collected_date`), not a report — seven reports shared one date; producer docstring corrected (doc-only). **Step 4:** read-only-confirm findings recorded, nothing built. Also normalised entry headings 143–145 to no-hash. DECISIONS #146/#147, OPEN_QUESTIONS Q58. | **DONE** — ff-merged to master; local + remote branch deleted (`git cherry` clean). Commits `18a94dc` (gov: heading normalise) → `c710756` (feat: derived confidence + backfill) → `905154c` (asset: PSA + rebind) → `5b94316` (governance close) → renumber-and-land governance (tip). **Renumber applied on-branch BEFORE the ff (G5):** DECISIONS `#NEXT`×2 → **#146/#147**; OPEN_QUESTIONS `Q-NEXT` → **Q58**; and — new this merge — **six source tokens** the branch itself wrote (`labs.py`/`test_labs_confirm_confidence.py`/backfill migration → #146; `producer.py` draw-docstring → #147; the resolved `#140` at producer.py:246 left untouched). Backend suite **460 passed** (457 baseline +3). Two data migrations (no schema change); no `frontend/`. | **Deliberately NOT built:** the 4b-ii `GET /labs/interpretation` endpoint (draw-triggered per #147 — decision recorded, logic deferred to its own brief) and the **editable confirm screen** (`OPEN_QUESTIONS` Q58, findings A/B/C — needs a provenance design first; no `frontend/`, no schema, no provenance field added this branch). Derived confidence is triage, not a verdict; no human-verification field folded into `confidence`. | **— (merge complete).** #146/#147 minted and Q58 resolved on-branch before the ff (G5). **Operator steps STILL OUTSTANDING post-deploy (Railway unreadable from session — deploy is not verification):** `SELECT id, panel_name_raw, overall_confidence FROM lab_reports ORDER BY id` (no report left 0.0 unless its rows warrant) and `SELECT count(*) FROM lab_results WHERE marker_canonical IS NULL` = **0** (PSA bound; if the rebind hit 0 rows the stored `unit_canonical` differs from `ug/L` and both the canonical entry and the rebind gate need correcting). Record the results back on this row. Owner: Luke. |
| `feat/relation-preconditions` | Resolve **Q56 + Q57**. **Q57 (asset):** `declared_factor_keys` on all six `lever_dictionary.levers` nodes — only `testosterone_substrate_load` joined (`["trt"]`), the other five `[]` (truthful "no declared factor represents this lever", distinguishable from absent); no declared-state entry invented. **Q56 (asset):** a precondition object `{factor_key "trt", admissible_phases ["steady"], grade "moderate", rationale, 5 evidence_refs, contested_note}` on `hpg_gonadotropin_suppression`, authored+authorised by Luke and transcribed verbatim, replacing `precondition_phase: "on_trt"`. **Producer (feature):** resolves `precondition_status` (satisfied/not_satisfied/unresolvable-naming-the-factor) and emits `expected_by_phase` with **no authority**; declared state resolved ONCE, draw-dated. **Step 0 (gov):** resolved the carried `#NEXT` docstring in `producer.py` → `#140`. DECISIONS #143–#145. | **DONE** — ff-merged to master; local + remote branch deleted (`git cherry` clean). Commits `eb024b5` (gov: Step 0) → `fce128a` (asset: Q57) → `778a3c1` (asset: Q56) → `0404c64` (feat: Steps C–E) → `524cc60` (governance close) → `cd35786` (asset: promotion-note) → `f37b759` (session close-out) → renumber-and-land governance (tip). **Renumber applied on-branch BEFORE the ff (G5 — master never carried `#NEXT`):** DECISIONS `#NEXT`×3 → **#143/#144/#145** (file order); Q56 `DONE → #143` (precondition), Q57 `DONE → #145` (lever join, incl. the mid-sentence `Corrected #145`). Backend suite **457 passed** (453 baseline +4). No migration, no schema, no `frontend/`. | **HELD for 4b-ii** (demotion stays held; the split is the same as #142): relation-based **demotion** of gate 1's delta arm, `shared_levers` **emission/filtering** (the *join* landed here, the *consumer* did not), `axis_verdict`, `member_lever_effects`, `mechanism`, `stable_rationale`, the `GET /labs/interpretation` endpoint, and fixture regeneration. **`expected_by_phase` is no longer held — it is emitted** (with no authority). `on_trt` gone from producer source + live asset (G7); history/`_deferred`/held-fixtures legitimately retain it. Q57's lever join proven output-neutral; the precondition resolution proven should_surface/news_gate byte-identical. | — (complete). Merged; #143/#144/#145 minted and Q56→#143 / Q57→#145 resolved in the renumber commit, before the ff (G5). **4b-ii's only remaining blocker is ingestion** (empty `lab_reports`/`lab_results`, view-wiring half); the two asset blockers are discharged here. Owner: Luke. |
| `feat/interpretation-relations` | Interpretation producer **4b-i (structural half)**: three-pass restructure (`should_surface` computed after relations, not during member build), member `relations_rendered[]` with operand degradation (`complete`/`degraded`+`operands_missing`, never fabricated) and `feedback`→`precondition_status: "unresolvable"`, and `meta.protocol_context_snapshot` dated to the panel's `collected_date` (never `date.today()`). Governance-only Step A first corrected two stale safety-asset facts (Q41→`DONE → #139`; ROADMAP safety-band blocker + gate-3 row). DECISIONS #140–#142, OPEN_QUESTIONS Q56/Q57. | **DONE** — ff-merged to master; local + remote branch deleted (`git cherry` clean). Commits `e84383d` (gov: Step A) → `f6b3d1c` (feat: Steps B/C) → `3a9b18e` (feat: Steps D/E) → `c541561` (governance close) → `1e3896d` (session close-out). **Renumber applied at merge:** DECISIONS `#NEXT`×3 → **#140/#141/#142** (file order); OPEN_QUESTIONS `Q-NEXT`×2 → **Q56/Q57**; entry #142's "next entry / Q-NEXT" ref → Q56. Backend suite **453 passed** (448 baseline +5). No migration, no schema, no `frontend/`. | **Deliberately HELD, not forgotten** (named so an audit does not read absence as omission), all 4b-ii: relation-based demotion of gate 1's delta arm, `shared_levers` (I3 already-in-play filtering), `axis_verdict`, `member_lever_effects`, `mechanism`, `stable_rationale`, `expected_by_phase`, the `GET /labs/interpretation` endpoint, and fixture regeneration. `relations_rendered` emits on grouped members ONLY; ungrouped rows keep the full absent list by construction (no group → no relations). Restructure proven behaviour-neutral (byte-identical `build_foundation` output pre/post; 4a test file byte-unchanged at the checkpoint). | — (complete). Merged; numbers resolved in the merge governance commit. **4b-ii blockers (never blocked THIS branch, now numbered):** **Q56** (`precondition_phase`↔`derive_phase` vocabulary mismatch) and **Q57** (lever↔declared-factor join) — both asset content; plus ingestion (empty `lab_reports`/`lab_results`, unverified-from-session) which blocks 4b-ii's view-wiring half only. Owner: Luke. |
| `gov/session-closure-sweep` | The #90 vocabulary-adoption sweep (completes #88's adoption, does not amend it): relabel every `BRANCHES.md` row + `OPEN_QUESTIONS.md` entry in health-app to the four states (DONE/BLOCKED/OWED/UNSTARTED); push 3 previously local-only branches with dedicated rows; adjudicate 2 invalid-state rows + 5 OWED loops against evidence; strike the stale ROADMAP "CLAUDE.md deferred to laptop" row. Governance-only — no `backend/`, `frontend/`, or `alembic/`; test count must not move. DECISIONS_LOG #90 + FEEDBACK §13 at merge. | **DONE** — ff-merged to master at `05f0282`, pushed; local branch deleted (was local-only, never pushed to origin). 7 concern-split commits, `e99c09e` → `05f0282`. Governance-only: 6 files, zero `backend/` / `frontend/` / `alembic/`; backend suite **206 passed**, unchanged from #88's 206. DECISIONS_LOG #90 + FEEDBACK §13 landed. | — (complete). | **Phase 2 remains OWED (separate repo, separate session):** `health-connect-app`'s `BRANCHES.md` (4 rows) and `OPEN_QUESTIONS.md` (Q1/Q2/Q4/Q5 on `PENDING`) are still on superseded labels — this sweep could not reach them under the single-repo rule. Owner: Luke, in an HCA-rooted session. |
| `feat/feedback-ledger` | `FEEDBACK.md` §19 — append-only, status-mutable integrity ledger for analysis-loop failures, typed `HUMAN`/`MODEL`/`COUPLED`, seeded 12 rows from the 15 Jul calf investigation; `prevention` mandatory (inclusion test as hard gate), `signed` records error direction as a bound, `status` mutable so phantoms are struck-not-deleted, `caused_by` = inverse of `caused`. CLAUDE.md §19 pointer (repo-specific section); OPEN_QUESTIONS Q50/Q51. Landed DECISIONS_LOG #129–#132. Governance-only: no feature code, no migration, no schema, no test delta. | **DONE** — merge-landed to master (own merge commit), pushed; local + remote branch deleted. Governance-only (suite unchanged at 412). **Renumber applied at merge:** DECISIONS `#85`→`#129`, `#86`→`#130`, `#87`→`#131`, `#88`→`#132`; FEEDBACK `§12`→`§19` (master's §12 is #88's unseeable-surface rule; subsections `12.1`–`12.6`→`19.1`–`19.6`, and the heading's `#85–#88` back-ref → `#129–#132`); OPEN_QUESTIONS `Q28`→`Q50`, `Q29`→`Q51`; CLAUDE.md §12 pointer → §19. Master's content preserved throughout; branch entries appended at next-free. | — (complete). | — (complete). |
| `feat/interpretation-view-skeleton` | Increment 1 of 5 of the lab-interpretation view: static three-section render (What Moved → Stable → Mechanisms) of the contract v0.4 §2 worked example, committed verbatim as `frontend/src/fixtures/interpretationExample.json`; new `/interpretation` route behind `RequireAuth`; hybrid weighted-inline levers; colour-as-fact. New `frontend/src/components/interpretation/*` + `pages/InterpretationView.jsx`. Frontend-only — no backend, no migration, no schema. DECISIONS_LOG #135. | **DONE** — merge-landed to master (own merge commit), pushed; local + remote branch deleted. Ships **INERT by design** (renders a fixture; the `context_builder` AI pointer swaps only at a later increment). Frontend-only, so the backend suite is unmoved (**445**). **Renumber applied at merge:** DECISIONS `#NEXT` → `#135` (its cross-refs #47/#49/#63/§2/§8 point at existing master entries and are unchanged; the `#49` in `LeverStrip.jsx` is an existing-decision ref, kept). **CONTRACT DRIFT — landed as owed work, NOT fixed this session (per brief):** the committed fixture's top-level keys are `['groups','meta']` but master's #86 producer emits `{meta, groups, ungrouped}`. Increment 1/5 renders against this superseded contract; because it is fixture-driven nothing ships broken, but the increment that wires it live must regenerate the fixture and add an `ungrouped` render section. Tracked as **OPEN_QUESTIONS Q54**. | — (complete). | Increments 2–5 are separate future work; Q54 gates the live-wiring one. |
| `feat/checkin-injury-probe` | Probe tier of the injury-aware check-in: `backend/injury_probes.py` — a versioned, provenance-stamped scaffold for injury-type-specific probe questions, seeded with ONE worked example (gastroc strain); two structural gates (elicitation-only questions, referral-only escalation) enforced in tests and both mutation-tested; cadence from injury type + entry set-date. Strikes the stale "future" note in `docs/checkin-schema.md`. OPEN_QUESTIONS Q52/Q53. No migration, no schema, no frontend. | **DONE** — merge-landed to master (own merge commit), pushed; local + remote branch deleted. Ships **INERT by design** (no ledger entry carries `injury_type`; `evaluate_escalation` is a pure function with no caller). **Renumber applied at merge:** DECISIONS `#89`→`#133`, `#90`→`#134` (and the `#89`/`#90` strings in `injury_probes.py` + `tests/test_injury_probes.py` docstrings moved with them); OPEN_QUESTIONS `Q30`→`Q52`, `Q31`→`Q53`; `docs/checkin-schema.md` probe-questions note `#89`/`#90`→`#133`/`#134`. No FEEDBACK entry (the branch references §8/§10/§11 but adds none). `docs/checkin-schema.md` soreness bullet kept as MASTER's (branch's variant dropped, master wins); the branch's new probe-questions bullet appended. Suite **445 passed** (was 412, +33 injury-probe tests). | — (complete). | Feature goes live only when a real injury entry carries `injury_type: gastroc_strain`. |
| `feat/interpretation-producer-foundation` | Interpretation producer FOUNDATION (4a): `backend/interpretation/` (gates + `build_foundation`) over a new `labs_reads.marker_series` seam. Emits `{meta, groups[], ungrouped[]}` with 4a fields only — every 4b field absent. DECISIONS_LOG **#86**. | **DONE** — landed on master at `f02b4a2`. Backend-only, no migration. 3 concern-split commits: `012ef1d` (feature: series + gates + producer + tests) / `b249a60` (debt: declared-state #85 mint) / 4a governance. **206 tests green** (184 pre-existing incl. #85's 47, + 22 new). | — (no external precondition to land) | **Branch RESET this session:** its prior 3 commits (superseded groups-of-one design, never merged) were discarded — this brief re-specced ungrouped markers as a FLAT `ungrouped[]` list (not synthesised groups-of-one), a lab_flag-only gate 2, and an explicit-panel signature. **#86 provisional:** if `feat/feedback-ledger`/`feat/checkin-injury-probe` merge before this is pushed, they take #86+ in merge order and this renumbers. **Oracle caveat:** `INTERPRETATION_OUTPUT_CONTRACT.md` (corrected §2, UI-maintained per #63) is NOT in the repo; the fixture `backend/tests/fixtures/interpretation_s2.json` is the §2 worked example from `feat/interpretation-view-skeleton`, mechanical 4a fields stable across the 4b correction. That divergence risk has now occurred ONCE and been resolved (#87): the corrected contract file / asset carries `hepatocellular` display_name "Hepatobiliary enzymes + bilirubin", the fixture was re-synced to match, and the G3 oracle now asserts group display_name — so the re-sync mechanism is exercised, not theoretical. Re-sync again if it diverges on any further field. Gates G5/G6 mutation-tested; G1/G2 boundary greps clean (no current_state/declared_state import, no `levers[]` read); `latest_lab_results` byte-unchanged. **Unblocks 4b** (verdict/relations/levers, now phase-aware via #85). **Still dangling (not this session's concern):** `feat/interpretation-view-skeleton` (increment 1 render layer) remains an unmerged local branch — it consumes this producer's shape once wired. |
| `feat/protocol-declaration` | The prerequisite 4a spawned before 4b: a structured, queryable declaration of the user's stack. `backend/declared_state.py` (continuity-aware value schema + pure `derive_phase` + `is_assumable_present` + the `current_state` lift) and `_seed_declared_state` in `seed_engine.py` seeding the confirmed real stack under three new `user_knowledge_entries` types (protocol/supplement/behavioural). Closes the "no structured protocol" gap 4a surfaced; corrects the unreliable Clinical_Protocol.md. DECISIONS_LOG `#NEXT`. | **OWED** — landed on master at `b249a60` (declared_state + seed + governance); prod seed live (operator-confirmed, 23 rows written). Open loop = confirm `current_state.declared_state` READS BACK 23 factors in prod — the seed WRITE is attested, the read-back is not. Owner: Luke. DECISIONS entry **minted #85** (was an unminted `#NEXT` in master — the number-at-merge debt, reconciled 2026-07-18: it was already in master past #84, so it took the lowest free number; `feat/feedback-ledger` #85–88 and `feat/checkin-injury-probe` #89–90 are unmerged hardcodes that renumber at their own merges). Backend-only, **no migration**. **184 tests green** (137 + 47). | — (no external precondition to land) | **Numbering:** headed `#NEXT`; master max is **#84**, #85–#90 are local-only hardcodes on `feat/feedback-ledger` / `feat/checkin-injury-probe`. **MERGED ≠ LIVE:** `current_state.declared_state` reads EMPTY until the Railway seed runs — a Luke operator step post-merge (`seed_engine.py`, injury-ledger precedent). **Design call refining the brief, mutation-proven:** the row's `active` column ("this declaration is current", always True) is SEPARATED from `value["active"]` ("the user takes this"). The brief's literal reading — write the row inactive when the factor is untaken — was rejected because it (a) drops tirzepatide/glow/hgh/ultra_muscleze_night out of `current_state` (which loads active=True rows only), making their phase underivable, and (b) breaks idempotency (skip keys on active=True, so untaken rows duplicate every re-run — the mutation fails `assert 4 == 0`). `derive_phase` orders by CONTINUITY, never by value.active, so a stopped factor still resolves washout/stopped. **Phases verified end-to-end through current_state** against the brief's table: trt→steady, tirzepatide→washout, cjc_ipamorelin/igf1_lr3→episodic (active yet `assumable_present=False`), glow→stopped, hgh→None, cbt_i→re_entering, ultra_muscleze_night→None. Derivation mutation-proven non-vacuous 4 ways. **Seed corrects Clinical_Protocol.md** (HGH never used; peptides episodic-not-discontinued; GLOW = BPC-157/TB-500/GHK-Cu not KPV); tirzepatide date flagged triangulated-not-a-dosing-log. **Unblocks 4b** (phase-aware verdict/relations/levers). **Follow-on, separate concern:** surface `declared_state` into the context_builder system prompt (this branch deliberately does NOT touch prompt rendering). **Incidental:** untracked `backend/gate_test.py` (operator stray) fires a live paid Anthropic call at pytest COLLECTION and 400s a bare `pytest` — scope to `tests/`; not this session's to delete. **Loop (partially closed):** the Railway seed HAS run (23 rows, operator-confirmed). Still owed: confirm `current_state.declared_state` reads back 23 factors in prod. Pairs with `feat/interpretation-view-skeleton` (inc.1) and `feat/interpretation-producer-foundation` (4a), both in flight; none touch each other. |
| `fix/probe-harness-fidelity` | Make `probe_resolver.py` able to reach the code it measures, and unable to report success when it doesn't (FEEDBACK §11): per-probe subject expectations + non-zero exit, turns that drive to a block, constraint-neutral subjects, and the injury-refusal probe split onto its own pinned fixture. | **OWED** — merged (`adb67e8`, 2026-07-15; ff to master, local branch deleted, never pushed to origin) but the loop is NOT closed: the outstanding check is `/opt/venv/bin/python probe_resolver.py 1` in the container, which has never been run (operator-only). 2 commits: `5c5b43f` (instrument) / `adb67e8` (governance). B1–B4 rode one commit deliberately — B4's probe depends on B1's expectation machinery, so splitting would have landed a red intermediate. **Test instrument only — no production code path changed** (diff touches `backend/probe_resolver.py` alone). No migration. 137 backend tests green. | — (no external precondition to land) | **Verified under `--synthetic`, exit 0, both probes reach their subject:** resolver forced 3 guessed titles (`Calf Raise`, `Preacher Curl`, `Pullover`) in 2 turns with `suggest_candidates` actually called; injury probe confirmed no forbidden id reaches `create_routine`. B1 proven by reproducing the 2026-07-15 null result with a never-confirming probe → loud non-zero. **Finding (behaviour, not a defect):** the injury gate does not refuse wholesale — it refuses the MOVEMENT and substitutes a safe one ("Lower A (Injury Modified)": leg press + trap bar deadlift). A `provisioned == 0` predicate failed on the platform working correctly, so the expectation is now "forbidden template ids never provision". Loop closes on Luke: `/opt/venv/bin/python probe_resolver.py 1` in the container — the **live** run, which still has NOT been executed by Code (the container is operator-only). The injury probe is synthetic-only by design and will be skipped there. |
| `feat/resolver-candidate-suggestions` | Ranked catalogue candidates on an unresolved title (#83) — resolution stays EXACT, a unique candidate is never auto-resolved, fail-closed unchanged; plus `backend/probe_resolver.py` landed as an operator-run model-facing probe (#84). | **DONE** — all 5 concern-split commits landed on master (ff-merged, branch since deleted): `35f4d1c` (suggest_candidates) / `b21ce7f` (chat candidate warning) / `62bf248` (probe harness) / `40c8b0d` + `3feaa44` (#83/#84 governance, incl. ratio-tier exercised live). No migration, no schema change. 137 backend tests green. *Adjudicated by content, not `git cherry`: the branch ref no longer exists (neither local nor `origin`), so patch-id was unrunnable — instead confirmed #83/#84 present in `DECISIONS_LOG.md` and `suggest_candidates`/`probe_resolver.py` present in the tree, all five commits on `origin/master`'s history. The prior "IN FLIGHT / local-only, never pushed" label was stale — the work merged and the branch was deleted.* | — (no external precondition to land) | **C3 verified CLOSED** (the chat brief's hard stop): the warning DOES reach the model — actions appended at `chat.py:540` → `ChatResponse.response` → stored as the assistant message (`ChatPanel.jsx:82`) → echoed back as `conversation_history` (`ChatPanel.jsx:77-80`). Loop confirmed live: a 1/3 routine went 3/3 and provisioned on the turn after the warning. **LOOP CLOSED — verified in prod 2026-07-15**, live 494-row catalogue, real model, real user state: the model emitted `exercise_template_id` for in-history movements and a bare title (`Calf Raise`) for the out-of-history one — #82's contract followed exactly. Exact match missed; `suggest_candidates` returned 5 candidates, **all 5 genuine, zero noise** — `Seated Calf Raise`, `Standing Calf Raise`, `Standing Calf Raise (Smith)`, `Standing Calf Raise (Barbell)`, `Standing Calf Raise (Machine)`. Fail-closed held: nothing written until the user disambiguated ("standing machine"), after which the routine provisioned. **`_SUGGEST_MIN_RATIO = 0.5` — OWED ITEM NOW CLOSED (2026-07-15, later run):** the repaired probe forced three guessed titles live and the ratio tier fired for the first time — `Preacher Curl` → `Rope Cable Curl` (0.643) / `Drag Curl` (0.636); `Pullover` → `Pull Up` (0.533). The floor admits noise; containment-first ranking put every genuine candidate above every ratio-tier one, and `limit=5` bounds the tail. Measured, and it held — but ranking is what carries it, not the threshold. The floor must NOT be raised on this evidence: 0.512 (a real candidate) sits below 0.533 (noise), so ratio does not separate signal from noise in either direction. See #83's ratio-tier addendum. |
| `feat/hevy-resolver-activation` | Fire the landed-but-dormant #60/#61 title→id resolver: history renders CATALOGUE titles via an upstream `canonical_title` join (#81), the provisioning contract permits title emission with EXACT matching retained (#82), and the #43 context-builder parity guard is narrowed rather than retired (#80). Strikes the ROADMAP "Hevy resolver activation" NEXT row. | **OWED** — code landed + deployed at `9193453` (ff-merged, pushed; local branch deleted). 4 concern-split commits: `bc81911` (#81 catalogue titles) / `8ca6108` (#82 title emission + #80 guard narrowing) / `320eedf` (B3 pin) / `9193453` (governance). No migration, no schema change. 124 backend tests green. Limb 1 of the loop is prod-proven; **limb 2 is outstanding** = a live chat request naming a nonsense movement, confirming the routine is refused AND that the unresolved title is named back to the user. Owner: Luke. | — (no external precondition to land) | **B0 finding:** the "byte-parity guard" named in ROADMAP/BRANCHES prose is `test_context_builder_output_unchanged_pre_post_refactor` (`tests/test_current_state.py:138`) — a full-string old-vs-new assertion against pinned SHA `3360ed5`. It is UNRE-BASELINEABLE (the SHA cannot move without going old-vs-old, per the test's own comment), hence #80's narrowing. **B3 was a no-op:** `_process_routine_actions` already surfaced unresolved titles and refused the routine; no silent-failure path existed. **Loop CLOSED — evidence, not expectation (adjudicated #90 sweep).** "The resolver has never fired in prod" is now DISPROVEN, on master-canonical evidence independent of `feat/resolver-candidate-suggestions`'s (stale) row: DECISIONS_LOG **#83 "SCALE ADDENDUM (2026-07-15)"** and the immutable-dated governance commit **`40c8b0d`** (2026-07-15) both record a live 494-row prod run, real model, real user state — the model emitted `exercise_template_id` for in-history movements and a bare title (`Calf Raise`) for the out-of-history one (#82's contract exactly), exact match missed, fail-closed held until the user disambiguated, **after which the routine provisioned**. **Limb 1 — CLOSED on that evidence.** **Limb 2 — OUTSTANDING, which is why this row is OWED and not DONE:** "nonsense movement → routine refused with the title named" is a *distinct code path*, not a corollary of limb 1. `suggest_candidates → []` (refuse and name the unresolved title) is a different branch from `suggest_candidates → [5]` (fail-closed pending disambiguation, which is what the 15 Jul run exercised), and the refusal string is the user-visible surface — so it is provable only by running it. Outstanding: a live chat request naming a nonsense movement; confirm the routine is refused and the unresolved title is named back. Owner: Luke. |
| `fix/exercise-tag-coverage` | Fix the one unresolved tag title (`Bulgarian Split Squat` → `Bulgarian Split Squat (Dumbbell)`, keyed to the catalogue not the workout log) + new read-only `backend/audit_exercise_tag_coverage.py` measuring the ID-keyed fallback hit-rate; extracts the three-state rule to `selection.classify_coverage` so the audit and the read path share one definition (DECISIONS_LOG #79) | **DONE** — landed on master at `8a29b1f`, pushed; local branch deleted (never pushed to origin). 3 concern-split commits: reference fix (`68e3c9e`) / audit+extraction (`727adb3`) / governance (`8a29b1f`). No migration, no schema change. 105 backend tests green. | — (audit `--strict` exit-1 path still NOT live-exercised: the prod window had zero untagged movements, so the branch never fired. Unit-tested only.) | **LOOP CLOSED 2026-07-14.** Both operator steps ran in the Railway backend container (`root@3752627012b6:/app`), both verified: (1) `seed_exercise_region_tags.py 1 --confirm` → `titles_resolved: 56`, `titles_unresolved: 0`, `tags_written: 37` (36 + the now-resolving BSS row), `no_pattern_adjudicated: 21`, `confirmed: True`; (2) `audit_exercise_tag_coverage.py 1 --days 28` → **fallback hit-rate 0.0%** over 38 distinct movements (25 TAGGED / 13 ADJUDICATED NO-PATTERN / **0 UNTAGGED**), 12/45 workouts in window. The keyword fallback no longer fires on the active window — the behavioural claim the seed alone could not make. **#79 confirmed in prod, not merely argued:** `B5D3A742` resolves TAGGED as `Bulgarian Split Squat (Dumbbell)` while still `logged as: Bulgarian Split Squat` — a title the catalogue does not contain, which a title-keyed audit would have scored as a miss. (The earlier title-keyed pass scored 38/38 and the id-keyed pass also reports 38 distinct; the figures coincide, but only the id-keyed one decomposes 25/13/0 and only it is sound.) Hevy fetch + `user_hevy_key` now live-verified (5 pages, all HTTP 200) — the invalid-key failure mode did not materialise. |
| `fix/hevy-template-sync-activation` | Activate the Hevy template sync at the OPERATOR layer: new `sync_hevy_templates.py` CLI (`--user-id` safety valve, non-zero exit on empty/partial), per-user error isolation + loud empty-list signal in `sync_exercise_templates`, empty-substrate precondition gate on the seeder (DECISIONS_LOG #77) | **DONE** — landed on master at `811ef6e`, pushed; local branch deleted (never pushed to origin). No migration. 94 backend tests green. | — (wiring/resilience only; the three template features #60/#61, #65, #74-#76 were inert on an unpopulated prod substrate) | **LOOP CLOSED 2026-07-14.** Both operations ran against Railway, both verified: (1) `sync_hevy_templates.py` → `hevy_exercise_templates` = **494 rows** (non-zero; the precondition gate the branch shipped is what made (2) meaningful rather than a silent 0-row no-op); (2) `seed_exercise_region_tags.py 1 --confirm` → **55/56 titles resolved**, 36 `exercise_region_tags` rows, 55 templates adjudicated, alembic head `c3a2d8e5f109`. The single unresolved title (`Bulgarian Split Squat`) was a reference-data defect, not a sync defect — fixed on `fix/exercise-tag-coverage` (DECISIONS_LOG #79). |
| `feat/tag-adjudication-three-state` | Three-state tag coverage (tagged / adjudicated-no-pattern / untagged) via `hevy_exercise_templates.adjudicated_at`; reject calf→ankle_df; corrected tag file moves four strength-lift families to `no_pattern` (DECISIONS_LOG #76, resolves Q26, opens Q27) | **DONE** — landed on master at `1bd4793`, pushed; local branch deleted (never pushed to origin). Migration `c3a2d8e5f109` (head); Railway auto-runs `alembic upgrade head` on deploy. 87 backend tests green. | — (feature INERT until seeded, as with #74) | **LOOP CLOSED 2026-07-14** — both checks verified ((1) and (2) below). (1) DONE: `seed_exercise_region_tags.py 1 --confirm` against Railway → 36 tag rows, **55 templates adjudicated** — the three-state substrate is live, so `adjudicated_at` now distinguishes no-pattern from never-looked in prod. (2) **DONE — G5 CONFIRMED IN PROD 2026-07-14** (operator run): 3× `sync_hevy_templates.py --user-id 1` (1,482 upserts) left `exercise_region_tags` at 36 rows / tag fingerprint `5ed53a9f018b13c5288a4ac6342a090e` unchanged, and `hevy_exercise_templates` at 55 adjudicated / 55 lateralised / meta fingerprint `d1979d0b8326de4bef7f790c30667062` unchanged. `_upsert_template` provably never assigns `laterality` or `adjudicated_at`. **Loop closed.** *Baseline caveat: those fingerprints are pre-BSS (36 tags / 55 adjudicated); prod is now 37/56 after the #79 re-seed, so they are a dated, SUPERSEDED baseline — not a reproducible current check. The result stands regardless: the clobber mechanism is indifferent to row count.* Q27 (v1 strength-ratio axis) remains a separate future design pass. |
| `feat/exercise-catalogue-taxonomy` | Replace the `_LOADED_KEYWORDS` substring matcher with a real taxonomy join: new `exercise_region_tags` table + `hevy_exercise_templates.laterality`; `infer_loaded_regions` rewritten to a table lookup with the keyword map demoted to an instrumented fallback (exercise-catalogue sequence step 1 of 4; DECISIONS_LOG #74/#75) | **DONE** — landed on master at `814890a`, pushed; local branch deleted (never pushed to origin). Migration `b2f1c9a4d7e8` (head); Railway auto-runs `alembic upgrade head` on deploy. 86 backend tests green. | — (feature code is INERT until tags are seeded — with no tag rows, `infer_loaded_regions` falls back to the keyword path = pre-land behaviour, so the land ships no behavioural change) | **LOOP CLOSED 2026-07-14** — (1), (2) and (3) all verified. (1) DONE: reference confirmed and seeded with `--confirm` (human adjudication stamped). (2) DONE: `seed_exercise_region_tags.py 1 --confirm` against Railway → 55/56 resolved, 36 tag rows over a 494-row substrate. The feature is **no longer inert** — tagged templates now take the authoritative join instead of the keyword fallback. (3) **DONE — G5 CONFIRMED IN PROD 2026-07-14** (operator run): 3× `sync_hevy_templates.py --user-id 1` (1,482 upserts) left tags and template meta byte-identical — `exercise_region_tags` 36 rows / fp `5ed53a9f018b13c5288a4ac6342a090e`; `hevy_exercise_templates` 55 adjudicated / 55 lateralised / fp `d1979d0b8326de4bef7f790c30667062`. `_upsert_template` provably never assigns `laterality` or `adjudicated_at`. The S1 table split holds under a real resync. *Fingerprints are pre-BSS (36/55) and thus a dated, SUPERSEDED baseline — prod is now 37/56 — but the mechanism is row-count-indifferent, so the result stands.* **Loop closed.** The separate question — whether the fallback stopped firing — was answered by `audit_exercise_tag_coverage.py` (DECISIONS_LOG #79): **hit-rate 0** in prod, same date. |
| `feat/constraint-consumption` | Injury constraints consumed, not decorative: AM soreness items ← active injury ledger; injury `trajectory` in JSON `value` + divergence / symptom-gated review surfacing in `get_readiness_snapshot`; `naive_baseline` soreness term = max across reported sites (constraint-consumption brief, Steps 1–4; DECISIONS_LOG #72/#73) | **OWED** — landed on master at `e70437b`. Open loop = browser-verify `CheckInAM.jsx` renders the injury-derived AM soreness items (logic is unit-verified only). Owner: Luke | — (backend + frontend, no migration, no external precondition) | Code side complete. **Railway seed DONE 2026-07-13** — `seed_engine` run against prod Postgres (service `health-app-DB`) via `DATABASE_PUBLIC_URL`: `injury ledger rows written: 2` (right hamstring + pes anserine, each with trajectory; the 3 pre-existing injuries skipped unchanged, add-only seed). Remaining (organic, not a code gap): trajectory divergence/review flags surface in `get_readiness_snapshot` only once (a) the backend redeploys with the Step-3 code and (b) an AM check-in records soreness under the derived keys. Frontend `CheckInAM.jsx` not browser-verified (logic unit-verified). Note: `get_readiness_snapshot` via the MCP connector appears to read a non-prod DB (it showed the new injuries before any prod seed succeeded) — verify prod directly with a Postgres query, not the tool. |
| `feat/hevy-exercise-template-resolver` | Hevy exercise-template synced store + title→id resolver (DECISIONS_LOG #60/#61) | **DONE** — ff-merged to `master` at `41a8998`, pushed; local branch deleted. Loop CLOSED on evidence: prod is 3 migrations past the resolver's own stamp. | — (prod-stamp precondition `217dce22fbc5` confirmed before land) | **Loop CLOSED — evidence, not expectation (adjudicated #90 sweep).** The loop watched for prod `alembic_version = 3497ab483935` — which IS this feature's own migration (`3497ab483935_add_hevy_exercise_templates`, the migration that CREATES the `hevy_exercise_templates` table). Repo chain: `3497ab483935` → `f4e1a2b3c6d7` → `b2f1c9a4d7e8` → `c3a2d8e5f109` (head), so the stamp is a direct ancestor of head, 3 steps back. Prod reached it: a master-recorded operator run (2026-07-14, `sync_hevy_templates.py`) shows `hevy_exercise_templates` = **494 rows** — a table `3497ab483935` creates, so its population is direct proof the migration applied — and reports alembic head `c3a2d8e5f109`. Prod is past `3497ab483935`; the stamp check is superseded. |
| `feat/hevy-create-loop` | `create_and_resolve` — app-originated custom exercise via create→sync→list-back (DECISIONS_LOG #65, resolves Q14) | **DONE** — landed on master at `e13e3a2`, pushed; local+remote branch deleted | — (backend-only, no migration, no external precondition) | Code side complete. `create_and_resolve` is a library function; resolver-activation wiring stays dormant (unchanged this session). |
| `feat/connector-error-policy` | Connector failures decouple from session auth (401→424, read handlers → 502, global 500 CORS guard) + "See all" true pagination (DECISIONS_LOG #66, #67) | **OWED** — code landed + deployed at `51c7091` (ff-merged, pushed; local branch deleted). Open loop = live "See all" E2E through the deployed backend against a valid Hevy connector key: an account with >1 page of workouts, confirm pagination reads past page 1. Owner: Luke. | — (backend + frontend, no migration, no external precondition) | **Stays OWED (adjudicated #90 sweep) — no evidence to close on.** git log on master shows only the landing commit `51c7091` and its BRANCHES governance — no later commit records a live See-all verification; the row's own history says NOT E2E-verified (connected key was invalid). The brief's expectation ("key replaced 12 Jul, See-all verified live") has no master/prod artifact behind it. A Hevy-MCP probe cannot vouch: it uses its own `HEVY_API_KEY`, not health-app's stored connector key, and does not exercise health-app's See-all endpoint. Worst case still degrades to page 1, guarded by the 502 helper. |
| `fix/hevy-exercise-history-path` | Correct `get_exercise_history` path to `/v1/exercise_history/{id}` (DECISIONS_LOG #69, resolves Q16) | **DONE** — landed on master at `2fc21e2`, pushed; local branch deleted (never pushed to origin) | — (backend-only, no migration, no external precondition) | Code side complete. Doc-evidence basis (official docs + 3 clients); live `exercise_history` corroboration optional belt-and-braces later (local Hevy MCP hung this session). Method currently unwired — zero call sites — so no downstream behaviour-shift. |
| `fix/hrv-sleep-integrity` | Ingest bounds guard + deep-sleep excluded from daily readiness (DECISIONS_LOG #70, #71 — HRV & Sleep Data Integrity brief Tasks 3, 4) | **DONE** — code landed at `dccca41` (ff-merged, pushed; local branch deleted). Task 3 Railway history sweep RUN 2026-08-16 against Railway Postgres: the full 15-field `_BOUNDS` `NOT BETWEEN` sweep over all 56 `samsung_hrv_readings` rows returned zero violators, so no backfill was required and no rows were written (DECISIONS_LOG #215, `OPEN_QUESTIONS` Q18). | — (backend-only, no migration, no external precondition) | **Was OWED (adjudicated #90 sweep); CLOSED 2026-08-16.** The bounds-guard + deep-sleep-exclusion code was already DONE on master; the Task 3 historical sweep was outstanding only because Railway is operator-only and no session had reached it. It has now been run — clean, zero writes. Both fixes are independent of the HRV-metric question (Q17, resolved → #89). |
| `chore/markitdown-mcp` | Adopt MarkItDown as the document→markdown ingestion path: `markitdown` MCP at **user scope** (`uvx markitdown-mcp`) + `markitdown[pdf,docx,pptx,xlsx,xls]==0.1.6` CLI; CLAUDE.md repo-canonical Tooling section + DECISIONS_LOG #78 (deterministic + cheap on born-digital prose; table structure flattens, scanned/broken-font → cid garbage — native-vision fallback recorded) | **DONE 2026-07-14** — ff-merged to `master` at `de38820`, pushed; local branch deleted. Machine-local tooling only, no repo code, no migration. Nothing outstanding: the durable Claude Desktop registration was declined as low-value (it only buys the weaker in-context path), so no open loop remains. | Machine-local: the MCP registration + CLI install do not replicate across machines — re-run the setup on any new machine. Durable Claude Desktop registration declined as low-value; if ever wanted, quit Desktop FIRST then edit `claude_desktop_config.json` (the running app rewrites it and drops edits), or Settings → Developer → Edit Config + restart. | — (complete; no open loop). |
| `chore/process-notes` | Session process notes off the #78 work: FEEDBACK.md #9 (Bash tool is Git Bash/POSIX — use `<<'EOF'`, never PowerShell `@'...'@`) + CLAUDE.md Tooling note loud-vs-silent axis appended to the MarkItDown Limits (refines DECISIONS_LOG #78, does not edit it) | **DONE** — landed on master at `cd87e64`, pushed; local branch deleted (never pushed to origin). Docs only, no repo code, no migration, DECISIONS_LOG untouched. | — (governance notes only; no code precondition) | — (complete). |
| `feat/recovery-metrics-rhr` | Expose RHR as a series in `get_recovery_metrics` (HRV & Sleep Data Integrity brief Task 2) | **DONE** — merge-landed to master (own merge commit), pushed; local + remote branch deleted. The recorded UNSTARTED was a mislabel: `a4e1887` is a COMPLETE feature — it adds `RHR={rhr}` to the `get_recovery_metrics` per-day series line (`mcp_server.py`), the one thing master's function did not surface (master fetched `resting_heart_rate AS sleep_hr_bpm` but emitted it only in a different function). Clean merge, no code conflict, no migration, no prod write. Suite **412 passed**. Its former blocker (the Q17 (A)/(B) gate) had already resolved (→ #89). | Prior rationale (scraper RHR shares HRV's *device family*) is superseded by a stronger one: `last_shr`/`sleep_hr_bpm` was **itself phantom-affected** — HCA #19 fixed it in the same commit as HRV — so scraper RHR is not a clean (A)/(B) discriminator either; it moved for the same instrumentation reason. Health Connect `resting_heart_rate` (`health_connect_syncs`) remains the only independent path. | — (gate resolved; pick up as a standalone RHR-series feature whenever wanted. DECISIONS entry numbered at merge.) |
| `feat/cbti-module` | CBT-I data substrate (phase 1, brief Steps 1–4): migration `e5f2a9c7b104` adds 9 nullable AM-moment sleep-diary columns to `daily_records` + two append-only ledgers (`cbti_blocks`, `cbti_prescriptions`, append-only as a model+application invariant with `ck_cbti_prescription_decision` the one DB-enforced constraint); `import_cbti_block.py` loads the completed 2026-03-19→05-13 block (VA CBT-I Sleep Diary Calculator export) to Railway with ±0.001 SE reconciliation + negative control. DECISIONS_LOG #107/#108/#109, OPEN_QUESTIONS Q42. | **DONE** — ff-merged to master at **`d97c992`**, pushed; local + remote branch deleted; recovery ref `backup/cbti-pre-rebase` deleted after Gate 6. 6 commits (`6b43860`→`d97c992`), rebased onto `origin/master` mid-flight after master advanced 8 commits (pre-rebase range `f0899eb`→`5fb625d`). Backend suite **275 passed** (master's 258 + 17: 10 substrate, 7 import). Migration verified up/down/up on SQLite in isolation, applied to Railway, and `alembic heads` confirmed a single head after the rebase (master added no migration, so `c3a2d8e5f109` remained the sole predecessor). Import verified against the DB: 1 block / 9 prescriptions / 53 daily_records, DB-stored `diary_se_pct` reconciles 0/53, exit aggregates (TST 428m / SE 89.6%) reproduce #107's decline figures. **Prod/master divergence CLOSED at Gate 6:** `railway run --service health-app-DB` → `alembic current` = `e5f2a9c7b104`, and that revision now exists on master. Owner: — (complete). | — (no external precondition to land; held for review per #98 push-while-holding). | **PROD SCHEMA AHEAD OF MASTER — RESOLVED.** For the branch's lifetime the migration was applied to Railway while the migration file existed only on the branch; the ff-merge at `d97c992` reconciled it, confirmed at Gate 6. **Renumber debt — RESOLVED at the rebase (historical):** the branch was rebased onto `origin/master` after master took #104/#105/#106 (safety-threshold gate) and Q41 (haematocrit citation capture). The CBT-I entries were renumbered by anchored pattern to **#107/#108/#109** and **Q42**, and the brief-provisional `#101`/`#102` left dangling in `models.py` / `import_cbti_block.py` were corrected to #107/#108 in the same pass. Master max at rebase: **#106 / Q41**. If `feat/feedback-ledger` (#85–88) or `feat/checkin-injury-probe` (#89–90) ff-merge before this one, these renumber again above them. **Phase 2 (brief Steps 5–7) is a separate future brief:** titration engine, AM/PM surfaces + the 12h-clock prefill gate (Q42), ISI capture — none started. |
| `feat/cbti-engine` | CBT-I phase 2 (titration engine + surfaces). Landed: `backend/cbti/timeutil.py` (the single definition of the conditional midnight wrap), `backend/cbti/engine.py` (pure titration engine), `backend/cbti/replay.py` (DB adapter + Gate-4 replay), `daily_records.got_into_bed`, and `cbti_prescriptions` basis-provenance columns. Brief Steps 1-4 of 5. DECISIONS_LOG #114/#115. | **DONE** — ff-merged to master at **`ee9aa1f`**, pushed; local + remote branch deleted (0 unmerged commits by patch-id). 6 commits `8ad304e`->`ee9aa1f`. Backend suite **352 passed** (was 275 at branch cut). Migrations `a7b3f1c8d240` + `c4e8a2019bd7`, single head. **Prod reconciled at merge:** Railway's `upgrade head` ran on deploy and `railway ssh` -> `alembic current` returns `c4e8a2019bd7`, matching master — no divergence in either direction, unlike phase 1. #114/#115 claimed at merge (master max was #113); Q45 was already on master. | — (complete). | **Step 5 (surfaces) NOT STARTED** — a separate brief and the first work in this sequence to touch `frontend/`. The engine ships **DORMANT**: no router or service imports `cbti/`, so nothing changes behaviourally until a surface is wired to it. Three constants ship recorded-as-unvalidated (#114): `MAX_MOVE_MIN` bound 0 of 8, `PLATEAU_TOL_MIN` never reached, `MIN_VALID_NIGHTS` undeterminable at n=3/3/2. |
| `feat/cbti-surfaces` | CBT-I phase 2 capture surfaces + block 3 opened live. Block status on `/prefill`+`/today`, Samsung clock-position prefill with a 4h sanity gate, AM diary capture with `diary_se_pct`/`diary_tst_min` frozen at write, waking-cause columns, `CheckInAM.jsx` section, and `open_cbti_block3.py`. DECISIONS_LOG #117/#118/#119. | **DONE** — ff-merged to master at **`2f9004e`**, pushed; local + remote branch deleted (`git cherry` clean). Deployed and verified after settle (#116): deployment `63523eb4` SUCCESS, `alembic current` = `b2d5f9e04a17` with the migration file present in the image (the discriminating probe), deployed `checkin_v2.py` carries `diary_prefill`/`_freeze_diary`. **Block 3 opened live in prod**: `cbti_blocks` id=2 (open, anchor 05:45), prescription id=10 (`adopt`, 23:45→05:45, window 360, device-derived basis tst=349/se=89.3 over 27 nights). Freeze formula validated 12/12 against block 2's stored rows before shipping. Backend suite **386 passed**. #117/#118/#119 minted at merge (master max was #116). Owner: — (complete). | — (complete). | **REMAINING CBT-I SURFACE WORK — a NEW brief, not this branch (deleted):** the reorder brief deliberately scoped only open→AM-capture→render→merge. NOT built: **PM prescribed-lights-out display + nap capture** (`/today` returns the prescription; `NightlyCloseOut.jsx` must show it and accept `naps_min`), and **the manual witnessed evaluation trigger** (#118's PM-offer half — offer evaluation on PM close-out once ≥7 days since `effective_from`; the block-open half is DONE). **ISI capture** remains owed and BLOCKS a clean block-4 (a block-open ISI cannot be retrofitted; block 3 already opened without it). Q46 (device-vs-diary basis provenance) and Q47 (adherence Samsung-bedtime lag) are filed. |
| `feat/cbti-isi-pm` | Give the ISI a home + surface the prescription at night. `cbti_isi` table (items + reported total, canonical total derived), block 3's baseline ISI backfilled to prod, PM prescribed-window display in `NightlyCloseOut.jsx`, and correction of the false "opened without ISI" claim in closeout/ROADMAP. DECISIONS_LOG #120. | **DONE** — ff-merged to master at **`9331c31`**, pushed; local branch deleted (`git cherry` clean; never pushed to origin — the work reached origin via the master ff-merge, so nothing to prune). Deployed + verified after settle (#116): deployment `c85518dc` SUCCESS, `alembic current` = `d3f7a1908c62`, `cbti_isi` table present (the discriminating probe). Baseline ISI written and read back: `cbti_isi` id=1, block 2, items `[0,3,2,3,2,2,3]`, canonical 15 / reported 16, administered 2026-07-24 09:10 UTC (19:10 AEST), exactly one baseline row (ABORT guard confirmed). Migration verified up/down/up in isolation on SQLite before merge. Suite **386 passed**. #120 minted at merge (master max was #119). Owner: — (complete). | — (complete). | **Nightly loop now displays but does not yet CLOSE in-app.** Remaining (a new brief, from master): the **manual evaluation trigger** (#118's PM-offer half — cannot fire before ~31 Jul, needs a full cycle) and PM **`naps_min`** capture. The ISI has no read endpoint yet — a UI for future administrations waits for the exit ISI, which lands with the trigger. |
| `feat/cbti-pm-naps` | Capture `naps_min` at PM so the engine's nap exclusion stops being structurally dead for block 3. `NightlyCloseOutIn`/`submit_pm`/`TodayOut` + a `NightlyCloseOut.jsx` field gated on `block_open`; blank→0 (never null) client-side so the `is not None` guard at `engine.py:212` can fire. DECISIONS_LOG #122. | **DONE** — ff-merged to master at **`802ddd6`**, pushed; local branch deleted (`git cherry` clean; never pushed to origin — work reached origin via the master ff-merge). No migration, no prod write. **Both changed services verified per #121** (its first application): backend OpenAPI carries `naps_min` on `NightlyCloseOutIn` + `TodayOut` (control field absent); frontend served bundle `index-DxJndbcD.js` carries "Naps today (minutes)" / "Any nap excludes tonight" (control string absent). Both deploys settled SUCCESS. Suite **394 passed** (was 386). #122 minted at merge (max was #121). Owner: — (complete). | — (complete). | **Only the manual evaluation trigger remains** to close the in-app titration loop (#118's PM-offer half) — a new brief from master, not before ~31 Jul (needs a full cycle). **Carried:** block 3's 24–25 Jul nights keep `naps_min = NULL` (pre-capture), un-gateable for naps without a memory backfill — two nights, manual note. |
| `feat/daily-notes` | Free-text `am_notes` / `pm_notes` on `daily_records`, captured through both check-in surfaces; observational, not block-gated. Two columns not one (independent AM/PM submits mustn't clobber). DECISIONS_LOG #125. | **DONE** — ff-merged to master at **`a4bfd39`**, pushed; local branch deleted (`git cherry` clean; never pushed to origin — reached origin via the master ff-merge). Migration `f1a4c7e29b83` chained off the real head `d3f7a1908c62` (Step-A VERIFY caught the brief's stale `b2d5f9e04a17`), verified up/down/up in isolation on SQLite via `create_all`, applied on prod (`alembic current = f1a4c7e29b83`, both columns present per #116). **Both services verified per #121:** backend OpenAPI carries `am_notes`/`pm_notes` on AMCheckInIn/NightlyCloseOutIn/TodayOut; frontend bundle carries "Notes (optional)"/"This morning:"; negative control on each. Suite **406 passed** (was 401). #125 minted at merge (max was #124). Owner: — (complete). | — (complete). | Only the manual evaluation trigger remains to close the in-app titration loop (~31 Jul). Excluded to the next brief (per this brief's GUARD): the external-disruption boolean and the `n_alcohol_unknown` retirement — both touch `classify_night` with open VERIFYs. |
| `fix/cbti-replay-effective-rx` | Q49 — the replay adjudicates each cycle against the EFFECTIVE prescription read from `cbti_prescriptions` (window, lights-out, anchor), not a chain regenerated from the engine's own decisions; cycles anchor to each prescription's `effective_from` and never span a boundary, so a mid-cycle correction is adjudicated against, not false-held. Reworked `replay.py` + prescription SELECT carries `effective_to`/`wake_anchor`; new `test_cbti_replay.py` (module had none). DECISIONS_LOG #128, resolves Q49. | **DONE** — ff-merged to master, pushed; local + remote branch deleted. No migration, no prod write (replay is read-only). Suite **412 passed** (was 406, +6 replay tests). Verified read-only against prod block 1 (9 cycles, one per ledger prescription) and block 3 (id=10 stub vs id=11 correction). #128 minted at merge (master max was #127). Owner: — (complete). | — (no external precondition to land). | **The live evaluation trigger (#118, unbuilt) MUST reuse this same effective-prescription read** — the shared "≥7 nights since effective_from" model — or the two evaluation paths diverge again. That is the only open follow-on. |
| `gov/feedback-renumber-debt` | FEEDBACK §20 — hardcoded governance numbers on held branches accrue renumber debt; records the pattern with the 5-branch landing session as evidence (one `#NEXT`-compliant branch vs two hardcoded, cost scaling with intervening merges not age) and points at the documented `#NEXT` rule (CLAUDE.md, shared block) as a compliance failure. ROADMAP NOW gains a cross-repo debt row for the shared-block scope extension. | **DONE** — ff-merged to master (own commit), pushed; local + remote branch deleted. Governance-only (FEEDBACK.md + ROADMAP.md + this row); no code, no CLAUDE.md, no DECISIONS_LOG, no migration. Section number `§20` resolved at write time (landed immediately, not held — the rule it documents applies to held branches). Suite unchanged (**445**). | — (no external precondition to land). | Shared-block scope extension owed as cross-repo debt (ROADMAP NOW) — do not write it to CLAUDE.md from a single-repo session (G1). |
| `cbti/engine-comment-neutralisation` | S1: strip block-2 measurements cited as design justification from `cbti/engine.py` comments + module docstring, keeping first-principles reasoning or marking `not attested — prior block discarded`; markers (NOT implemented) for two pending-policy v2 items — alcohol excusable-vs-disqualifying and the `incomplete` predicate off SE. S2: delete the confirmed-no-op `--admit-unknown-alcohol` flag in `replay.py`. S3: OPEN_QUESTIONS Q55 records four gate constants (`NAP_EXCLUDE_MIN`/`TRAINING_RECOVERY_MIN`/`ADHERENCE_TOL_MIN`/`ADHERENCE_FAIL_N`) as chosen-not-derived. | **DONE** — ff-merged to master, pushed; local + remote branch deleted. Comments-only + one no-op deletion → **no behaviour change**; suite **445 unchanged** (no test exercised the deleted flag). `replay.py` needed no S1 edit (the Q49 rework already neutralised its old `nights_since_effective_from` note). No DECISIONS_LOG entry (governance action, not a design decision). Q55 authored against the `Q-NEXT` placeholder (§20) and resolved at merge — branch not held. | — (no external precondition to land). | Pending v2 items (exclusion-class split, `incomplete` redefinition off SE) remain MARKED, not built — they depend on an unratified policy revision. `MIN_VALID_NIGHTS`/`MAX_MOVE_MIN`/`PLATEAU_TOL_MIN` left as correct-epistemic-practice. |
| `gov/block2-discard-scope` | Mint the block-1 (`cbti_blocks.id=1`, clinical "block 2") discard for OUTCOME CLAIMS as a canonical DECISIONS entry (#136), with scope stated: void = TST/SE/titration/adherence/exclusion/cycle outcomes; survives = instrument characterisation, parameter-setting from unforced periods, and #107's week 7. Governance-only — the premise was previously only in chat, yet landed code (`d07b538`) and Q55 already lean on it. | **DONE** — ff-merged to master, pushed; local + remote branch deleted. No code. Suite unchanged (**445**). VERIFY (1a): not previously minted — only *referenced* by Q55 and `d07b538` comments. VERIFY (1b): the buffer decision is **#115** (the brief's "#112" mis-resolves; #112 is cross-repo-debt), and #115 + #114 cite block-1 outcomes that `d07b538` stripped from code → store/code drift **REPORTED, not resolved** (superseding a locked decision is a separate call). `#NEXT` resolved to #136 at merge. | — (no external precondition to land). | **Owed, deferred:** reconciling #115's buffer derivation and #114's regularity / adherence-arm rejections against this discard — each a separate supersession, not this brief. #124 needs none (self-labels its block-1 figures "not evidence, confounded"). |
| `gov/supersede-block1-derived-bases` | Supersede the recorded BASES of #115 (buffer +30) and #114 (regularity-not-gating + rejected 2nd adherence arm) — void under #136 (block-1 discard) — in one entry (#137). Forward-reference only (append-only: #114/#115 NOT edited in place, per CLAUDE.md + the #33/#30 precedents). No constant, no code change: aligns the store to the code already landed by `d07b538`. Closes #136's deferred clause. | **DONE** — ff-merged to master, pushed; local + remote branch deleted. Governance-only, **no `.py`** (verified `git diff --name-only` has zero `.py`); suite unchanged (**445**). Buffer restated to ONE support (SRT convention; the +36 block-1 derivation void — it measured window-vs-actual on nights the window was not run). Rejected gates restated **NOT ATTESTED, not refuted** — omitting a gate needs no evidence, so both stay unbuilt with no case for building. #114's constants portion (`MAX_MOVE_MIN`/`PLATEAU_TOL_MIN`/`MIN_VALID_NIGHTS` unvalidated) untouched. Step-3 confirmed the engine comments already match. `#NEXT` resolved to #137 at merge. | — (no external precondition to land). | Buffer is LIVE, not settled — re-derivation belongs to the pending policy revision, NOT here (GUARD: do not re-derive; the TIB↔TST servo problem is noted in the entry). |
| `gov/interpretation-sequence` | Record the interpretation-layer build sequence in `ROADMAP.md` as a NOW sub-block: the seven increments in execution order (1 / 4a / declared-state / 4b / 2 / 3 / 5) with four-state status tags, 4b's two blockers (safety-band citation capture, ingestion), and the corrected **three-runtime-gate** model (two-armed, non-demotable-safety-arm gate 1 — not v0.4's two-gate framing). Pointer added to the existing "Interpretation layer build" NOW row; no row deleted. Records existing state — **no new decision**. | **DONE** — ff-merged to master at `5a2a62e`, pushed; local + remote branch deleted (`git cherry` clean — one `+` commit, now on master). Governance-only: `git diff --name-only` = `ROADMAP.md` **only** (zero `.py`/`.json`/`DECISIONS_LOG`), plus this `BRANCHES.md` row. Suite **445 passed**, unchanged. **V1 gate facts re-verified against `backend/interpretation/gates.py` at read time** (three gate fns `news_gate`/`range_gate`/`safety_gate` + `delta`; gate-1 safety arm forces `is_news` unconditionally, no demotion path; surfacing field is `should_surface`, not `is_moved`; `safety_thresholds.json` `thresholds:{}` → `no_asset` for every marker) — all four match the brief, so the block states the code. | — (no external precondition to land). | Landed `5a2a62e` — **zero renumber debt** (carries no decision / question / section number, so it was safe to hold and pointless to; a clean instance of FEEDBACK §20's actual claim — hardcoding is the cost driver, not holding). **Two mintings deliberately NOT folded in** (they belong to the v0.5 minting brief, minting here would double-record): (1) **contract v0.5** superseding v0.4's two-gate model with three gates + member-level safety field + group-level surfacing field + `ungrouped[]` with its own render section; (2) the ruling that **ungrouped markers render in their own section**, surfacing-first, between What Moved and Stable. |
| `gov/interpretation-contract-v05` | Mint interpretation output contract **v0.5** as a `DECISIONS_LOG` entry (`### #NEXT`): the three-gate supersession of v0.4's two-gate safety model (gate 1 two-armed with a non-demotable safety arm, gate 2 range never-suppressed, gate 3 level-vs-band), the three producer keys v0.4 lacked (`safety_gate` per row, `should_surface` per group, top-level `ungrouped[]`), and the ruling that **ungrouped markers render in their own section** between What Moved and Stable. The contract is UI-maintained outside both repos, so the entry carries its own substance rather than pointing at the file. Governance-only. | **DONE** — ff-merged to master at `0d55761`, pushed; local + remote branch deleted (`git cherry` clean). `#NEXT` resolved to **#138** at merge (master max was #137). Governance-only: `git diff --name-only` = `DECISIONS_LOG.md` + this `BRANCHES.md` row **only** (zero `.py`/`.json`/`ROADMAP.md`/`CLAUDE.md`). Suite **445 passed**, unchanged. **V1 re-verified at read time** against `backend/interpretation/gates.py` + `producer.py` (three gate fns; gate-1 safety arm forces `is_news` with no demotion path; `should_surface` not `is_moved`; producer emits top-level `ungrouped[]` + `safety_gate` per row) — all four match the brief. **V2:** entry stands alone; the only clause not establishable from master was the draft's invariant ordinals **I8/I9** — master fixes the contract invariant series only at **I1** and **I6**, so next-free is unconfirmable — so the two invariants (safety-arm non-demotability; no-present-marker-dropped) are recorded **by content, ordinal omitted**, per "resolve or omit". | — (no external precondition to land). | Landed `0d55761` as **#138**. **Verification-gap fork left OPEN in the entry, deliberately not decided:** commit the contract file to the repo, or accept the generated fixture as the operative contract and treat the document as design narrative. Downstream: the 4b spec drafts off v0.5's §3/§5/§6. |
| `feat/safety-bands-haematocrit` | Populate `safety_thresholds.json` with three **cited** haematocrit bands — `watch` 0.50 (monitoring target ceiling), `elevated` 0.52 (observed risk inflection, first-year scope), `high` 0.54 (intervention) — moving haematocrit from `_deferred` into live `thresholds` and **gate 3 from inert to live** for the marker it was designed around. Each band carries its OWN `evidence_refs` (no shared citation — I1). Reference asset + its schema/gate test only; no producer or gate logic. | **DONE** — ff-merged to master at `babd9e6`, pushed; local + remote branch deleted (`git cherry` clean, two `+` commits). `#NEXT` resolved to **#139** at merge (master max was #138). **V1** verified the unit convention: `haematocrit.unit_established` is null, so `safety_gate` does not normalise — it plausibility-gates `[0.20,0.70]` and compares raw; bands stored as **fractions** (0.50/0.52/0.54) matching the lab's fraction results (0.47) — **no mismatch, no stop**. **V2** verified the live-band shape from `_schema` (ordered array; band keys `band_key`/`value`/`grade`/`evidence_refs`/`note`; highest-breached wins). Asset re-verified **pure ASCII, 0 literal em dashes, parses** (reference-JSON edit guard). Diff = `safety_thresholds.json` + `test_safety_thresholds_schema.py` + `DECISIONS_LOG.md` + this `BRANCHES.md` row — **no `gates.py`, no `producer.py`**. Suite **448 passed** (was 445, +3: three broken empty-asset assertions rewritten net-zero, +3 S2 gate-firing tests). | — (no external precondition to land). | Landed `babd9e6` as **#139**. Gate 3 is now live but **fires on nothing in production** until a haematocrit result is ingested (lab store empty per #138 build sequence). Gate 1's safety arm is now **reachable** — it fires on `first_observation_in_band`, so it does not wait for a prior. No band authored for any other marker (haematocrit only, per brief). |
| `fix/labs-confirm-persistence` | Investigate reported live data loss on lab confirm: ten `lab_reports` rows carrying zero `lab_results`, and a PSA re-upload that produced a second empty report. Brief hypothesised that `#156`'s duplicate guard could not fire on a zero-row write and that a displayed result was failing to reach the database. | **DONE** — ff-merged to master at `7ff5d26`, pushed; local + remote branch deleted (`git cherry origin/master` clean). `#NEXT` resolved to **#157**, `Q-NEXT` to **Q68** at merge (master max was #156 / Q67). | **The brief's causal hypothesis is FALSIFIED and the entry says so.** `#156` did not fail to fire — it fired correctly and *caused* the zero-row writes by design: all ten empty reports are re-uploads in which every submitted marker already existed at that `collected_date`, so every row was skipped, `result_count` returned 0, and the envelope was still created per `#155` retain-raw. **No data was lost.** Live DB at investigation: 43 reports, 168 results, **zero** duplicated `(user, marker, date)` groups, 66 distinct canonical markers against a 66-entry map. What actually failed: `#156` closes by recording that `result_count` reports rows written *"so a skipped collision is visible to the caller"* — and the caller (`Metrics.jsx`) discarded the response and rendered an unconditional `"Report saved"`. A genuine loss was found **separately**: the skip set was keyed by MARKER and the write loop tested membership by that key, so an intra-batch repeat suppressed every row carrying it, including the first, which had nothing to collide with — now decided per ROW INDEX. Latent today (no canonical id has >1 raw synonym), armed by any map edit. Persistence tests read `lab_results` back directly; **non-vacuity on the record: 2 failed / 6 passed pre-fix, 8 passed post-fix**. Suite **521** (was 513). Deploy verified per `#121` on BOTH services with discriminating probes: backend by introspecting the LIVE imported handler (`skip_indices` present, `skip_keys` absent), frontend by served-bundle grep (three new string literals present, the removed `"Report saved"` absent). | — (no external precondition to land). | Landed `7ff5d26` as **#157** + **Q68**. **Operator decision owed:** ten zero-result and eight duplicate `LabReport` rows reported, **not deleted** (`#155` retention; deletion is the operator's call). **Uploading can resume** — it was paused on the belief that reports were silently dropping results, which they were not. Q68 records the undecided fork this exposes: whether a full-collision re-upload should create an empty envelope at all. |
| `feat/labs-upload-history` | Move collision detection to the moment the operator is deciding, and stop a correctly-declined re-upload from living in "Your Results". **S1** confirm-screen collision pre-check; **S2** upload history (flat manifest of every ingested report); **S3** declined reports leave the results list. | **DONE** — ff-merged to master at `d65e98e`, pushed; local + remote branch deleted (`git cherry origin/master` clean). `#NEXT` resolved to **#158** at merge (master max was #157). | **Pre-check landed FRONTEND — no endpoint, verified before building.** `GET /labs/results` already returns every stored report with `collected_date`, `marker_canonical`, `marker_name_raw`, and `GET /labs/canonical-map` resolves a raw label exactly as the server does; both are already fetched on mount. **G2 PASS by hash, not assertion:** `#156`'s two guard regions are byte-identical to the pre-change baseline (detect `fb9e9cbf84a535c5`, write-loop skip `c478d7313ea5c50f`). **SCOPE ADDITION (reported, not absorbed):** the store could not distinguish a decline from a fault, and the gap was deeper than a missing field — an unparseable document could not reach the store **at all**, because `results=[]` tripped `assert row_confidences`, which raised before `db.commit()` (HTTP 500, no row). So the fault case had no representation to be confused with. Added `lab_reports.zero_row_reason` (migration `d7c4b1a90e35`: NULL / `all_markers_declined` / `no_values_extracted`) and turned the assert into a recorded event. That finding also makes the backfill a **proof**: the fault value was unreachable, so no legacy row can carry it. **DIVERGENCE from the brief:** it said not to reword the zero-row red panel, but the panel must survive for faults and its copy attributed emptiness to *a repeat* — left alone it would tell the operator an unreadable chart PDF was a duplicate. Fault copy now keys on `zero_row_reason`. **G1/G4/G5 verified by driving the REAL page** with a stubbed axios adapter: pre-check states the collision before any write and names the earlier report; **cancel issued ZERO requests**; the declined report is absent from results and present in history; the unparseable one is present in both. Suite **527** (was 521). Deploy verified per `#121` on both services — backend by AST introspection of the live handler (a substring grep for `assert row_confidences` returned a **#113 false positive** off the comment explaining its removal), frontend by served-bundle grep (6 new literals present, the removed decline copy absent). Live DB after migration: `alembic=d7c4b1a90e35`, all **10** zero-row reports backfilled to `all_markers_declined`, 33 with rows carry NULL, **0** mislabelled, total still 43 — none deleted. | — (no external precondition to land). | Landed `d65e98e` as **#158**. `#157`'s owed operator decision on the ten junk rows is **DISSOLVED, not deferred**: they are reclassified as upload events, `#155` retention holds, no deletion needed. New OWED surfaced in `ROADMAP` NEXT: **SCHEMA.md is stale for the entire lab family** and no lab migration has ever updated it. |
| `governance/q-temporal-bound` | Mint the last 1b Step 0 design finding that lived only in a chat transcript: `marker_series` has no temporal bound, so the interpretation output is a composite of draws rather than a reading of one. Governance only, no code. | **DONE** — ff-merged to master at `28f9564`, pushed; local + remote branch deleted (`git cherry origin/master` clean). `Q-NEXT` resolved to **Q69** at merge (anchored `^## Q[0-9]+\.` gave max Q68, 68 entries for a max of 68 — contiguous — and no letter-suffixed headings for the anchored grep to miss). | **Verified by running `marker_series(1, db)` against live data, not by restating the inventory.** Confirmed: the only filter is `user_id` (`labs_reads.py:131-156`); **39 of 66 markers** carry a current value from a draw other than the newest, across five dates (2025-05-16 → 2026-05-30); the hepatocellular group renders off 2026-03-06 (85d stale) with a 2025-12-27 comparison; `min_meaningful_delta` has no time dimension in any of its 8 authored entries or the fallback. **TWO DRAFT CLAIMS FALSIFIED and recorded as such:** (1) albumin is NOT an operand of `haemoconcentration_discriminator` — it is a separate `discriminator` key that **no code reads**, so its age cannot affect `operand_status`; that gap is already documented in `gates.py:351-353` and is already **Q65**, a different question. (2) **No relation today has cross-draw operands** — all ten resolve with a 0-day spread, so the hazard is latent, not observed. Also corrected: candidate (c) is a VIEW change, not a producer change — the producer already emits `members[].current.collected`, `.prior.collected` and `meta.trigger_panel.collected`. `Lab ID` is not merely uncaptured but **extracted then discarded** (`confirm_lab_report` never reads `report.patient`); its coverage on pre-2026 documents is **UNVERIFIED** (no source PDFs in the repo) and the entry says so rather than implying the key is universally available. Suite **527**, unchanged — governance only. | — (no external precondition to land). | Landed `28f9564` as **Q69**. Blocks wiring the interpretation view to live data (1b). Recommendation was deliberately held out of the entry by the brief. |
| `feat/1b-delivery` | 1b delivery: producer read over real series, floor descriptor, fixture, view, endpoint, reachable link. | **DONE - Steps 0-4 landed; Step 5 deferred by design (see `feat/1b-step5-wiring`, since merged).** Steps 0-4 ff-merged to master at `bf4b5f8`, pushed; local + remote branch deleted (`git cherry origin/master` clean). No DECISIONS integer claimed; `Q-NEXT` resolved to **Q70** at merge (master max was Q69). | **Step 5 NOT built.** Q69 as committed reads *"Blocks: wiring the interpretation view to live data"* and *"Resolve before: the interpretation view is wired to live data"*. Candidate (c) was implemented (per-marker collection dates), but Q69's own text calls (c) *"the mitigation that holds whichever of (a), (b) or (d) is chosen"* - so it does not decide the input rule, and the clause asks for resolution, not mitigation. `#150` verified NOT to forbid interim links, so Q69 is the sole blocker. **No DECISIONS entry:** VERIFY 1.2 found `#154` already establishes the interim content (*"axis_verdict emits the invariant per-group floor sentence in the interim"*), so the brief's conditional entry would have duplicated it. **Step 0 findings:** all three out-of-range markers (`ast` 47H, `alt` 53H, `bilirubin_total` 28H) come from 2026-03-06, 85 days before the trigger draw; gate 3 fires on nothing (no marker in an authored band); 51 of 66 markers are ungrouped; `is_derived` is false on all 168 rows. Surfaced **Q70** (censored deltas emit `delta_within_min_meaningful` without consulting the threshold - `lh` 1.0->`<0.1` reads as within noise). **Q65 pointer:** neither stale nor mis-scoped - Q65 carries TWO contradictory status lines (`State: open` header, `Status: DONE -> #154` footer), violating one-status-per-item. Reported, not actioned, per the brief. **Step 3 list DERIVED, not copied:** the brief's 4 items all real, 3 more found (`range_gate.note`, `rel.partner`, `safety_gate` rendered nowhere). Suite **539** (was 527). Deploy verified per `#121` on both services; the first frontend probe hit a CACHED index.html and was caught by its negative control (#116 timing), the cache-busted re-probe passed 3 positive + 4 negative. | Q69 resolution - choose between (a), (b) and (d) (owner: Luke). | Landed `bf4b5f8` as **Q70**. Remaining 1b work is ONE commit: point the view at `GET /interpretation` and add the dashboard link. |
| `governance/q69-resolution` | Resolve `Q69` by amending its candidate set and choosing (e) - partition the output by provenance; split `min_meaningful_delta`'s time-blindness into its own question. Governance only, no code. | **DONE** - ff-merged to master at `7be0891`, pushed; local + remote branch deleted (`git cherry origin/master` clean). `#NEXT` -> **#159**, `Q-NEXT` -> **Q71** at merge (master max was #158 / Q70, both contiguous). | **GUARD's predicted correction landed:** (e)'s relation qualifier does NOT need a fourth state in `#154`, and adding one would be a CATEGORY ERROR - `excluded`/`not_excluded`/`not_assessed` are BRANCH resolutions, whereas operand provenance is a property of the inputs whose peer is `operand_status`, outside the branch model. `#154` NOT amended; its own `#47` coverage clause already licenses the statement. **Group date coherence verified a property of the DATA, not the code:** `_assemble_members` pulls each member independently with no date constraint; two partial-group draws have already occurred (`hpg_axis` 3/6 at 2026-01-07 and 3/6 at 2026-04-20); and that group's PRIOR side already spans three dates (2026-04-20 / 2026-01-07 / 2025-12-27), so rule 2 is already firing. **Q70 verified NOT to cover interval sensitivity** (a comparison that never happened vs one that did over an unstated span), so Q71 was minted rather than Q70 extended. **Numbering trap caught:** the anchored `^### [0-9]+\.` grep UNDERCOUNTS DECISIONS_LOG by three - entries 126/127/128 lack the trailing period, so a period-anchored sweep reports 155 for a max of 158 and invents three phantom gaps (#113 shape). Counted period-agnostically. Only the heading placeholder was substituted in DECISIONS_LOG; seven other `#NEXT` occurrences are pre-existing prose ABOUT the convention. Suite **539**, unchanged - governance only. | - (no external precondition to land). | Landed `7be0891` as **#159** + **Q71**; `Q69` now `DONE -> #159`. Wiring bar for 1b Step 5: member-level dates (built, `#158`) AND group-level as-of rendered. |
| `feat/1b-step5-wiring` | 1b Step 5: group-level as-of, point the view at `GET /interpretation`, dashboard link. | **DONE** - ff-merged at `edb8f13`, pushed; local + remote deleted (`git cherry` clean). | **VERIFY 1.1: `#159` does NOT fix the as-of derivation**, so the conditional LOG entry applied -> **#160** (current side only; the prior side is `Q71`). **VERIFY 1.2 corrected the brief:** `#159`'s committed text is *"labelled rather than merged"*, NOT *"labelled and separated"* - so an in-place label is exactly compliant, not a minimum standing in for sectioning. **G2 shape parity BEFORE the switch:** 157 deployed-endpoint key paths vs 138 fixture, ZERO structural differences - every delta is the new `as_of` or a data-dependent optional key in one direction or the other. A null-audit over the live payload (15 grouped members, 51 ungrouped, 7 levers) found no field the view hard-reads is null. **G3:** loading / empty / error all exist and are distinct - 404 is EMPTY (the endpoint's documented no-reports case), everything else is a red fault panel that says it is a fault. Suite **545** (was 539). | - | Landed `edb8f13`. |
| `fix/date-badge-dedup` | Suppress the member-level date badge where the group as-of already states it. | **DONE** - ff-merged at `192ed54`, pushed; local + remote deleted. | **A Step 4 finding - found by READING the live page, after every automated check had passed.** The page carried **forty** "not this panel" badges, five of them repeating verbatim the label the hepatocellular group header had just shown. The member badge now defers where the group is coherent and shows where the group spans, matching `#159` rules 1 and 2. 40 -> 35 on the live payload. Committed to master by mistake first, then moved onto a branch before pushing. | - | Landed `192ed54`. |
| `governance/as-of-derivation` | Mint the as-of derivation decision (`#160`). Governance only. | **DONE** - ff-merged at `f7aa6bc`, pushed; local + remote deleted. `#NEXT` -> **#160** (master max #159, contiguous, counted period-agnostically). | Records the derivation, the `#159` quotation correction, and the badge-dedup finding. | - | Landed `f7aa6bc` as **#160**. |
| `feat/hub-shell` | Build the `#150` hub shell: `/dashboard` becomes a module tile grid (hub-as-home) with chat docked — right rail >= md, bottom sheet < md — `HealthPanel` / `WorkoutPanel` relocated verbatim to new `/recovery` and `/training` routes, and the seven header links retired into tiles. Resolves `Q63` to structural counts. `DECISIONS_LOG` **#NEXT**, `OPEN_QUESTIONS` `Q63` -> `DONE -> #NEXT`. | **OWED** — committed and pushed, held for review; NOT merged, so `#NEXT` is unresolved and the deploy gate has not run. | Frontend-only; no backend delta. `npm run build` clean; eslint at the 5-error master baseline (no regression). Verified in-browser at 375x812 and 1280x800: one `ChatPanel` instance at both (a two-instance rail+sheet would double-POST `/chat`), rail static and side-by-side at >= md, sheet off-screen when closed / flush-bottom with the input visible when open, backdrop-tap re-closes. `HealthPanel` / `WorkoutPanel` / `ChatPanel` unedited. | Owner: merge decision. Then, per `#116`/`#121`, `railway service health-app-frontend` -> `railway deployment list` SUCCESS and grep the live `assets/index-*.js` for `HRV, sleep and overnight vitals` (confirmed present in the local production bundle, survives minification). Also OWED: no frontend test runner exists, so the `#47` no-priority-phrasing assertion is inspection-backed, not test-backed. |
| `feat/hevy-template-sync-wiring` | Give `sync_exercise_templates` a call site. New `POST /integrations/hevy/sync` (operator trigger, `get_current_user`, `only_user_id=current_user.id`, summary returned verbatim) + `connect_hevy` converted to `async def` and seeding the catalogue on the same path right after the key's `db.commit()`. `_sync_failure` maps `users_attempted == 0` -> 404 (identical body to `_require_integration`) and `users_failed >= 1` -> the recorded error through `_hevy_error_to_http`, with `_SYNC_ERROR_TYPES` rehydrating `HevyAuthError`/`HevyForbiddenError` from the summary's error STRING so `#66`'s 401->424 decoupling survives the round-trip. A seed failure never fails the key store (201 stands) and is never swallowed (`response.sync`). `hevy_templates.py` untouched; no migration. `DECISIONS_LOG` **#163**, `OPEN_QUESTIONS` **Q75** (recurring-sync fork, OPEN). | **DONE** | **ff-merged to master at `9c8176a`, pushed; local + remote branch deleted (`git cherry origin/master` clean, tip confirmed an ancestor of master). `#NEXT` -> **#163**, `Q#NEXT` -> **Q75** (master max was 161; 162 left owed to the pre-existing hub-tile placeholder, which precedes this entry in the file and is already staked as 162 by `feat/cbti-eval-trigger`).** Code + tests landed. 16 new tests in `tests/test_hevy_sync_wiring.py`; full backend suite **594 passed** (master baseline **578**). Tests drive the ROUTES over a standalone app, not the handler objects, because the defect is "the function exists and nothing reaches it". Two paired negative controls, each failing exactly its own half: route renamed to `/hevy/sync-UNWIRED` -> 6 failed / 10 passed; connect-seed call replaced with a literal -> 4 failed / 12 passed. | **PROD POPULATION GATE — CLOSED 2026-08-03.** BEFORE (2026-08-02): 494 rows (451 default / 43 custom), `max(synced_at)` 2026-07-14 12:09 — the July hand-run CLI. Merged `9c8176a`; deploy `34cda96f` SUCCESS 07:38; live `openapi.json` gained `/integrations/hevy/sync` (six paths, was five); unauthenticated POST -> 401 against 404 for a nonexistent sibling path (paired control on identity). Luke ran the authenticated POST. AFTER: **499 rows (451 default / 48 custom)**, `max(synced_at)` **2026-08-02 21:59:50+00** (= 07:59 2026-08-03 AEST — the timestamp is UTC; it is today, not stale). `defaults_seen > 0` confirmed in **durable DB form** rather than from the response: **451 default rows carry the new timestamp and ZERO rows remain stale**, so the run demonstrably read the default catalogue, not just customs — a check that survives the response body not having been captured. The +5 delta (43 -> 48 customs) is real customs added in the Hevy client since July; the 451 defaults are unchanged in COUNT because the sync is upsert-only, which is the predicted success shape, not a null result. |
| `feat/hevy-create-exercise-block` | Wire app-originated custom-exercise creation (step 2 of 2). New `<hevy_create_exercise>` block + `chat._process_exercise_actions`, processed BEFORE routine blocks so a same-turn create-then-use resolves by title; `context_builder._section_exercise_creation` carries the contract with the live spec enums and a hard permanence warning. Typed errors mapped (403 limit / 400 names the field and echoes its enum / unresolved forbids a retry / key-missing surfaces). Idempotency re-checked in the processor so the confirmation cannot falsely claim a create. NON-GOAL held: `_resolve_missing_ids` still only refuses on a miss and mints nothing. `hevy_templates.py` / `connectors/hevy.py` / the routine resolution logic untouched; no migration. `DECISIONS_LOG` **#164**, `OPEN_QUESTIONS` **Q76** (enum drift) + **Q77** (live round-trip deferral). | **DONE** | **ff-merged to master at `353d7ad`, pushed; local + remote branch deleted (`git cherry origin/master` clean).** `#NEXT` -> **#164**, `Q#NEXT` -> **Q76**, `Q#NEXT+1` -> **Q77** (master max was 163 / Q75, each re-verified free at the instant of the claim; 162 still reserved for `feat/cbti-eval-trigger`). Code + 61 tests landed. Full suite **655 passed** (baseline 594 at #163). Enum artifact captured from Hevy live spec; cross-check against the 499-row catalogue found the GET-vs-CREATE `type` divergence (4 GET-only values invalid for creation, incl. the `bodyweight_assisted` / `bodyweight_assisted_reps` near-miss) — pinned by test. Paired mutation control: block tag renamed -> **13 of 61 fail, and exactly the block-driving 13**. Mutation run also surfaced and fixed a real regression in the #43 context-builder parity guard (extra section separator shifted whitespace; fixed by collapsing both Hevy authoring contracts into one slot — the guard was not narrowed). | **LOOP CLOSED 2026-08-03.** Nothing outstanding. Prod gate resolved to **(b)** by decision — no live custom was minted, so there is no prod verification pending and none is owed. The deferred live create->list-back round-trip is carried as **Q77** with a named watch-point on the first real create: a `+ Custom exercise ... created in Hevy` confirmation means list-back beat the retry bound, while the unresolved warning means `_CREATE_RESOLVE_ATTEMPTS` or its backoff needs raising. Both failure modes are loud and correctable, and neither loses data. |
| `feat/cbti-hunting-titration` | Retune the titration from a converge-and-close weekly arc into a perpetual 4-night hunting search: `CYCLE_NIGHTS` 7->4, `MIN_VALID_NIGHTS` 5->3, `MAX_MOVE_MIN` 30->15, `ADHERENCE_FAIL_N` 3->2, new `CENTRE_CYCLES`/`centre_estimate`; the plateau `close` becomes a non-terminal converged HOLD; the sleep-need estimate is the dither centre, surfaced on `/checkin-v2/today` and in `PrescriptionCard`; deep-sleep levers added as `#47`-bounded education. Also resolves the stale hub `#NEXT` -> **#162**. `DECISIONS_LOG` **#165**, `OPEN_QUESTIONS` **Q78** (multi-user nap attribution). | **DONE** — ff-merged to master at `004581d` (feature `4b247f4`), pushed; local + remote branch deleted, `git cherry origin/master` clean. Both `#121` deploy probes PASS — see Detail. | Backend suite **674 passed** (655 baseline +19). Every changed replay decision traced to an intended constant — 3-night stub `insufficient hold`->`extend` (MIN_VALID 5->3), a 7-night span splitting 1->2 cycles (CYCLE_NIGHTS 7->4), moves capping at 15, plateau -> converged HOLD; nothing flipped for another reason. `MIN_VALID_NIGHTS < CYCLE_NIGHTS` now asserted at import. **Correction: there was no engine-close to remove** — nothing writes `closed_on` from an engine decision; `close` was consumed only by `replay.py`'s advisory #107 report and by tests, so `#118`'s "close is engine-driven" half was specified and never built. `close` retained in the `Decision` Literal + DB CHECK (block 1 imported carrying it; ledger is append-only). Existing engine/replay/settling tests retuned to derive from the constants rather than hardcode 7 — `week()` became `cycle()`. `npm run build` clean; eslint at the 5-error baseline. **Deploy verified on BOTH services (`#121`) after SUCCESS on both (`#116` timing — both deploys reached SUCCESS 2026-08-03 11:15 before any probe ran).** Backend: live `/openapi.json` shows `CBTIContextOut` carrying `centre_minutes` / `centre_cycles_n` / `dither_minutes`, the last **defaulting to 15** — a value probe, not a field-presence one, so it discriminates the retuned image from the old `MAX_MOVE_MIN=30` build. Frontend: served bundle `assets/index-D1XPQevM.js` greps 1 for `What drives deep sleep` and 1 for `Estimated need`. **Negative controls on both:** no `cbti` evaluation routes in the schema and 0 hits for `Cycle complete` / `Accept and prescribe` in the bundle — `feat/cbti-eval-trigger` is genuinely unmerged, proving both probes read the intended image and not a stale or mixed one. | Owner: merge decision, `#NEXT`/`Q#NEXT` resolved **on-branch, pre-ff** to **#165**/**Q78** (master max re-read at that instant: #164 / Q77). **Deploy probes OWED, both services (`#121`/`#116`):** backend `railway deployment list --service health-app-backend` SUCCESS + `/checkin-v2/today` returns `centre_minutes`; frontend served-bundle grep for `What drives deep sleep`. **Conflict expected with `feat/cbti-eval-trigger`** (also unmerged): both branches resolve the stale hub `#NEXT` to `#162` — same value, so take either side. This branch landing first heals the #162 hole on master. That branch's eligibility gate reads `CYCLE_NIGHTS`, so it inherits the 4-night cadence automatically once both land. |
| `feat/cbti-eval-trigger` | `#118`'s PM evaluation trigger: `GET /checkin-v2/cbti/evaluation` (read-only offer) + `POST .../accept` (witnessed mint), surfaced in `NightlyCloseOut.jsx`, reusing `#128`'s ledger read via `cbti.replay.evaluate_live_cycle`. Built 2026-08-02 against the **7-night converge-and-close** engine. Its own `#NEXT` is unresolved. | **DONE (superseded) — reworked and landed as #213 on `feat/cbti-eval-trigger-v2` (PR #66); `fec0324` deleted (local + remote).** Was pushed at `fec0324`, never merged, obsolete against master's engine: `#165` retuned the cadence to 4 nights and stopped the engine ever emitting `close`. The rework checklist below was **DISCHARGED on v2** (top row): fresh port from master, close arm deleted, 10 tests green (**838 = 828 + 10** — the derived `~730` / `#169` figures this row warned against were correctly not reused). | Verified by throwaway trial integration (2026-08-03, discarded — nothing committed): merging it onto `#165` leaves **5 of its 11 tests failing**. Four are arithmetic (`window_minutes_proposed` 420→**405** under `MAX_MOVE_MIN` 15, ×2; `nights_counted` 6→**3** as the selected complete cycle shrinks to 4 nights; excluded night falls outside that cycle). The fifth is **semantic**: `test_close_recommendation_is_surfaced_but_not_acceptable` fails because the engine no longer emits `close` at all, so the 409 guard, the `acceptable` flag and the "no accept control" UI branch are **unreachable dead code**. Conflicts branch-vs-branch in `checkin_v2.py` + `NightlyCloseOut.jsx` (both auto-mergeable by keeping both sides); `replay.py` and `OPEN_QUESTIONS.md` auto-merge clean. | **Rework checklist (owner: Luke).** (1) **Offer eligibility 7 → 4 days.** `evaluate_live_cycle` gates on `days < CYCLE_NIGHTS`, so it compiles either way — and `days_since_effective_from == 7` still *passes*, which is the trap: a weekly offer sitting on a 4-night engine is half-migrated, and the passing test is the bug, not the proof. (2) **Delete the close path** — the 409, `acceptable`, and the UI branch. It closes by subtraction; surfacing a `close` no engine emits is the dead-end `#165` removed. (3) **Fix the 5 tests to the new arithmetic.** (4) **Re-point exclusion surfacing at the engine's 4-night cycle**, so the offer and `#165`'s GATE-1 nap guard name the *same* exclusions — otherwise Q45 visibility silently regresses on exactly the cadence that made it matter more (`Q78`). (5) `#NEXT` → **169** and expected suite ~**730** (719 + 11) — both DERIVED, not measured, and both already moved twice since this row was first written (`#NEXT` was 166, then 168): **re-read master's max and the suite baseline at the instant you claim them**, never reuse a figure from this row. Treat a mismatch as worth investigating, not as a target. Rebase onto master first (`#148` renumber scope, `#113` read-the-matches). |
| `fix/hevy-create-response-parse` | **Production bug in the #164 create path: false negative + orphaned template.** `create_exercise_template` ended `return self._check(r).json()`; the live 2xx body is not the spec's `{"id": <int>}`, so `.json()` raised AFTER Hevy had created the template, unwinding past `create_and_resolve`'s sync + list-back. Template live in Hevy, absent locally, reported as "Failed to create", one retry from a permanent duplicate on a delete-less API. Fix: on a 2xx, log status + raw bytes and return `{}` instead of raising — the body is non-load-bearing (`create_and_resolve` discards it and resolves by list-back, #65). Typed 400/403 pre-checks and `_check`'s raises untouched; NOT generalised to the GET call sites, where the body IS the payload. `DECISIONS_LOG` **#166**, `FEEDBACK` **§23**, `OPEN_QUESTIONS` **Q77** -> OWED. | **OWED** | **ff-merged to master at `e438f45`, pushed; local + remote branch deleted (`git cherry origin/master` clean). Deploy `f47eb9de` SUCCESS 2026-08-03 13:59; app answers 200.** `#NEXT` -> **#166** (master max was 165, re-verified free at the claim). Code + 16 tests landed. Full suite **690 passed** (master baseline **674** post-#165 — NOT the brief's 655, which predates it). Tests fake the TRANSPORT (`httpx.AsyncClient.post`) rather than the client, so the real parse executes — the structural reason #164's 61 tests passed over a live-broken path. Paired negative control: reverting the parse fails **11 of 16**, and the 5 survivors are exactly the 403/400/500/401 typed-error paths plus the well-formed-body positive control. Trace re-verified against master before changing anything. | **Three items OWED, all owner Luke.** (1) **Orphan recovery — DISCHARGED 2026-08-03.** Luke ran `POST /integrations/hevy/sync`: summary `users_synced 1 / rows_processed 500 / defaults_seen 451 / customs_seen 49 / collision_count 0`, and the DB confirms **500 rows (451 default / 49 custom)**, `max(synced_at)` 2026-08-03 04:03:40+00. Verified at RESOLVER level, not just row-presence: the resolver predicate (exact title + `is_custom IS FALSE OR owner_user_id = 1`, default-wins) returns `238ebf2c-d96c-4673-abe0-f7a0ec887867` for `Copenhagen Adductor Plank Hip Lift`, and the stored title is pure ASCII so no U+2011 trap. The template is no longer orphaned. Incidentally confirms from REAL data what `#65` inferred from the spec: the canonical list-back id is a string UUID, distinct from the integer the POST returns. (2) **Live response bytes** — the new `logger.warning` captures them on the next real create. (3) **Q77 re-test on a FRESH movement name** — the orphan's title now short-circuits to "already in the catalogue" and would be a false pass. Merge + deploy before (2)/(3). |
| `gov/next-resolution-guard` | Enforce number-at-merge instead of trusting it. `scripts/check_governance_placeholders.py` refuses a push to master carrying `^### #NEXT` or `^## Q#NEXT`, wired as a repo-versioned `.githooks/pre-push` hook (`git config core.hooksPath .githooks`, once per clone). Rule added to the **shared loop block** of `CLAUDE.md`. `DECISIONS_LOG` **#167**. Also carries, as its own separate commit, the concurrent session's `#166` orphan-discharge `BRANCHES` line (see Detail). | **OWED** — split, pushed, held for review; `#NEXT` resolved **on-branch, pre-ff** to **#167** (master max re-read at that instant: **#166**, gaps none). | **The guard is on the REF, not on `git land`** — settled by evidence: the merge that healed `#162` was done by hand, so an alias-only guard would not have fired for it. 10 tests; suite **700 passed**. **Positive control is the real defect, not a fixture** — run against `001df4c`, the actual `feat/hub-shell` merge that put a live `### #NEXT` on master, it exits 1 and names the line; against `4b247f4` it catches both arms. Negative controls: clean master exits 0, and the three false-positive shapes are asserted explicitly (`CLAUDE.md`'s own rule text, a correction quoting the superseded token, a mid-line occurrence) — `#113`, since a guard that cries wolf gets bypassed out of habit and protects nothing. Cannot-run exits **2**, never 0. Hook end-to-end with crafted stdin: master push with placeholder REFUSED, same ref to a branch ALLOWED, clean master ALLOWED, zero-sha deletion ALLOWED. **Provenance:** this branch's tip was briefly a single mixed commit (`f64d4bb`) after a concurrent session's `git add -A` swept this work into a message describing only its own one-line edit; split path-scoped **before** any push, so nothing mixed ever reached a remote. Recorded as `FEEDBACK` §19 rows **17** (COUPLED) and **18** (MODEL — a push verified by the command's exit rather than the remote ref). | Owner: merge decision,  **Then the shared-block propagation to `health-connect-app`, recorded in `ROADMAP` NOW** — without it master lands a shared-block edit with no obligation to mirror it, the exact two-master drift the block prevents. Known gap, deliberately recorded in the entry: the `@claude` GitHub Action pushes without a local hook and is **not** covered — closing that means a CI check, not a hook. |
| `feat/chat-catalogue-visibility` | Stop the chat model claiming it cannot see the Hevy catalogue. In prod it answered a "are nordics in the catalogue?" question by saying it could only confirm exercises from the workout history, and offered to build the sync that shipped in **#61**. Both claims false: `hevy_exercise_templates` has held the full catalogue since July and the resolvers already run over all of it — `build_system_prompt` simply never surfaced it. Adds `hevy_templates.catalogue_titles` (scoped by `_visible_to`, the resolver's own predicate) + `context_builder._section_exercise_catalogue`, read UPSTREAM in `chat.py` because `context_builder` is a pure formatter and takes no Session (**#43** parity-guard invariant) — the brief's `(db, user_id, connected)` signature would have broken it. Both authoring contracts corrected. Chat-layer only; sync, resolvers, create path and schema untouched; no migration. `DECISIONS_LOG` **#168**. No `Q` — the full-inject-vs-lookup trade was DECIDED, not deferred, so it lives in Do-not-revisit. | **OWED** | Code + 19 tests landed on the branch, pushed. Full suite **719 passed** (baseline **700**). Live check against Railway: **`Nordic Hamstrings Curls` is a Hevy DEFAULT** — both words plural, which is exactly why the list is injected rather than recalled. Payload MEASURED: 500 rows (451/49) = 11,356 chars ~ 2,839 tokens/turn. **14 of 500 titles are non-ASCII** (U+2011 non-breaking hyphen: `Single-Leg RDL`, `B-Stance RDL`) — the section tells the model to copy rather than retype, with a test pinning verbatim rendering. Mutation control: unwiring the section fails exactly **2 of 19**, the two end-to-end prompt assertions, while the 17 direct-call tests pass. Parity guard untouched (conditional append, no empty-slot separator). | **Outstanding: the ff-merge (owner Luke).** Number-at-merge contends with `feat/cbti-eval-trigger`, which also holds a `### #NEXT` — whichever lands first takes 168; re-read master max at the instant of the claim, do not hardcode. No prod gate: this is prompt text over an already-verified catalogue read, so a deploy check is the whole verification. Bonus once landed — it retires the Q77 probe-target awkwardness: "is this movement novel?" becomes answerable in-chat instead of needing a manual `resolve_exercise` pre-flight. |
| `gov/placeholder-guard-cross-repo` | Generalise `#167`'s guard so it can be propagated rather than mis-propagated. Two arms of the shared block hardcoded `health-app`'s heading grammar: the **session-open sweep** (`^### [0-9]+`) matches **168** entries here and **0** in `health-connect-app`, whose entries are headed `### #21 — …  ·  active`; the **guard's question pattern** (`^## Q#NEXT`) can never fire against HCA's `### Q8 — …  ·  OWED` headings. Fixed in `health-app` first — the script is one implementation of one rule, so fixing it in the copy would mint a second master one layer under the shared block. Patterns become `^#{2,3} #NEXT` / `^#{2,3} Q#NEXT` (level tolerated, form still pinned); the session-open anchor becomes `^### #?[0-9]+` (sigil-agnostic as well as period-agnostic). Touches `scripts/check_governance_placeholders.py`, `backend/tests/test_governance_placeholder_guard.py`, `CLAUDE.md` (two shared-block bullets), `DECISIONS_LOG`, `ROADMAP`, `FEEDBACK` §24, `OPEN_QUESTIONS`. DECISIONS_LOG **`#169`**, OPEN_QUESTIONS **`Q79`**. | **DONE** — ff-merged at `d573927`, pushed; local + remote branch deleted, `git cherry origin/master` clean (patch-id, not merge-base). Placeholders resolved **on-branch, pre-ff**: master maxima re-read at that instant were **168** (decisions, no gaps 1..168) and **78** (questions), giving **#169** / **Q79** — not reused from session open. Post-resolution tree: 169 decisions / 79 questions, no gaps, no duplicates. **Renumber scope per `#148` — classified, not counted:** 12 branch-introduced tokens resolved, each substitution asserting its expected occurrence count so a miscount fails loudly; every remaining `#NEXT` left untouched by classification (pre-existing debt, convention quoted as rule text, regex literals, test-fixture heading forms). Guard exits **0** against the pushed `origin/master` ref, and `#169` / `Q79` / `FEEDBACK` §24 each assert present **on the remote ref**, not on a command's exit (`FEEDBACK` §19 row 18). | **The brief's predicted trap was real but mislocated, and re-deriving it against the tree is what found the actual one.** Chat predicted the break in the guard's *integer* anchor; the guard has no integer anchor — it matches the literal token and never computes a max. The `^### [0-9]+` chat was reaching for lives in the **session-open ritual**, where the failure is worse than predicted: not a guard that fails to fire but a count that returns **zero** and reports it as the repo's max. **Controls, post-change:** positive — `001df4c` (the real `feat/hub-shell` merge that holed `#162`) still exits **1** and names the line, `4b247f4` still catches **both** arms; negative — clean tree **0**, `master` **0**, unreadable ref **2**. **3 new tests** pinning both repos' grammars (HCA-form placeholders fire, HCA-form *resolved* headings do not); suite **722 passed** (baseline 719). `#113`'s three false-positive shapes still assert as non-matches — `{2,3}` widens the level, not the form. **Hook end-to-end, crafted stdin, four paths:** master+placeholder **REFUSED (1)**, same ref to a branch **ALLOWED (0)**, clean master **ALLOWED (0)**, zero-sha deletion **ALLOWED (0)**. | Owner: merge decision, Luke. **Then the HCA propagation itself** — `.githooks/` + script copy, `core.hooksPath` install, shared-block re-mirror, HCA `DECISIONS_LOG` / `OPEN_QUESTIONS` / `BRANCHES` rows — which is **not** done and **cannot** be done from this session: `health-app`-rooted, and loop work in a second repo requires an HCA-rooted session (global `CLAUDE.md`, single-repo scope). Carried in `ROADMAP` NOW. Known gap unchanged and inherited by HCA on copy: the `@claude` Action pushes without a local hook and stays uncovered — closing it needs a CI check, not a hook. **Now tracked** as `OPEN_QUESTIONS` **`Q79`** (also minted on this branch); before it, the gap lived only in `#167`'s prose and a `BRANCHES` row, so the store whose job is *what is undecided* showed a fully-enforced guard and there was nothing for the HCA row to mirror. Also on this branch: **two** placeholders, not one (`DECISIONS_LOG` → **#169**, `OPEN_QUESTIONS` → **Q79**) — and the branch carrying both is what exercised the widened guard against real content rather than fixtures. |
| `ci/placeholder-guard-action` | Close `Q79` by running the `#167` placeholder guard on a surface a client-side hook cannot reach. `.github/workflows/governance-guard.yml` (the repo's **first** workflow file), `ubuntu-latest`, on `pull_request` targeting master + `push: [master]`: **2a** asserts `.githooks/pre-push` tracked mode `100755`, **2b** *executes* the hook with crafted pre-push stdin against a known-clean ref, **2c** runs the guard against the ref that would land. Also corrects `check_governance_placeholders.py`'s docstring, which claimed the `land` alias calls it — it never did. `DECISIONS_LOG` **`#170`**, `OPEN_QUESTIONS` **`Q79` → DONE**. | **DONE** — ff-merged at `e49a8f1`, pushed; PR `#11` MERGED, remote branch auto-deleted by GitHub on merge, local deleted, `git cherry origin/master` clean (patch-id, not merge-base). `#NEXT` resolved **on-branch, pre-ff** to **#170**, master max re-read at that instant: **169** (count 169, no gaps 1..169), questions **79**; post-resolution 170 decisions, no gaps, no duplicates. **Both trigger arms verified on real content, not fixtures:** the `pull_request` arm green on the resolved tip (run `30882397491`, head `e49a8f1`) and the `push: [master]` arm green on the landing itself (run `30882424558`, head `e49a8f1`). Suite **722 passed**. | **`Q79` was right about the hole and wrong about the agent.** health-app has **no `.github` directory in any commit on any ref** — the `@claude` Action was never wired here. The real uncovered path is the **web-UI merge button**: five merges on master committed by `GitHub <noreply@github.com>` (`e62f89f`, `0aa0200`, `f4b538f`, `cb1b58f`, `9f9437c`), server-side ref updates a per-clone `core.hooksPath` cannot bind. Closing the question against the wrong agent would have covered a path that does not exist while the merge button stayed open behind a green check. **Prevention vs detection, stated rather than blurred:** `push: [master]` fires after the ref has moved, so on this path it is detection; prevention is the PR arm **plus branch protection**, which is GitHub-side config and **not committable** — see the Blocker column. **Four control runs, evidence quoted from the failing output:** negative — PR `#11` clean, run `30881982823` **success**; positive/placeholder — PR `#12`, run `30882006064` **fails at 2c** naming both offences against merge commit `91aeccc`; positive/mode — PR `#13` at `100644`, run `30882020463` **fails at 2a** (`tracked mode: 100644`), which is `#23` reproduced deliberately on the first surface that can see it; positive/execution — PR `#14`, run `30882100017`, minted **because the mode control did not prove what it appeared to** (2a short-circuits the job, so 2b never ran against the bad mode), removing 2a only: `./.githooks/pre-push: Permission denied`, **exit 126**. Scratch PRs `#12`/`#13`/`#14` closed and all three remote branches deleted; `git branch -r` verified back to baseline. **`ubuntu-latest` is load-bearing:** every other control in both repos runs on Windows with `core.filemode=false`, where Git honours the shebang regardless of mode, so no number of Windows controls could ever express `#23`'s class. | Owner: merge decision, Luke. **Then: set branch protection requiring the `governance-guard` check** — until that is done the PR arm is advisory and the merge-button path is detected, not prevented. Code cannot land it (GitHub-side repo config). Two of the three enforcement layers are now unversioned config (`core.hooksPath` per clone, branch protection per repo); only the workflow file has a diff, so a green run is not evidence all three are installed. **Also owed:** HCA `Q12`, the mirror of `Q79`, stays OPEN until an HCA-rooted session propagates this — they close together or the asymmetry returns in a new form. **Unverified, deliberately:** whether a GitHub App holds push rights here (GitHub-side, unreadable from the tree). |
| `chore/merge-path-pr-migration` | Make the pull request the sole route to master and re-site the merge motion onto it. Ruleset `master-pr-gated` (id `20414758`) created and **verified by observation**: PR required, `placeholder guard (POSIX)` required under a strict up-to-date policy, non-fast-forward forbidden, `bypass_actors` empty. `land` rewritten around `gh` and made repo-local; `stale` untouched. Merge-path mechanics leave the shared block under a new **boundary criterion** — a rule is shared only if its correctness is independent of any surface outside the tree. DECISIONS **#171** + **#172**, OPEN_QUESTIONS **Q80**. | **DONE** — merged to master as `4308867` (PR #16) by `gh pr merge --merge --delete-branch`, the new ritual's first live exercise; local **and** remote branch removed by the merge itself, `git stale` confirms the ref is gone. Master's `push` detection arm ran green on the merge commit (run `30957706908`). Three concern-split commits: `56d71bc` (CLAUDE.md shared/local split) → `d9f45f3` (guard docs: third enforcement surface) → `f9bea0a` (stores: two entries + the Q + ROADMAP row), then the resolve commit as tip. Backend suite **722 passed**; guard's own tests **13 passed** after the docstring/message edits. Governance + docs only — no `backend/` logic, no `frontend/`, no migration. **Renumber on-branch immediately before the merge, master max re-read at that instant** (the new rule's own requirement): 3 headings (`### #NEXT` ×2, `## Q#NEXT`) plus **9 forward references I had written as `#171`/`#172` rather than as placeholders** — classified, not counted, per `#148`: `CLAUDE.md` ×3, `DECISIONS_LOG.md` ×2, `OPEN_QUESTIONS.md` ×2, `ROADMAP.md` ×1, `scripts/check_governance_placeholders.py` ×2. Those inline refs were pre-resolved because two entries on one branch make a bare inline `#NEXT` ambiguous; the cost is that they are only correct while master max stays `#170`, which is exactly what the pre-merge re-read checks. | — (merge complete). | **OWED to Luke, and NOT closable by any commit in this repo:** the `land` alias body. Git aliases live in unversioned config — the **old** ff-only body is still in `~/.gitconfig` and is now unrunnable, since its `push origin master` is refused by the ruleset. Remove it there (`git config --global --unset alias.land`) and re-add it **repo-locally** in this clone: `git config --local alias.land '!f() { gh pr merge "$(git branch --show-current)" --merge --delete-branch; }; f'` — thin on purpose, adjudication belongs in the guard (`Q#NEXT`), not in a per-machine file. `stale` stays global and needs no change. A committed doc is not a rewritten alias, so this row stays open on that item until Luke runs it. **Also OWED:** shared-block propagation to `health-connect-app`, ROADMAP NOW row 4 — HCA takes the amended block verbatim and loses nothing, since its own merge path is deliberately unaffected. Owner: Luke. |
| `feat/status-diff-engine` | Status-tooling lane, Steps 0–3: pending-queue adjudication (#208 lift #186 → mint filter · #209 extraction gate = questions+branches, decisions by sequence, premises re-measured via the #207 reader · #210 `#NEXT` never `#N`; Q-A/Q-B DISCHARGED-BY-#207, not minted), the #209 any-missing-state gate that names offenders (pipe-split VERIFIED column-bounded), and the cross-repo diff engine + invokable report (`scripts/status_diff.py`) with renumber-survival by title fingerprint. S4 halt-visibility is paper-only in `closeout.md`; the Step-4 build stays gated. DECISIONS_LOG **#208–#210**. | **OWED** | Three concern-split commits — `87890b7` (gov: adjudication) · `cc6b2e9` (gate remainder) · `8612a1f` (diff engine + report); pushed; **PR #58 open, awaiting Luke's review** (code — full human review, not self-merged). 9 `scripts/tests/` pass; `--self-check` green; report demonstrated on the real snapshot pair (HCA questions correctly flagged "1 closed + 2 opened, possible renumber — verify by hand"). Numbers #208–#210 resolved against master max #207; re-verify at merge. | **Outstanding: review + merge PR #58 (owner Luke).** Then Step 4 (S4) stays gated on Luke's ruling on the halt-visibility proposal in `closeout.md`. No schema/migration; no deploy gate — the report + `--self-check` are the verification. |
| `feat/catalogue-freshness-on-create` | **Create-path catalogue freshness — closes the stale-catalogue mint window #211 left open (Q75 residual).** The chat `<hevy_create_exercise>` path reads the local template store twice — the #164 honest-confirmation pre-check in `_process_exercise_actions` and `create_and_resolve`'s own idempotency read (#65) — so a create with no recent workout fetch races a stale catalogue and can mint a permanent duplicate against Hevy's delete-less API (the 2026-08-05 mint offer). Fix: `refresh_catalogue_if_stale(db, user_id)` once before the chat pre-check, reusing #211's per-user marker + 24h gate + non-blocking wrapper verbatim. Placement is load-bearing — before the chat pre-check, NOT inside `create_and_resolve`: honest confirmation needs THAT read fresh, and one refresh there leaves `create_and_resolve`'s read fresh too. `DECISIONS_LOG` **#212**. | **DONE** | Two concern-split commits — `60c5fa9` (feat: chat-path gate + test) · `aa5477c` (gov: #212); land housekeeping (this row + `#NEXT`→212 resolution) rides the branch per #176(b). Full backend suite green **828** (823 baseline + 5 new `test_hevy_create_freshness_gate.py` — real gate + real pre-check over a faked sync transport). `#NEXT` → **#212** (master max 211, re-verified at land). Merged via PR, branch deleted. | **Nothing outstanding.** Chat is the sole caller of `create_and_resolve`; the gate lives at the chat layer by design. Live smoke against app chat (the mint-offer path) folds into Luke's existing Q75 smoke test — now exercises both fetch (#211) and create (#212) gates. No schema/migration; no deploy gate beyond the standard bundle. |
| `fix/q45-nap-attribution` | Orphan branch carrying the first attempt at the Q45 nap-attribution change (attribute a nap to the night it precedes; `NAP_EXCLUDE_MIN` 0->30). One pushed commit `4f77679`, cut before `#218` landed and based on `1e6cf0c` — it predates the accept-confirm rewrite of `EvaluationOffer.jsx` and the `#218` changes to `test_cbti_eval_trigger.py`, so it will not fast-forward and its frontend/test hunks are presumed stale. Pre-existing at session open 2026-08-17; had no row until this one — the invisible-work class the branch gate exists to catch. | **DONE — superseded, branch deleted** | Never merged, and correctly so: it predated `#218`, would not fast-forward, and Step 2 of the rework found its 3-site frontend diff already stale (`#218` had rewritten one target and independently added a fourth Q45 site the orphan never saw). Its INTENT was re-derived — not replayed — onto `fix/q45-nap-attribution-rework` and landed there as `#219`. **No commit of `4f77679` reaches master.** Deleted from origin after the rework merged. | **Nothing outstanding.** The engine change, the date-1 attribution and the Q45 close all landed via the rework; this branch's only remaining value was the record that it existed unrowed, which `#219` carries. |
| `fix/q45-nap-attribution-rework` | **Q45 closed on operator determination; nap attribution reversed (`DECISIONS_LOG` #219).** A nap attributes to the night it PRECEDES (`replay.load_nights` reads Night(W)'s nap from row W-1) and `NAP_EXCLUDE_MIN` rises 0 -> 30. Re-derived against current master rather than replayed from the orphan `fix/q45-nap-attribution` (`4f77679`), which predated `#218`. Closes **Q45 -> #219**; **Q78 stays OPEN** (unblocked by the close, not resolved by it — two over-threshold nap nights still starve a 4-night cycle); retires the dated `ROADMAP` NOW row's "contaminating capture / unverified attribution" framing. | **DONE** | Code+tests+governance, so full human review, not the guard-only path. Backend **877 -> 882** (+5: threshold-floor both directions, attribution positive AND negative, the one-day-outside-the-window read, a nap on a diary-less day, and attribution x threshold composed); frontend **41**, unchanged. `#218`'s `test_cbti_accept_decision_class.py` verified green in isolation (8/8) — the rework does not touch it. Non-vacuity control: with the reworked tests in place against master's `replay.py`, all five attribution assertions FAIL, so they discriminate the new read rather than the new threshold. Seven stale comment sites rewritten, not the orphan's three — `models.py`, `checkin_v2.py` (x2) and `import_cbti_block.py` were never in the orphan's diff. `#NEXT` -> **#219** resolved at merge on a master-max re-read. Merged via PR, branch deleted. | **Nothing outstanding.** No schema change and no migration, so no `SCHEMA.md` update and no migration gate. Deploy-settle probe per `#116`/`#121` against `health-app-backend`. Residual work is **Q78** (frequent-napper cadence) and the `Q78` do-not-resolve-by caution on further `NAP_EXCLUDE_MIN` movement — both recorded, neither owed here. |
| `claude/injuries-operator-view-lbxyvf` | The `/injuries` operator view — frontend-only surface over the #222/#223 endpoints: route + hub tile + single `include_resolved=true` fetch + active list ordered by chain-earliest `added_at` + display-only history toggle distinguishing resolved from superseded + per-row resolve gated on a human-authored `basis` and a `resolved_by` tier. Constraint cite #232. Effect readout corrected mid-review to stop asserting contraindication (a server-side computation absent from the payload, false for `pes anserine`/`finger`) and to label `restrictions[]` surfaced-not-enforced. | **DONE → #237** — merged via **PR #100**, merge `8098e63` (feature tip `5e31321`); branch merged + deleted. Frontend suite 57 passed; lint/build clean; `placeholder guard (POSIX)` success. | Item 4 landed (on PR #101): DECISIONS_LOG **#237** + this DONE row + the CLAUDE.md Recent-landings pointer. Owner: Luke. |
| `gov/injuries-followups-closeout` | Governance from #100's session: `OPEN_QUESTIONS` **Q120** (injury value shape has no onset field; "on record since" is a compensation), `ROADMAP` backfill-audit lane **reframed** (reordered to lead with the reframe; prior premise retained superseded below) + new **edit-and-supersede** NEXT lane, this session's `closeout.md`, these BRANCHES rows, and — after #100 merged at `8098e63` — **item 4**: DECISIONS_LOG **#237** (the #100 stub), the feature branch's DONE row above, and the CLAUDE.md Recent-landings pointer. Master merged in to satisfy strict mode. | **DONE** — merged via **PR #101** (this branch); docs + item-4 decision entry, guard-gated per #176, removed-line audits clean. | Branch merged + deleted at PR #101. Owner: Luke. |
| `claude/shared-loop-merge-disposition-sgu6o7` | **Merge disposition made explicit in the shared loop block — Code merges its own PRs on green, the human merge gate is removed (`DECISIONS_LOG` #238).** New `### Merge disposition` subsection inserted in `CLAUDE.md`'s shared block, after `### The loop` and before `### The unseeable-surface rule`: PRs open ready-for-review never draft, Code merges as soon as every required check is green — no confirmation request, no operator wait, no scheduled check-in re-reporting a clean `mergeable_state`; a green PR left unmerged is a defect. Sole exception: a schema-migration PR (or anything the session was told to hold) waits for explicit operator instruction. Governance-only diff — `CLAUDE.md`, `DECISIONS_LOG` **#238**, this `BRANCHES` row, the Recent-landings pointer; no code, no migrations, so the #176 guard-gated path applies and the only removed lines are the declared Recent-landings trim (#233 pointer drops off the cap-3 block). Verbatim propagation to `health-connect-app` is a separate HCA-rooted session (Session B). | **DONE** — merged via PR (this branch), opened **ready-for-review not draft** and merged on green **without a confirmation request** (the rule's own first exercise); branch deleted; merge SHA recorded at close-out (a `--merge` commit's SHA is unknowable on the merged branch). `#NEXT` → **#238** resolved on-branch pre-merge, master max re-read at that instant **237**, no gaps. | **Nothing outstanding in `health-app`.** The shared-block propagation to `health-connect-app` — its own `BRANCHES` row + Recent-landings pointer, **no** second `DECISIONS_LOG` entry — is Session B, HCA-rooted, and cannot be done from this session. Owner: Luke / Session B. |
