# BRANCHES — every branch not master lives here until merged+deleted

| Branch | Purpose | Status | Why parked | Unblocks on |
|--------|---------|--------|-----------|-------------|
| `feat/constraint-consumption` | Injury constraints consumed, not decorative: AM soreness items ← active injury ledger; injury `trajectory` in JSON `value` + divergence / symptom-gated review surfacing in `get_readiness_snapshot`; `naive_baseline` soreness term = max across reported sites (constraint-consumption brief, Steps 1–4; DECISIONS_LOG #72/#73) | **LANDED 2026-07-13** — ff-merged to `master` at `e70437b`, pushed; local branch deleted (never pushed to origin) | — (backend + frontend, no migration, no external precondition) | Code side complete. **OWED (post-land):** live Railway seed of the two new injury entries + trajectories (`seed_engine` against Railway Postgres) then `get_readiness_snapshot` read-back — verified only on local sqlite this session (MCP connector invalidated; #42 precedent). `_seed_injuries` is add-only: the two new keys don't exist in prod yet so first seed includes trajectory; the 3 pre-existing injuries stay unchanged (no trajectory, by design). Frontend `CheckInAM.jsx` change NOT browser-verified (logic unit-verified). |
| `feat/hevy-exercise-template-resolver` | Hevy exercise-template synced store + title→id resolver (DECISIONS_LOG #60/#61) | **LANDED 2026-07-08** — ff-merged to `master` at `41a8998`, pushed; local branch deleted | — (prod-stamp precondition `217dce22fbc5` confirmed before land) | Code side complete. Loop closes on Luke's Railway post-apply stamp reading `3497ab483935` (`SELECT version_num FROM alembic_version`). |
| `feat/hevy-create-loop` | `create_and_resolve` — app-originated custom exercise via create→sync→list-back (DECISIONS_LOG #65, resolves Q14) | **LANDED 2026-07-11** — ff-merged to `master` at `e13e3a2`, pushed; local+remote branch deleted | — (backend-only, no migration, no external precondition) | Code side complete. `create_and_resolve` is a library function; resolver-activation wiring stays dormant (unchanged this session). |
| `feat/connector-error-policy` | Connector failures decouple from session auth (401→424, read handlers → 502, global 500 CORS guard) + "See all" true pagination (DECISIONS_LOG #66, #67) | **LANDED 2026-07-11** — ff-merged to `master` at `51c7091`, pushed; local branch deleted (never pushed to origin) | — (backend + frontend, no migration, no external precondition) | Code side complete + deployed. NOT E2E-verified: live "See all" against a real Hevy account (connected key invalid); worst case degrades to page 1, guarded by the 502 helper. |
| `fix/hevy-exercise-history-path` | Correct `get_exercise_history` path to `/v1/exercise_history/{id}` (DECISIONS_LOG #69, resolves Q16) | **LANDED 2026-07-12** — ff-merged to `master` at `2fc21e2`, pushed; local branch deleted (never pushed to origin) | — (backend-only, no migration, no external precondition) | Code side complete. Doc-evidence basis (official docs + 3 clients); live `exercise_history` corroboration optional belt-and-braces later (local Hevy MCP hung this session). Method currently unwired — zero call sites — so no downstream behaviour-shift. |
| `fix/hrv-sleep-integrity` | Ingest bounds guard + deep-sleep excluded from daily readiness (DECISIONS_LOG #70, #71 — HRV & Sleep Data Integrity brief Tasks 3, 4) | **LANDED 2026-07-13** — ff-merged to `master` at `dccca41`, pushed; local branch deleted (never pushed to origin) | — (backend-only, no migration, no external precondition) | Code side complete. Task 3 history sweep NOT run (local DB is dev SQLite; sweep SQL supplied for Railway). Both fixes are independent of the HRV-metric question. |
| `feat/recovery-metrics-rhr` | Expose RHR as a series in `get_recovery_metrics` (HRV & Sleep Data Integrity brief Task 2) | **PARKED 2026-07-13** — 1 commit at `a4e1887`, local-only (never pushed) | Held with the HRV investigation, not a standalone fix: RHR from the primary `samsung_hrv_readings` source is the scraper's `sleep_hr_bpm` (same device family as HRV), not the independent Health Connect `resting_heart_rate` the (A)/(B) discrimination assumes | **Task 1 node dump** (`HRVAccessibilityService` in `health-connect-app`, a separate repo) resolving the (A) instrumentation vs (B) physiology gate. Lands with the HRV story; DECISIONS entry numbered at merge. |
