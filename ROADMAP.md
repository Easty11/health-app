# ROADMAP

Last updated: 2026-08-18

---

## NOW — active sprint

_Date-anchored work, ordered by external date. The one undated row (cross-repo propagation) is pinned here by #112, which names ROADMAP NOW as the canonical home for cross-repo debt._

| Item | Notes |
|------|-------|
| **CBT-I:** Q45 nap day-attribution (the `naps_min` date-1 read) | **DONE -> #219 (2026-08-17).** Closed on OPERATOR DETERMINATION, not clinical provenance: which night a nap belongs to is a modelling convention the operator sets, and the convention is that a nap attributes to the night it PRECEDES. The date-1 read is now defined correct rather than provisionally trusted, so the row's former "contaminating capture / unverified attribution" framing is retired — `replay.load_nights` performs the read the `daily_records.naps_min` column comment has specified since the column was created, and the engine's exclusion no longer rests on an unconfirmed referent. `NAP_EXCLUDE_MIN` 0 -> 30 in the same change, so a sub-30-min nap no longer spends a 4-night cycle's one-night margin. The old bar (confirm from VA protocol docs or the administering clinician) is superseded as the wrong gate for a convention; the workbook's scoped-null search stands and is not re-run. **Not fully discharged for a second user:** two OVER-threshold nap nights still starve a cycle, which is `OPEN_QUESTIONS` **Q78** — still OPEN, now unblocked by this closure rather than gated on it. Owner: Luke. |
| **CBT-I:** manual witnessed evaluation trigger (#118's PM-offer half) | **DONE → #213, verified in prod (2026-08-13).** Merged via PR #66 (merge `118574e`). Offer + accept both exercised against real block-2 (`cbti_blocks.id`=2) data: offer parity as-of 08-12 (eligible, last-complete-cycle `hold`, 390/22:30, n=2); accept minted rx 12 through the endpoint — one CORS-preflighted `POST …/accept` → 200 in the request log, engine-parity on every field, rx 11 carrying only `effective_to`=08-12 + `superseded_by`=12, block untouched (re-confirmed this session by a read-only Railway query: rx 12 `hold`/n=2/390/22:30, rx 11 superseded_by=12, block open). Discharges the OWED verification. Two follow-ups: **Q101** (elapsed-vs-sufficient selection) and the accept-confirm UI defect (NEXT row + #214). rx 12 stands pending Luke's decision on whether to correct it. Reworked onto the 4-night hunting engine, cut fresh from master; the obsolete `feat/cbti-eval-trigger @ fec0324` was reference-only, never merged. `#165`'s 4-night retune had already removed engine-`close`, so the original branch's 7-day eligibility and its entire close-refusal path were obsolete: the offer now gates on `days >= CYCLE_NIGHTS`, the whole close arm (the `acceptable` flag, the accept 409, the dead-control UI) is DELETED as unreachable (the engine emits a `converged HOLD`, never `close`), and the retuned suite is green — **838 = 828 + 10** backend, +12 frontend vitest. Offer/accept semantics, gate evidence, and the preserved-master-features note are in #213. Historic framing below. **DATED — ~31 Jul, first titration cycle.** The last piece to *close* a titration cycle in-app: PM close-out offers evaluation once ≥7 days have elapsed since the current prescription's `effective_from`; the engine returns the decision and its basis; the row is minted on acceptance (#118). Block 3 opened 24 Jul, so the first evaluation is due ~31 Jul — a dependency, not a deferral (cannot fire before a full cycle of nights exists). #118's block-open half is already built. **Q49 RESOLVED → #128 (gate lifted):** the replay now reads the effective prescription per cycle from `cbti_prescriptions` (window, lights-out, anchor) and anchors cycles to `effective_from`, so a mid-cycle correction is adjudicated against, not false-held. The trigger, when built, must reuse that same read (the shared "≥7 nights since effective_from" model, #118) — it is no longer gated. Cut a fresh branch from master. Owner: Luke. |
| Lab upload pipeline | PDF/photo → Vision extraction → confirmation screen with outlier flagging → stored. First stage of the medical spine. Consumer hero-feature dependency. File-first UX design is Locked — DECISIONS_LOG #48, not implemented. **Marker canonicalisation (#50) is now FULLY IMPLEMENTED (2026-08-18, #220):** the map moved from a startup-loaded `marker_canonical.json` dict to the `marker_canonical_entries` table (seeded from that JSON, 70 rows), and `POST /labs/canonical/bind` gives the confirmation screen a guarded runtime bind that also promotes the marker's historical unmapped rows — #50's "confirmation-populated" half, which was never buildable against a static repo file. The over-collapse unit-guard was already live at confirm and is now carried to bind-time and backfill-time too. #50's "Not implemented" status line is superseded in place. Ends the hand-edit-JSON-plus-backfill chore run four times (PSA, conjugated bilirubin, CK, Cystatin C). Residual: `OPEN_QUESTIONS` **Q104** — `gates.py`/`rephrase.py` still read the JSON, which is now only the seed; verified degrades-safe, blocks nothing. **Raw read-back shipped (2026-07-29, `feat/frontend-readback`):** `GET /labs/results` (#59's consumer) + a read-only values/ranges/lab-flags table in `Metrics.jsx`, plus a Dashboard "Labs" nav link (was URL-only). #47-bounded — values only, no interpretation. The interpreted delta-first view stays the Interpretation-layer lane (#49): a different surface, not superseded by this. **Ingest integrity closed (2026-08-01, #157):** ten `lab_reports` rows carrying zero results were investigated as suspected live data loss and are NOT — every one is a re-upload whose markers already existed at that `collected_date`, skipped correctly by `#156`. Nothing was lost; the values sit on the earlier report. The real defect was silence: the confirm response reported the write count and the client discarded it. Confirm outcome is now surfaced, an empty report renders as a fault, the ingest path asserts persistence against the database rather than response success, and a separate latent loss (intra-batch repeated marker dropping every copy) is fixed. **Uploading is unpaused.** Operator decision owed on the junk rows — see `closeout.md`. |
| Interpretation layer build | Design Locked, build pending — DECISIONS_LOG #49 (delta-first, three sections, filtered levers). Depends on the lab store (row above) and the lever dictionary — DECISIONS_LOG #51 (GRADE-tiered, decided not implemented). Education pathway (explain mechanisms, list/filter levers), NOT clinical advice — regulatory boundary per DECISIONS_LOG #47. **Packages OPEN_QUESTIONS Q36–Q41** (the six 'Due 4b' forks) — grouped there under the 4b package; this row is their single ROADMAP home. Q35 is a related but separate live question, not in the package. Dated: the early-Aug TRT panel is the first real consumer. Execution order, status tags, and the three-runtime-gate model are detailed in the **Interpretation layer — build sequence** block below. **1b DELIVERED (2026-08-01, #159/#160):** the view reads `GET /interpretation` live, groups carry an as-of date and are labelled when off the trigger draw, ungrouped renders, and the route is linked from the dashboard. The remaining increments are the rephrase pass (2), lever-tap threads (3) and go-live (5). |
| Appointment brief | Hero consumer feature — "Never waste a medical appointment again." Pre-appointment synthesis across modules. Depends on lab pipeline + interpretation layer. Can now query `current_state` directly instead of re-deriving it. |
| ~~**Cross-repo:** propagate the number-at-merge ENFORCEMENT rule to `health-connect-app`~~ | **DISCHARGED: shared block byte-identical (SHA-256 `5790ae3d527e0a3b7f5a8c0cfebcc560d6ff3e0c96b6b6ce8f111eb8ca737130`, 97 lines / 6039 bytes LF-normalised, `BEGIN`…`END` inclusive), enforcement landed HCA-side (#24/#28/#34); verified 2026-08-17.** Evidence gathered in-session, not inherited: both repos' `BEGIN…END SHARED LOOP RULES` spans extracted and SHA-compared, and HCA master confirmed to carry all three enforcement artefacts — `scripts/check_governance_placeholders.py` (blob `c322cc4d5fb6`), `.githooks/pre-push` (blob `8cd10013aa69`), and `.github/workflows/governance-guard.yml` (blob `3d041c4b613e`). The "copy the Python, do not port to Node" position this row recorded as *authored in chat and not yet a decision* has since been adjudicated in HCA's own log (#24 — the guard propagated, its header repo-specific by necessity; #28 — `Q12` closed on three verified layers rather than the ruleset alone). Struck, not deleted. Historic detail below. **OWED.** The shared block gained the `Number-at-merge is ENFORCED, not trusted` bullet plus the `git config core.hooksPath .githooks` install line beside the `stale` alias (the `land` alias sat there too at the time of writing; it has since left the shared block entirely — see the boundary-criterion row below, and copy the block as it stands now, not as this sentence describes it), and (`#169`, 2026-08-04) a **rewritten Session-open bullet** naming BOTH maxima plus a **new verify-before-you-copy clause in the shared-block preamble** — **four separate insertions**, all between the `BEGIN`/`END SHARED LOOP RULES` markers in `health-app/CLAUDE.md`. Must be copied **byte-identically**; a paraphrase is drift, which is the two-master failure the block exists to prevent. **PRECONDITION NOW MET, and it was not a formality:** propagating the block as it stood on 2026-08-03 would have carried a session-open decision sweep (`^### [0-9]+`) matching **0** of HCA's 21 entries, **no question sweep at all** (the analogous `^## Q[0-9]+` matches 0 of HCA's 11), and a guard question-arm pattern (`^## Q#NEXT`) that can never fire against HCA's `### Q8 — …` headings. All three were generalised in `health-app` first (`#169`, 2026-08-04) precisely so the copy stays a copy. **And the direction is now conditional:** HCA's wording of the session-open rule was *generic and correct* where health-app's was pinned and wrong, so a verbatim copy would have replaced a correct line with a defective one — the preamble's new clause requires verifying the source against the destination's actual shape **before** copying, which is a precondition of this row, not a review of it. **The script and the hook now DO propagate** — the chat brief of 2026-08-03 takes the position *copy the Python, do not port to Node*, on the grounds that a second implementation of one rule is the drift the shared block exists to prevent, and that HCA thereby gains a Python dev-tooling dependency which is to be **logged, not hidden**. That position is authored in chat and is **not yet a decision**: it must be adjudicated and recorded in HCA's own `DECISIONS_LOG` from an HCA-rooted session. Until then HCA has the rule written down and no enforcement. Landable only from an **HCA-rooted session**, `pwd`-verified first — the desktop folder picker has opened the wrong repo before. Owner: Luke. Per #112 this is the canonical home for cross-repo debt. |
| ~~**Cross-repo:** propagate the CLAUDE.md shared block to `health-connect-app`~~ | **DISCHARGED: shared block byte-identical (SHA-256 `5790ae3d527e0a3b7f5a8c0cfebcc560d6ff3e0c96b6b6ce8f111eb8ca737130`), enforcement landed HCA-side (#24/#28/#34); verified 2026-08-17.** #111's secret-rendering prohibition is inside the compared span, so the byte-identity above IS this row's discharge — the prohibition cannot be present in one repo and absent in the other without changing the hash. HCA #34 records the return trip: the block was pruned to invariants and replaced verbatim, byte-identity proven at that landing too, so this is a second independent confirmation rather than a restatement of the first. Struck, not deleted. Historic detail below. **OWED.** The shared block gained #111's secret-rendering prohibition (health-app `CLAUDE.md`, between the `BEGIN/END SHARED LOOP RULES` markers). It must be copied **byte-identically** into `health-connect-app/CLAUDE.md` — a paraphrase is drift, which is the two-master failure the shared block exists to prevent. **Drift verified, not assumed** (read-only check, 2026-07-23): HCA is cloned at `Projects/health-connect-app` and carries the shared block's `BEGIN/END` markers, but greps **0** for the secret-rendering rule where health-app greps 1. Blocked here on three counts: `chore/secrets-residuals` is not cut in HCA, HCA's working tree is not clean, and a canonical-store edit in a second repo is forbidden from a health-app-rooted session. `.claude/settings.json` does **not** propagate (its deny patterns are health-app-specific paths). Owner: Luke, from an HCA-rooted session. Per #112 this is the canonical home for cross-repo debt. |
| **Cross-repo (shared-block edit owed):** extend the `#NEXT` / number-at-merge rule to cover more than DECISIONS entries | **OWED.** FEEDBACK §20 (2026-07-26) records that hardcoded governance numbers on held branches accrue renumber debt (the 5-branch landing session: one `#NEXT`-compliant branch cost one substitution, two hardcoded branches cost four and seven, one leaking into code docstrings). The documented rule (`CLAUDE.md` → *Number-at-merge*, shared block) names DECISIONS `### #NEXT` **only**; the fix is to extend it to placeholder tokens for FEEDBACK / OPEN_QUESTIONS entries **and `#N`/`§N`/`Q N` refs in code comments/docstrings**. That is a **shared-block** change — byte-identical across both repos (G1) — so it cannot be written from this single-repo session; it must land in `health-app`'s shared block then propagate verbatim to `health-connect-app/CLAUDE.md`. Owner: Luke. Per #112, ROADMAP NOW is the canonical home for cross-repo debt. **STAYS OWED, RE-SCOPED (2026-08-17).** Its three sibling cross-repo rows are discharged; this one is not, and the reason is now mechanical rather than a matter of drafting. **The orphaning is STRUCTURAL.** `#167`'s guard anchors on the store headings `^### #NEXT` / `^## Q#NEXT`, and the claim step's file scope is the stores — so a `#NEXT` written into a docstring or comment is invisible to BOTH: the guard cannot see it, and the resolve sweep never looks at it. A branch that follows the rule perfectly still lands orphaned tokens in source, and lands them GREEN. That was demonstrated again by PR #71's session. **So the fix has two parts, and neither is a wording change:** (a) a **claim-time sweep outside the stores** — the resolve step must enumerate `#NEXT` across the whole tree, not the store set, and resolve source sites per-site; and (b) an **allowlist in the guard's fixtures**, because the guard's own test corpus legitimately contains placeholder tokens and a tree-wide arm would fire on them. This session's branch 1 exercised (a) by hand — 13 sites, 4 anchored in the stores and 9 swept from six source files after verifying those files carried no other `#NEXT` — which is the shape the rule should encode. Note the asymmetry that makes the anchoring non-optional: all three stores carry pre-existing `#NEXT` tokens belonging to other branches, so a file-wide replace there reproduces the PR #71 corruption (55 lines, `#175`); source files carried only this branch's, so a count-verified scoped replace was provably complete there. **RE-EXERCISED AND RE-CONFIRMED (2026-08-18, #220's session).** The hazard is not theoretical and not yet fixed: a blanket `#NEXT` -> `#220` replace across the touched files corrupted **31 lines of pre-existing `#NEXT` prose in `DECISIONS_LOG.md`, 3 in `OPEN_QUESTIONS.md` and 1 in `SCHEMA.md`** — the same failure mode as PR #71, in a session that had read this row. It was caught **before commit**, and only by an audit of the diff's REMOVED lines (`git diff | grep '^-'`) run for `#176(c)`, not by any gate: the guard passed clean throughout, because every corrupted line was prose that no longer matched `^### #NEXT`. Recovery was `git checkout --` on the two stores plus a one-line restore in `SCHEMA.md`, then re-applying the additions with literals. **Two lessons for the fix's shape:** (a) the count-verified scoped replace this row already prescribes must be MANDATORY, not advisory — resolve per-site after asserting the file's total `#NEXT` count equals the branch's own; and (b) the removed-line audit is the only control that actually caught it, so it belongs in the close-out ritual rather than being a `#176(c)` side effect. |
| ~~**Cross-repo:** propagate the shared-block **boundary criterion** and the merge-path split to `health-connect-app`~~ | **DISCHARGED: shared block byte-identical (SHA-256 `5790ae3d527e0a3b7f5a8c0cfebcc560d6ff3e0c96b6b6ce8f111eb8ca737130`), enforcement landed HCA-side (#24/#28/#34); verified 2026-08-17.** The boundary criterion is the rule that decides what may sit inside the compared span at all, so byte-identity is the strongest available evidence that both repos are applying it the same way — a divergent criterion would have produced a divergent block. HCA #33 (`the merge-path section described a repo that no longer exists`) and #34 are the landings; the `land` alias's departure from the shared block, which this row's neighbour flagged mid-sentence, is settled by the same comparison. Struck, not deleted. Historic detail below. **OWED** (`#172`, 2026-08-05). Three shared-block changes to copy byte-identically: (a) a new preamble clause defining **what belongs in the block at all** — a rule qualifies only if its correctness is independent of any surface outside the tree; (b) **number-at-merge restated against *landing*** rather than fast-forward, plus a new bullet naming the resolve→merge window and the re-resolve requirement; (c) the **`land` alias removed from the disposition bullet**, which now states that `stale` stays global because disposition is an invariant while the merging alias is repo-local. **HCA loses nothing and changes no behaviour:** its merge path is unaffected and its `land` stays exactly as it is — the split exists precisely so HCA is not told its only working merge route has changed. health-app's new repo-local `### Merge path` section is **NOT** propagated; it is below `END SHARED LOOP RULES` by construction. **Verify before copying** per `#169`: HCA's block markers and its own heading grammar, not health-app's. Landable only from an **HCA-rooted session**, `pwd`-verified first. Owner: Luke. Per #112 this is the canonical home for cross-repo debt. |

### Interpretation layer — build sequence

_Numbering is organic: it began as a five-increment plan, the producer split into 4a/4b, a declared-state increment was inserted before 4b, and the order was resequenced. Original labels retained; rows are listed in **execution order**, not numeric order. This block is the execution-order detail for the **Interpretation layer build** and **Lab upload pipeline** rows above — those are the sprint-level entries, this is their sequence._

| # | Increment | Status |
|---|-----------|--------|
| 1 | **Interpretation view skeleton.** Static render of the contract example — three sections, group cards, present-marker relations, hybrid weighted-inline levers. No LLM, no producer. | **DONE** — merged, routed behind auth, unlinked |
| 4a | **Producer foundation (phase-free).** `marker_series` newest+prior, delta, gates in mechanical form, group-level surfacing. Range breaches surface un-softened. Proven fixture-as-oracle. No verdict, relations, levers, or endpoint. | **DONE** — merged, library only |
| — | **Declared-state ledger.** Inserted before 4b. Three continuity-aware entry types plus phase derivation, surfaced on `current_state`. The prerequisite for phase-aware interpretation. | **DONE** — merged, seeded live |
| 4b-i | **Producer structural half.** Three-pass restructure (`should_surface` computed after relations), `relations_rendered` with operand degradation, `meta.protocol_context_snapshot` dated to the panel. No demotion, no levers, no verdict, no endpoint. | **DONE** — ff-merged to master (#140–#142); backend suite 453. |
| 4b-ii | **Producer interpretive half.** Relation-based demotion of gate 1's delta arm, `shared_levers` with already-in-play filtering, `axis_verdict`, `mechanism` / `stable_rationale`, `expected_by_phase` (emitted; demotion still to gain authority), the endpoint (draw-triggered per #147), and the view wired fixture→live. Two forks decided here: cache-on-confirm vs compute-on-read, and verdict depth. | **DONE (2026-08-01) — producer complete and DELIVERED.** `axis_verdict` emits the invariant per-group frame (`#154`/`#158`); the endpoint, live view, group as-of and dashboard link landed as 1b (`#158`-`#160`). Historic detail below. Formerly: — split by PROVENANCE into 1a/1b/2 (chat-side correction), then largely built. **DONE:** every MEMBER field in the contract is emitted — `stable_rationale`, `member_lever_effects`, `mechanism` (content authored for all 15 group members, flat `marker_interpretation` slot) and group `shared_levers` (I1-cited, present-filtered, already-in-play via #145); **relation-based demotion of gate 1's delta arm** (#153 — predicate is `feedback` + precondition satisfied + operands complete, never a `crossed_ref` crossing; I8 holds by ORDER, demotion before the safety arm). `axis_verdict` reduced to `{protocol_phase, text}` by **#152** (`verdict` + `confidence` removed from the output entirely). **ONE ITEM LEFT — `axis_verdict`**, on two named blockers: `protocol_context_snapshot.factors` is a LIST while `protocol_phase` is a scalar, so the projection needs a source-factor rule; and the authoring table (keyed on **evaluability**, not truth — 9 of 10 relations carry no machine-readable condition) is not yet minted. Q62 has no current consumer (`text` is authored, not generated). **Then 1b** = fixture regeneration → view corrections (consume `should_surface`, don't recompute; add the Ungrouped section; fix the `protocol_context_snapshot` reader; drop `.verdict`/`.confidence` reads) → endpoint (draw-triggered #147) → wire. Per **#151** the producer completes before any of that. Demotion is inert on the canonical panel (the §2 seed omits `lh`, so its feedback relation is permanently `degraded`) — so the regenerated fixture will ship with the demotion path unexercised in the view; deliberate, since adding `lh` would break the `fsh` oracles. Open: **Q64** (marker-authored fields on ungrouped rows), **Q65** (the other four relation kinds have no `demotes_when`; also gates the verdict table's keying). Nav hub (#150) build-deferred behind this. |
| 2 | **Rephrase pass.** Register setting, backend rephrase endpoint over the deterministic base text, disposable presentation cache, and two evals as pytest: rephrase-may-not-change-claims, simplify-may-not-become-reassurance. | **DONE — #202** (`presentation.py` / `rephrase_validator.py`, migration `c3f1a8b2d9e4`; training-wheels review #1 verified in-DB). Luke's O2 review had flagged the base text as clinically correct but too complex for a layperson ("most people would need an interpreter"); increment 2 simplifies presentation without changing claims, guarded by the two evals. |
| 3 | **Lever tap → scoped education thread.** Structured seed from the tapped lever, opening a scoped ephemeral thread architecturally distinct from general chat. Follow-ups deflect personalised-action questions. | **UNSTARTED** |
| 5 | **Go-live.** Confirm a real panel through extract→confirm, promote the two `ai_draft` asset groups, swap the view-pointer placeholder, reconcile fixture⇄asset drift. First live run against real data. | **DONE (#194).** First live run against the 2026-08-04 draw captured. Dispositions: (a) real-panel confirm done pre-session (#187 path, census-confirmed); (b) assets promoted `ai_draft → human_verified` on Luke's O2 (3 files `_meta.status` + 6 levers `draft_status`); the erythroid group was already active (stale `_deferred` entry removed), the `trt_erythrocytosis_watch` relation stays `blocked_on_contract`; (c) view-pointer already live at #158; (d) fixture⇄asset drift none (deterministic regen byte-identical). Haematocrit 0.50 fired the safety arm at the band boundary carrying band/citations/`contested`, register education not urgency (G4). #51 status is convention-only (Q92). |

**4b-ii is now UNBLOCKED — every prerequisite is discharged.**

- ~~**Ingestion.** The lab store is empty — `lab_reports` and `lab_results` both hold zero rows.~~ **Exercised end to end (2026-05-30 draw, ingested 2026-07-28).** The lab path ran on real data: 7 Sullivan Nicolaides reports, 27 results, **26 of 27 bound to canonical markers on first contact** (the 27th, Total PSA, bound by `feat/ingestion-findings`). The store is no longer empty and the view-wiring half now has a real panel to verify against. Struck, not deleted.
- ~~**Phase-vocabulary mismatch** and **lever→declared-factor join.**~~ **Discharged by `feat/relation-preconditions` (Q56/Q57).** The precondition object (`factor_key` + `admissible_phases`) replaced `on_trt` and the producer resolves it; `declared_factor_keys` joins levers to the ledger. `expected_by_phase` now emitted (no authority); relation-based **demotion** remains 4b-ii's own work, not a blocker on it.
- **Trigger resolution settled (#147):** the endpoint resolves the trigger as the newest `collected_date` (a draw), not a single report — the seven-reports-one-date panel falsified the report-shaped spec. Recorded so the endpoint inherits it; no endpoint built yet.

**Runtime gates — not build stages.** "Gate" is overloaded in this project. Inside the interpretation, every marker passes three independent runtime gates:

| Gate | Tests | Notes |
|------|-------|-------|
| **1 — news** | Meaningful change vs prior, or crossed a reference bound | **Two arms.** The delta arm may be demoted by an in-phase relation. The **safety arm** fires on a band change and is **non-demotable**. |
| **2 — range** | Out of range vs the lab's own per-report bounds | Always fires, never suppressed. Phase may annotate a breach as expected; it never hides it. |
| **3 — safety band** | Level vs an authored policy band | Independent of movement *and* of the reference range: an unmoved, in-range value can still sit in a band. Live for haematocrit as of #139 (three bands); `no_asset` for every other marker until authored. |

A group surfaces if any member trips any gate, which routes it to What Moved rather than the collapsed Stable line.

---

## NEXT — queued

_Live, undated — no external date orders these; pick by readiness._

**Post-1b next lanes (2026-08-02 reconciliation).** `ROADMAP.md` is the single canonical answer to "what's next" (per the canonical-stores table); the committed close-out's next-action and any chat proposal are candidates until promoted here. With 1b delivered, the interpretation lane's sequenced continuation remains increments **2 (rephrase) -> 3 (lever-tap) -> 5 (go-live)** in the build-sequence block above. Separately, the **hub shell (#150)** is now unblocked and is the operator-preferred next pick, with **`lab_accession`** as the strongest small alternative — both queued as rows below. Dated NOW items still precede these.

| Item | Notes |
|------|-------|
| ~~**CBT-I: accept commits an irreversible ledger write with no functional confirm** — from #213's first live run~~ | **DONE → #218** (2026-08-17). Both halves landed on `fix/cbti-accept-confirm`. **#214's two-step:** the offer card's live `Accept and prescribe` is replaced by `Review and accept…`, which fires no request and expands a confirmation restating the actual write — decision, window and lights-out current→proposed, and that it closes the current cycle and resets the evaluation clock (~4 days) — with `Confirm — record prescription` doing the POST and a `Cancel` that fires nothing. **Decision-class gating (the "consider" is now decided, per Q101):** an insufficiency-hold is not acceptable at all — the engine gained a structural discriminator (`CycleDecision.sufficient`, set at GATE 1 and threaded through `replay()` and `CBTIEvaluationOut`), `POST …/accept` refuses such a cycle with a 409 **server-side**, and the client renders it information-only with no accept control in any state. A converged HOLD stays acceptable (a decision-on-merits, and the block's only record of the level it found). Selection unchanged — `complete[-1]` stands. Suites: backend 869→877, frontend vitest 32→41 (the repo's first component tests; jsdom + `@testing-library/react` added as devDeps, declared per-file so the node suites are untouched). rx 12 let stand — an operator matter, not a migration. Struck, not deleted. |
| **Interpretation hub shell (#150)** — BUILT, held for review | 4b-ii closed (2026-08-01), which was #150's sole build-deferral (see the 4b-ii cell above). The other half of the session that opened this review: the "busy main screen" complaint #150 answers with hub-as-home, and the dashboard had since accreted a seventh link, so the problem was growing rather than static. **DONE — merged 2026-08-02 at `001df4c`** as **#162**; all four `#150` rules executed and `Q63` resolved to structural counts. Its `#NEXT` was not resolved pre-ff and rode master for three sessions as a live placeholder; healed here. Outstanding: the `#116`/`#121` frontend deploy probe, never run. See `BRANCHES.md` and `closeout.md`. Owner: Luke. |
| **Persist `lab_accession`** — small lane | `lab_accession` is parsed at extract and discarded at confirm; one column unlocks report identity, a dedupe key above the result rows (`Q68`), and episode scoping (`#159`'s do-not-revisit dependency). Owner: Luke. |
| **Canonical-map: `Bilirubin conjugated` + `CK`** — small lane, from go-live census (#194) | The first live draw (2026-08-04, `_ROUTINE CHEMISTRY`) bound 16/18; these two failed canonical binding (`marker_canonical` NULL) and sit outside interpretation groups. Both are discriminators that matter *for this user specifically*: conjugated bilirubin is the Gilbert's-pattern discriminator behind `bilirubin_total`'s `stable_rationale`, and CK is the muscle-origin discriminator for the recurring AST-elevation pattern — CK is already the `ck_muscle_discriminator` relation parked in `marker_groups._deferred._blocked_on_order` ("never ordered"); it *is* now ordered (CK 394, above range), so a `marker_canonical.json` addition both binds the result **and** can unblock that discriminator. Same class as the urine-ACR mapping fix. Owner: Luke. |
| ~~**Hevy: gate the create pre-check on catalogue freshness** — small lane, from #211~~ | **DONE → #212** (2026-08-13). `_process_exercise_actions` (`routers/chat.py`) now calls `refresh_catalogue_if_stale` once, before the honest-confirmation idempotency pre-check, so a chat-initiated `<hevy_create_exercise>` with no recent workout fetch resolves against a fresh catalogue — closing the create-path window (c) left open. Reuses #211's per-user staleness marker; no scheduler. Q75 stays DONE → #211 (not reopened). Struck, not deleted. |
| **Hevy: user 5 (Cooper) syncs zero customs** — ~~investigate~~ **DONE → OPEN_QUESTIONS Q100** (2026-08-12) | Investigation complete via read-only prod probe: user-id mapping correct (`user_integrations` row id 11 is genuinely Cooper's — H1 ruled out); stored key SHA-256 (first 12) unique vs Luke/Deb (copied-key form of H2 ruled out); the account behind the stored key has **0 customs AND 0 workouts** (`GET /v1/exercise_templates` → 451 defaults / 0 customs; `GET /v1/workouts/count` → `{"workout_count": 0}`). Cannot distinguish stale-belief (a) from wrong-account (b) from our side — Hevy exposes no whoami and 0 workouts leaves no in-band identity signal. Now tracked as **Q100**; resolution needs Cooper (confirm the key's account, re-issue if wrong). Deb (user 4) validated the multi-user path (customs 10). Owner: Luke. |
| **Engine: `restrictions[]` is dead data — the contraindication design pass** — from the Q23 audit (#216) | **OPEN_QUESTIONS `Q102`.** `is_contraindicated` reads `restrictions` for exactly one thing (the `ra_flare` alias); every other restriction string the injury ledger carries is inert. Blocking runs on `signal_type` + set membership + a `body_part` substring, which cannot express what the entries say — so entry 29 is typed `mechanical` to stop the engine blocking its own desensitisation lane, `"pes anserine"` escapes the `knee` substring, and `"heavy gripping"` restricts nothing. Also carries a LIVE over-block (`lunge_single_leg` contraindicated bilaterally against an actively-trained region, with no expiry despite the docstring's "time-limited" promise) that #216 deliberately did NOT patch: an ad-hoc removal trades a known over-block for an unknown under-protection. Shape: restriction-vocabulary → region-key mapping, plus `signal_type` refinement or per-entry overrides. Owner: Luke. |
| **Marker display-name polish (acronym-in-brackets)** — small lane, from go-live O2 | The view renders marker names as `marker_name_raw` (`producer.py:105` "polished names are 4b" — an explicit deferral). Luke's go-live O2 ask: show the full name with the acronym in brackets, e.g. `Aspartate aminotransferase (AST)`. Shape: a `display_name` field per entry in `marker_canonical.json`, producer reads it instead of the raw name; deterministic, no LLM. Discharges the deferred polish. Owner: Luke. |
| **Selectable term definitions / glossary** — new education feature, from go-live O2 | Luke's go-live O2 ask: make scientific terms (`cholestatic`, `haemolytic`, …) selectable to show a definition — the interpretation prose is precise but assumes vocabulary a layperson lacks. New feature, shape undecided (definition source; which terms are selectable; inline tooltip vs side panel); kin to increment 3's scoped education thread. Education-register only (no personalised advice, #47). Candidate for its own increment or a fold into inc-3. Owner: Luke. |
| **CBT-I user surface (interim)** | The titration engine is built (backend `cbti/`, #114/#115/#117/#118) but CBT-I is invisible in the app — no route/page/nav. **Gating fork is #47, not display hygiene:** the engine's *state* (current prescription: window/lights-out/anchor; cycle position) is an education-safe read-back, but its **MOVE/REVERSE verdict** surfaced as a directive is the AI layer giving a clinical instruction — resolve #47 (show state-only vs show the action) BEFORE building. Second fork: diary capture (operator-script vs in-app; in-app re-opens Q45). I1 sensor firewall (recall-diary only, never Samsung passive sleep) is a hard constraint but a projection-level one, not the gate. See OPEN_QUESTIONS **Q60**. Owner: Luke. |
| CBT-I module phase 2 (engine + surfaces + ISI) | Phase-1 substrate landed on `feat/cbti-module` (held for review, DECISIONS_LOG #107/#108/#109): schema + completed-block import (1 block, 9 prescriptions, 53 nights, SE-reconciled). Phase 2 = titration engine (weekly eval; sufficiency/regularity/adherence gates; TST-plateau exit with SE≥85% as a floor; **replay against the imported block = Gate 5**); AM/PM surfaces with the 12h-clock prefill sanity-gate (Q42); ISI 7-item capture. Separate brief. Confirm the VA nap-timing convention before the engine relies on the `naps_min` date−1 read. **LARGELY LANDED (2026-07-25):** engine (#114/#115), capture surfaces + block 3 opened live (#117/#118/#119), and **ISI capture/storage** (table `cbti_isi` migration `d3f7a1908c62`; block 3 baseline backfilled — corrects the earlier stale "ISI not captured" framing: it was captured 24 Jul, the gap was storage, now closed). Still open: PM prescribed-lights-out display and the manual evaluation trigger (#118's unbuilt half). **Remaining piece — the manual evaluation trigger — is promoted to a dated NOW row (~31 Jul); this row is now programme history.** |
| Scraper canary + honest score degradation | Detect null/stale/implausible scraper output. Surface degraded state to user when HRV unavailable — never silently score without it. |
| Basic readiness score → **Banister build (OWED)** | **Suppression lifted 2026-07-29 — the HRV-data precondition is met.** This row previously suppressed the metric *until* the HRV path was confirmed end-to-end with 7+ days of readings; that precondition is now satisfied and **Code-verified against Railway Postgres this session**: `samsung_hrv_readings` holds **47 distinct `passive_overnight` days** (2026-06-08 → 2026-07-29, every `hrv_ms` non-null; zero `calibration`/`session` rows), of which **32 have propagated to `daily_records.passive_hrv_ms`** — the consumed end-to-end value. **This does NOT mean readiness exists — it unblocks the *build*.** What this row now tracks is the still-owed **Banister fitness-fatigue model** (Form = Fitness − Fatigue, dual EWMA) integrated with RMSSD baseline deviation, sleep architecture, and RHR trend. `naive_baseline` exists and is displayed; `model_forecast` / `model_confidence` exist on `DailyRecordOut` (`checkin_v2.py`) but the model behind them is unbuilt. **Separate, still-live constraint — do NOT read as lifted:** `context_builder._section_daily_record` keeps the model forecast low-confidence "until it demonstrably beats the naive baseline on this user's data" (`context_builder.py:368-369`) — that governs the *model*, not the data path, and stands. ACWR rejected — see Decisions Log. Owner: Luke. |
| Manual cardio entry | Unconnected sessions (Rogue Echo bike, gym machines) must be loggable to prevent ACWR silently under-reading load. |
| Deploy companion app to wife's phone | Garmin → Health Connect path. Verify data flowing before deploy. |
| Supersede DECISIONS_LOG #3 | Polar not session-only, AccessLink live, SDK R-R as highest-fidelity HRV path. Blocked on a *How you know* artifact (Polar R-R verification). |
| HCA forwards writer identity (HCA session) | Forward `dataOrigin.packageName` + an HC `health_data_category_priority_table` snapshot in the `/health-connect/sync` payload. Producer half of the #36/#37 wire contract; source dedup arbitration now lives backend-side, so `validateNight()` becomes a faithful relay. |
| Backend F1 filter (backend session) | Apply source-priority dedup over `health_connect_record_sources` (built in #37). **F1 STAYS GATED — gate only PARTIALLY discharged (corrects `#188`'s over-broad "row above likewise discharged", 2026-08-09).** Row 73 carries **two** deliverables and only the first has arrived: **(1) `dataOrigin.packageName` — discharged 2026-07-05**, HCA has forwarded it since the **05:51:53Z cutover** and the live table carries real packages (`#188`); **(2) the HC `health_data_category_priority_table` snapshot — NOT arriving, no landing place in the backend** (verified 2026-08-09): no field on the `/health-connect/sync` payload model, no ordering constant, and the only backend `priority` matches are the three prose comments naming F1 as *source-priority dedup* (`models.py:222`, `routers/health_connect.py:515`, migration `c9b8a7d6e5f4:10`). Source-priority arbitration has **no priority order to arbitrate by**, so F1 remains gated on (2). Row 73 itself is left as written (HCA-repo producer work, unseeable from here). **Open fork — Luke's call, decide nothing here:** whether F1 should want HC's category-priority table *at all*. A backend-authored ordering (e.g. Polar over Samsung for workout HR) would discharge row 73 by making the snapshot **unnecessary** rather than by satisfying it — a distinct resolution, not the same dependency deferred. Measured arbitration load: **650 genuine heart_rate conflict groups** with unknowns excluded — **not** the **10,406** a naive `COUNT(DISTINCT source_package) > 1` returns, ~94% of which is the cutover's `'unknown'`/identified twins rather than real multi-writer conflict. Also unblocks F3a (frozen-session-set aggregation) once landed. |
| **Security:** identify the second credential digest in the transcripts | **OWED — cheap, and it separates a finding from an unknown.** A second credential-shaped digest (`9688f2…`) appears in 4 session transcripts alongside the now-rotated Railway Postgres credential. Co-occurrence test: if it appears **only** in transcripts that also carry the rotated credential, it is almost certainly that credential's predecessor and is dead twice over. If it appears **independently**, it is a distinct credential nobody is tracking and its liveness is unestablished. A few minutes' work, digests and counts only, never values (#111). Until it runs, "open by choice" and "possibly a live credential" are both true of the same item, which is the thing that shouldn't hold. Recorded here rather than in Q44's body — Q44 is `DONE → #111` and closed questions are not scanned for live work (#112). Fold into the next phase-2 session. Owner: Luke. |
| **Guard:** a canonical-surface consistency test — three comparisons, one mechanism | **OWED.** Two canonical surfaces must agree with their source and **nothing enforces it**; both divergences so far were caught by a human-directed VERIFY inside a brief, which is not a mechanism. The never-lag rule says surfaces must not lag and provides nothing that would notice if they did. **(a) SCHEMA.md vs `models.py`** — four columns from migration `c4e8a2019bd7` (`basis_n_samsung`, `basis_n_diary`, `basis_n_alcohol_unknown`, `basis_tib_over_run_min`) sat in `models.py` and absent from SCHEMA.md until a VERIFY happened to look; an omission when the migration was folded in, and folds recur. **(b) CLAUDE.md conventions vs `DECISIONS_LOG` entries** — *presence* is verified (every `(standing, #N)` citation resolves to a real entry; negative control: a fabricated `#999` returns 0) but *content* is not. A convention can say more than the entry it cites, which reads as authoritative and is not. Live instance: the corrected-doc clause at `CLAUDE.md` L247 is marked `POSTDATES #113` — locally truthful, but that annotation pattern permits **unbounded cumulative drift**, since each note is individually visible while the total distance between a convention and its backing entry is visible nowhere. A content comparison is what would surface it. **(c) Samsung-context filter convention vs its call sites** — the CBT-I read allowlists `context == 'passive_overnight'` (`cbti/replay.py`); the two readiness reads in `checkin_v2.py` (lines ~94 and ~204) instead *denylist* `context != 'session'`, which admits `calibration` readings into the passive snapshot and the HRV baseline — non-resting values contaminating a resting baseline. Modest, real, out of the surfaces brief's scope, and found in passing exactly like the `mcp_server.py` `Session` bug (row above). It is the drift the guard exists to catch: an established convention with no mechanism noticing a call site that departs from it. A content comparison (do all Samsung-context reads share one filter?) is what would surface it. Do **not** tighten the denylist inside a feature branch — it changes readiness behaviour; it lands with the guard, or as its own scoped change. **The detector already exists** and was demonstrated on (a) with a negative control — a fabricated column name must report LAG, proving the check detects absence rather than always passing. That control is the part worth preserving; without it the OKs mean nothing. Separate concern from CBT-I surfaces, so not that branch. Owner: Luke. Recorded here per #112. |
| CPAP mask-off events as an objective nocturia instrument | The waking-cause columns (`wakings_nocturia_n`, migration `b2d5f9e04a17`) rest on self-report — 3am recall of *why* one woke. The CPAP (ResMed AirMini) records mask-off events with timestamps, in-app for 30 days via AirView. A mask-off in the night is an objective correlate of a nocturia trip (the mask comes off to get up). Cross-referencing mask-off timestamps against `wakings_nocturia_n` would give the self-reported count an objective check — and, with PVR 229 mL on record and no urology relationship, help distinguish a behavioural titration stall from a urological one (Q47's neighbour on the "why is the window not opening" question) without relying on recall. No integration exists: the AirMini has no SD card and no OSCAR path; data is in-app only. Filed after block 3's first night showed a single 03:40 nocturia trip against an ISI with severe *maintenance* difficulty and zero *onset* difficulty. Owner: Luke. Recorded per #112. |
| Fix Health Connect permissions | Companion app returning errors for record types 38, 35, 11, 37. Partially resolved via adb pm grant; proper in-app dialog fix still needed. |
| Samsung Health package name correction | Re-run Health Connect diagnostic with `com.sec.android.app.shealth` filter (not `com.samsung.health`). Verify via Railway Postgres query, not on-device UI. |
| Morning check-in screen | Hooper Index pattern (fatigue, sleep quality, stress, soreness). Primary daily touchpoint. Mutable post-submission with audit trail. See Ideas file for DOMS/soreness split design. **Step-5 stale check (2026-07-25):** the core screen IS built — `CheckInAM.jsx` carries the full Hooper set (fatigue, sleep quality, `life_load`=stress, soreness from active injuries) and is routed. Gaps that keep it live: mutation is supported (pre-PM re-entry) but there is **no audit trail**, and there is **no DOMS/soreness split** (soreness is per-active-injury). Stays with both gaps named. **Read-back shipped (2026-07-29, `feat/frontend-readback`):** the PM done-state now shows the evening's submitted values, and a new `/checkin-history` page (over the existing `GET /checkin-v2/history` — frontend-only) shows AM + PM per day. NB the morning form is re-enterable (mutable pre-PM), so the AM read-back is the history page, **not** an AM done-state recap. The two live gaps — **audit trail** and **DOMS/soreness split** — are unchanged; edit-post-submission is still owed. |
| Persistent conversation history | Currently clears on browser refresh. Needs backend storage + frontend state management. |
| Session cards not clickable | UI bug — session cards in workout view not responding to click |
| Dual-panel scroll layout issue | UI bug — scroll behaviour broken in dual-panel view |
| ~~`mcp_server.get_hevy_workouts` references unimported `Session` type~~ | **DONE** (`feat/frontend-readback`, 2026-07-29) — one-line `from sqlalchemy.orm import Session` added. **Correction:** it did **not** "raise `NameError` at call time" — `db: Session = SessionLocal()` is a LOCAL annotation, which CPython never evaluates at runtime (verified empirically: no NameError), so the tool worked all along. A correctness fix, not a live-bug fix. Struck, not deleted. |
| Sleep duration displays `total_sleep_time_minutes`, which is the in-bed span, not sleep | **SEMANTIC error, not cosmetic — classification corrected 2026-07-24.** Three sites read `total_sleep_time_minutes` and fall back to `actual_sleep_time_minutes`: `context_builder.py:601`, `routers/recovery.py:67`, `HealthPanel.jsx:92`. Originally filed as a `total_ ?? actual_` *presentation inconsistency with no behavioural effect* — that judgement predated knowing what the fields mean. The CBT-I Step-3 probe settled it: across 31 real `passive_overnight` nights, `total_sleep_time_minutes` sits at the clock window `(wake − bedtime)` (remainder median 0) while `actual_sleep_time_minutes` is scored sleep (window − actual median +35). So `total_` is **time in bed**, and these three sites display TIB **labelled as sleep duration** — inflated by ~30–45 min. The correct field for "sleep duration" is `actual_sleep_time_minutes`; `total_` is a TIB/SE-denominator quantity. Fix is a field swap at three sites (mind the `??`/`or` fallback direction). Does **not** clear the bar to interrupt the CBT-I build; still ROADMAP. Corroboration it matters: tonight's 6h00 prescription was derived from `actual_` — the correct scale; `total_` would have prescribed nothing. Owner: Luke. Recorded here per #112. |
| **Status tooling: cross-repo diff engine (manually-invokable report)** — continuation of the status-reporting plan | Next step after the reader unification (`#207`) that closed `#191`'s two-reader divergence. `#190`–`#193` landed the parser gate + snapshot baseline (Steps 1–2); `#207` unified the machine model + digest readers (per-repo question vocab, HCA `·` channel, decisions best-effort `unstated`). Remaining: a manually-invokable cross-repo status diff/report over the snapshot model. Chat-brief-driven, not yet a decision; this lane is INSTRUMENT — the product lanes above (interpretation inc-2 rephrase, inc-3 lever-tap, lab-pipeline small lanes) stand ahead of it by readiness. Owner: Luke. |
| Adaptive programming lane: Plan schema (steps 2–4) + taxonomy v1 (Q27) | Framed by #75 (Plan WRAPS the engine: cycle/slots/cardinality are Plan-owned; probe/fortify region selection stays engine-side — the frame constrains this work, do not re-architect the engine it wraps). Two workstreams, internal order undecided: (a) Plan schema, steps 2–4 of the exercise-catalogue sequence — composes engine output into programmed sessions; (b) capability-taxonomy v1 per Q27 — v0 has no joint-level strength/ratio axis, so e.g. a tibialis raise (dorsiflexion STRENGTH; `ankle_df` is a mobility screen, category error per #76) cannot be selected until the vocabulary exists. Q27 names the external grounding (ER:IR, adductor squeeze, LSI); nothing gates starting (b). Entry preconditions discharged: engine live (#21), custom-exercise creation live end-to-end (#163/#164/#166). Target: offseason Block A (~Sep onward) — chat-adjudicated placement 2026-08-12; dated NOW rows and interpretation inc-2/3 precede by priority, not dependency. Owner: Luke. |

---

| **SCHEMA.md is stale for the entire lab family** | **OWED.** `CLAUDE.md` makes `SCHEMA.md` repo-canonical and says it "must never lag master; a canonical-but-stale schema doc is worse than a stale project-knowledge copy." It lags, and not by one column: it documents a **superseded** design — `lab_results` hanging off `lab_panel` **events**, plus `marker_aliases` and `unknown_markers` — none of which exist. The implemented `lab_reports` / `lab_results` pair (#52) has never been in it, and **no lab migration has ever updated it** (`c655bd6`, `005c1a6`, `7753758`, `d7c4b1a90e35` all touch `models.py` and none touch `SCHEMA.md`). Discovered while landing `zero_row_reason`: the rule could not be honoured because there is no table entry to update without first authoring the whole family. Fixing it means rewriting the lab section against master, not appending a column — a doc task with its own scope, deliberately not absorbed into a data-loss or interface branch. Owner: Luke. |
| ~~**Hevy custom-exercise creation (step 2)** — blocked on this session's prod gate~~ | DONE — gate and build both. Step-1 prod gate closed at #163 (Railway catalogue verified, 494→499). `<hevy_create_exercise>` landed at #164 (explicit separately-confirmed block, processor-side idempotency re-check). Response-tolerance fix (#166) confirmed a live create in production. Q75 (freshness beyond connect-seed) **RESOLVED → #211** (2026-08-12): sync-on-workout-fetch, staleness-gated on a per-user marker. Struck, not deleted. |

## LATER — planned

_Unchanged from before this triage._

| Item | Notes |
|------|-------|
| Injury object schema in project files | Schema and extraction method drafted in Ideas. Formalise into Decisions Log and API contracts once morning check-in screen is built. |
| Preset readiness models by sport/goal | Rugby vs endurance vs strength — different metric weighting presets |
| User-adjustable metric weighting | Let users tune what matters to their readiness score |
| AI-personalised model | After ~6 weeks of data per user; Claude infers pattern from history |
| GameTraka connector | Rugby performance data for Luke |
| Apple Health (son) | iOS path; requires either Expo iOS build or separate native integration |

---

## User rollout sequence

1. **Luke (Easty)** — primary dev user; Samsung scraper working; Health Connect partial
2. **Wife** — Samsung Galaxy + Garmin; needs companion app deployed and data flow verified
3. **Son** — iOS; future phase

---

## Dependencies and blockers

| Blocker | Blocks |
|---------|--------|
| Health Connect permissions fix | Polar and Garmin session data |
| Conversation history persistence | AI coaching continuity across sessions |
| Wife companion app deploy | Wife onboarding |
