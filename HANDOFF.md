# HANDOFF — interruption-survival ledger

One append-only ledger of session handoffs, so a session interrupted mid-crossing is
resumable from the repo alone — without reconstructing state from chat scrollback. One
ledger, not two.

**Append-only. Never edit or delete an existing line** — only add new lines, at the top of
the list below the header (newest first).

Format: `YYYY-MM-DD HH:MM AEST | LANE | one line`
Lanes: `CHAT→CODE` · `CODE→CHAT` · `CHAT` · `CODE` · `LUKE`. Code writes every entry.

The `CHAT→CODE` receipt is written before any work begins and states received-not-started —
that entry is the one that survives an interruption, so it must not wait for the work. Other
entries may ride the next substantive commit.

**Post-close-out agreements get a receipt at the moment of agreement (#98).** An agreement
reached *after* a close-out is written has nowhere to live: it is not in a brief, not in the
close-out already on disk, and the next brief carries it only if someone remembers. Q37 is the
worked example — flagged at #95's close-out and again before #96's merge, agreed both times,
and it still took a third session to land, because a "yes" between sessions had no home. This
is the step-0 breach from the opposite end: the receipt protects work in flight, and this
protects an agreement in flight. Write it as a `CHAT→CODE` line when it is agreed, not when it
is next actioned.

**Widened at #102 — any cross-lane instruction, including a go, is written here or it did not
happen.** An instruction is not an agreement, and a merge authorisation had no surface: the go for
`feat/erythroid-constants-and-lever` was given explicitly in chat with the bytes verified, never
reached Code, and the branch sat unmerged for a session while the next brief was written against a
master state that assumed it had landed. Third instance of one shape after Q37 and the post-close-out
agreement — each fix scoped to the exact form that had just failed, each next failure one step outside
it. Hence the widening to the general category rather than to "goes as well".

---

2026-09-20 12:30 AEST | CODE | **#315 built + governance banked, landing on green (self-merge, non-schema).** S1/S2 validator (exactly-one-of-three; `device_sports` REQUIRED on load_window + activity, refused on capacity; open vocab, casefolded match); S3 resolver claim order (exclusions → activity by sport declared-order → sport-scoped load_window; one session ≤ one slot; `unclaimed_session`/`no_sport`; due_slot spans kinds, due_capacity capacity-only); S4 surfaces (chat position + `QuotaWindow` render activity + "other activity"; #312 consistency counts `satisfies:{activity}`); S5 `expected_load` "none" + `satisfies:{activity}`. Reader sweep: validator/resolver/chat/consistency/frontend handle `kind="activity"`, current_state passthrough + /engine/next capacity-only unchanged. Divergence flagged at G0 (device_sports G1-optional vs S2-required → ruled REQUIRED). Tests: `test_activity_slots.py` (26) + `QuotaWindow.test.jsx` (+3); existing load_window fixtures updated for the S2 requirement. Full backend 1829 passed (bar the shallow-clone `git show 3360ed5` artifact, green on CI — activity render branches input-gated, satisfies-docs inside the STEP1–STEP3 parity excision); frontend 198. Governance: DECISIONS #315, Q164, ROADMAP (activity slots v2 DONE), BRANCHES #315 row, SCHEMA (microcycle validator + satisfies), CLAUDE Recent-landings rolled to #315/#314/#313 (healing a stale #308/#307/#306 pointer). No new FEEDBACK — the divergence was flagged at the gate, not breached.
2026-09-20 11:30 AEST | CHAT→CODE | **G0 rulings received (activity slots v2) — building S1–S5, self-merge on green (non-schema, confirmed S0(d)).** RULINGS: (1) Pilates → its own `activity: pilates` slot, `device_sports ["Pilates"]`; the premise correction is accepted+recorded (pilates never counted toward `stability`; the 3/2 was three Hevy rows incl. the since-excluded phantom). (2) Walks → UNCLAIMED (declared-vs-unclaimed is a per-phase microcycle choice, not code); code's part: the `unclaimed_session` reason renders as **"other activity"** in QuotaWindow and the chat position — visible, never counted for/against. (3) S2 prod query returned **0 rows** → no live `load_window` phase → **PROCEED**. (4) VOCAB CORRECTION — HC enum alone is not enough; conditioning is on the Polar H10 and Polar rows carry Polar's own sport names (prod shows "Fitness" and a NULL). (a) `device_sports` validates against HC title-cased `ExerciseSessionType` names ∪ the Polar sport vocabulary; **Polar is open-ended → validate as NON-EMPTY STRINGS, do not invent a closed set** (report the observed Polar distinct names at the gate). (b) a canonical session with NULL `sport_name` matches no sport-scoped slot → `unclaimed_session` detail **`no_sport`** (visible, so the operator can fix at source); fixture required. (c) matching is **exact, case-insensitive**; no fuzzy/category. STANDING READER-SWEEP GATE: list every reader of the microcycle JSON + the resolver slot output (panel, chat position, #312 consistency line, MCP tools) and show each handles `kind="activity"` — the #312 consistency line MUST count `satisfies: {activity: …}` items. GUARD: resolver decides MEANING, never the load model (no load-side change); no sport inferred from HR/duration/schedule; no floor/coefficient. Mint DECISIONS #315, Q164 (load treatment of activity sessions once stage-2 zones exist — a walk worn watch WILL produce HR samples → stage 2 would zone it unless the sport is excluded; a LOAD-MODEL decision, #302, not made here). Maxima by identity: decisions #314 → #315, questions Q163 → Q164 at merge. Received, building.
2026-09-20 10:30 AEST | CHAT→CODE | **Activity-slots v2 brief received — DEPENDS-ON gate PASSES (both merged: `schedule_item.satisfies` #312, HC-workout ingest stage 1 #309), so S0 proceeds. STOP at G0.** Drafted against `222c221`; adjudicating against master `821f586` (#314). Objective: a THIRD slot kind `activity` (evidenced by a device-recorded canonical aerobic session whose sport ∈ the slot's declared `device_sports`, depositing NO load by virtue of the slot); sport-scoping `load_window` (S2, REQUIRED — a recorded WALK must never satisfy a conditioning quota; replaces the duration-floor idea with a declaration); `satisfies` accepts `{activity: <name>}`; `EXPECTED_LOAD_VALUES` gains `"none"` (zero-load schedule items, S5). Claim order (S3): concurrent_strength exclusion → activity slots by sport (declared order) → remaining eligible for load_window; a session matching nothing → `uncounted` reason `unclaimed_session`; one session ≤ one slot; Rule 4 spans all three; `due_capacity` still capacity-only. **S0 probe (STOP): (a) reconcile the wk-of-14-Sep `stability 3/2` — the operator says gym×2 + pilates×1 (pilates in Samsung Health not Hevy); (b) exact `sport_name_for` strings for HC pilates/pool-swim/walk; (c) `EXPECTED_LOAD_VALUES` references that would mishandle a 4th value; (d) confirm non-schema (microcycle JSON + validator only) → else HOLD.** S2 confirm at S0: NO phase has ever opened with a `load_window` slot (if one exists, STOP — requiring `device_sports` would break it). **A Health_app_data MCP connector is now available** (get_hevy_workouts / get_training_sessions / …) — may answer the prod probes (a)/(b) that were unseeable before; flag operator-owed whatever it can't. GUARD: resolver decides MEANING, never alters the load model (no load-side change); no sport inferred from HR/duration/schedule; no floor/coefficient. LOG: DECISIONS #NEXT, Q#NEXT (load treatment of activity sessions once stage-2 zones exist — operator's position: walking is NOT metabolic load; a LOAD-MODEL decision, not made here). Maxima by identity: decisions #314 → #315, questions Q163 → Q164 at merge. Branch restarted from master `821f586`. Received, running S0.
2026-09-20 09:30 AEST | CHAT→CODE | **G0 rulings received (coach routine read/update) — building S1–S4, self-merge on green (non-schema: in-process cache + a preference-type declaration, no table).** RULINGS: (1) full detail = routines in the folder DECLARED for the CURRENT phase; all others by name only. **S0 addition — the declaration lives in a `type=preference` knowledge entry mapping phase→Hevy folder id (NO migration; a column on the phase row would be schema → avoided). Until a declaration exists, fall back to most-recently-USED folder and SAY the fallback is in use.** (2) full-detail cap ~2500 tokens; over it, most-recently-updated first, NAME the overflow by title (cap) — never silently drop. (3) **PUT /v1/routines/{id} has NO folder_id and there is no move endpoint (a sent folder_id is silently ignored — three independent clients + the OpenAPI spec agree):** `update_routine` never sends folder_id; the coach never claims to move a routine; a move request → "can't via API, a few seconds in the Hevy app." **Folder is set at CREATE only — create_routine places new routines in the current phase's declared folder, creating the folder first (POST routine_folders) when the phase has none.** (4) S0(d) CONFIRMED: compact index of all + full working set; **short-TTL (5min) in-process cache, invalidated on any coach create/update; 3s hard fetch budget inside the turn; on timeout/error serve the last cached copy marked with its age, else say routines unavailable; chat never waits on Hevy nor goes down for it; add basic 429/timeout handling to the connector calls used.** (5) Double-footer = display duplication (accepted): **strip model-emitted lines matching the deterministic footer grammar from the KEPT prose before the real footer is appended; add a protocol sentence (the coach does not write save-confirmation lines, the system appends them); guard test — one reply = exactly one write per block AND exactly one footer. Do NOT strip footers from conversation HISTORY (the coach needs to know what saved).** Mint **Q163** (a real tool runtime for the in-app chat — GUARD: don't build). ROADMAP note for the queued Hevy-DELETION brief: the official Hevy API exposes a paged workout-events feed of updates AND deletes since a date → that brief's S0(a) = YES. §45 applies: the `<hevy_update_routine>` instruction sits beside its documented schema (worked example, generated from the processor's definitions). FEEDBACK candidate: a prompt sentence naming a capability is a runtime claim needing a test. Maxima by identity: decisions #313 → #314, questions Q162 → Q163 at merge. Non-schema → self-merge on green. Received, building.
2026-09-19 13:00 AEST | CHAT→CODE | **Brief: the app coach can READ and UPDATE Hevy routines — received, S0 probe, STOP at G0. Drafted against `222c221`; adjudicating against current master `9e803e0` (well ahead — #305–#313 since).** Objective: the coach sees the athlete's Hevy routines as they are and can change one IN PLACE, so no one fakes a workout to show a routine (the phantom-workout cause of the resolver 3/2-vs-2/2 defect) and adjustments stop spawning undeletable duplicates. STEPS: S1 read (`## Hevy routines` — compact index of all + full contents of a bounded working set, from the SAME formatters the MCP server uses, MOVED to a shared module not copied; fetch failure isolated). S2 make the prompt true (remove the "you can call `search_hevy_routines`" claim — the in-app chat has no tool loop; grep the prompt for MCP tool names, assert none). S3 update via `<hevy_update_routine>` (id + COMPLETE contents since the API REPLACES; confirm-first; exercise-ids validated; rpe stripped; per-exercise diff vs a pre-write REFETCH not memory; stale updated-at → refuse+re-show; connector gains `update_routine`). S4 coach instructions (update existing not new; never ask to log a workout to show a routine; no weekday in a routine title — shared with the week-planner brief, first to land owns it). **S0 probe (STOP): (a) confirm 4 tree claims; (b) user-1 routine/folder count + compact-all vs full-one token cost (prod-owed where unseeable); (c) Hevy routine UPDATE endpoint + replace semantics — READ the API docs, don't assume; (d) `get_all_routines` latency/ratelimit (pageSize cap 10) — live per-turn vs short-TTL cache; prefer NO new table, if one is genuinely needed → schema → HOLD.** **CARRY-OVER (fix in THIS PR — same reply pipeline): the write-result footer rendered TWICE on one turn (prod 19 Sep, three schedule_item rewrites → "✓ 3 saved" block twice). Determine at G0 whether `_process_knowledge_updates` ran twice or only the footer rendered twice; if writes double-ran → fix + a test that one reply = exactly one write per block.** Standing rules: FEEDBACK §45 (the `<hevy_update_routine>` instruction sits beside its documented schema, worked example, generated from the processor's own definitions); §45-class S2 (no prompt sentence names a tool the runtime can't call). Provisional G0 ruling 1 (operator may change): full detail = most-recently-used folder(s), every other routine by name only. GUARD: no tool-use loop here (raise a Q#, don't build); no routine delete; no silent overwrite; no write without athlete confirmation. Maxima by identity: decisions #313 → #314 at merge, questions Q162 → Q163 if the tool-runtime Q is minted. Branch restarted from master `9e803e0`. Received, running S0.
2026-09-19 12:20 AEST | CHAT→CODE | **Follow-up to #312 — the write-protocol docs don't teach `training_plan`/`satisfies`, but S5(iv) instructs the coach to write both. Non-schema, self-merge on green. Received, building.** FINDING (chat's omission in the #312 brief, not a Code narrowing): the `<knowledge_update>` schema the coach learns write shapes from (`_section_user_profile` SCHEDULE INTELLIGENCE, context_builder) still lists `schedule_item | load_context | injury | preference` and never states the `training_plan` key/shape or the `satisfies` field — the coach is told (S5(iv), #312) to propose `training_plan` rewrites and set `satisfies`, without being told HOW. FIX: document BOTH shapes in the ONE write-shape home, with a worked example each, field lists GENERATED from `TRAINING_PLAN_FIELDS` and `_SATISFIES_VALIDATORS` keys (imported, not re-typed → cannot drift from the validators); training_plan rewrite sends the COMPLETE macro (supersede, not append) changing only what the athlete asked. GATES: (a) every `TRAINING_PLAN_FIELDS` field + every satisfies kind appears in the rendered protocol text (generation makes this drift-proof; the test is the standing guard against a future hand-rewrite dropping a field); (b) end-to-end — a `<knowledge_update>` block in the documented training_plan shape passes `_process_knowledge_updates` and lands a current row; (c) end-to-end — a schedule_item linked via `satisfies`. FEEDBACK standing rule: a prompt sentence that instructs a write must sit beside the schema for that write. Branch restarted from master `7cdae2b` (post-#312). Maxima by identity: decisions #312 → #313 at merge, questions Q162 (no new Q). Received, building. **MID-TURN ADDITIONS folded into the same PR (#313):** (i) prod evidence — the coach's `training_plan` save under key `offseason_2026_27` was correctly refused (finding confirmed, not hypothetical); (ii) the write-result footer was type-blind ("Schedule entry NOT saved" for a training_plan) → `routers/chat._entry_noun` makes it type-aware, tested for training_plan/injury/schedule_item; (iii) protocol text now states the macro is stored VERBATIM (partial change edits only affected lines) — behaviour, asserted as text; (iv) a raw `<thinking>` block rendered to the user in prod → `_strip_thinking` on the final reply, tested.
2026-09-19 11:35 AEST | CHAT→CODE | **G0 rulings received (plan-of-record + schedule↔quota link) — building S1–S6; self-merge on green (non-schema, confirmed at S0(a)).** RULINGS: (1) `macro` bound **4000 chars** (operator's reconciled core = 3,822; 2000 would cut the buffer rule / sacrifice order / knee-gate fallback — the point); reject-on-write above it with the **measured length in the error**; ~1000 tok/turn accepted. (2) Fixed key **`"training_plan"`** confirmed + keep belt-and-braces rejection of a second active row under any OTHER key. (3) `expires_at` MUST be null (supersede-by-rewrite only). (4) `revised_by` closed set `operator | coach`. Onboarding-suppression ruled (benign; any entry already suppresses). **S4 derivation CORRECTED by operator:** (a) `scheduled := sessions_per_week when present, else len(days)` — days are candidates not a count; FIRST read `validate_schedule_item` — it validates `days` and `sessions_per_week` INDEPENDENTLY (no cross-constraint, `knowledge.py:258-294`), so the rule STANDS; fixture `{days=3, sessions_per_week=2}` → scheduled 2. (b) "active" REUSES existing occupancy logic (`duration_weeks`/`season_end`/`supersedes`) — no second definition; verify it exists in-tree before reuse, flag at the gate if only the `active` flag exists. (c) **MISMATCH/UNPLACED emitted ONLY when the current leg's `sub_cycle_days == 7`**; else render `scheduled N/wk · quota M per K-day leg` with NO mismatch claim; fixture for a 10-day leg. (d) `done` from `resolve()`, never recounted. **DOCS-ONLY IN THE SAME PR:** ROADMAP NEXT gains a "Queued briefs (chat-authored, not yet received)" list, in order — coach routine read/update; activity slots v2; week planner; Hevy deletion reconciliation; phase-change flow (unwritten; awaiting operator form-vs-chat ruling); HC ingest stage 2 (blocked on HCA HR-lag, Q159) — one line each, no detail. Maxima by identity: decisions #311 → #312 at merge, questions Q162 (no new Q expected). Branch `claude/awesome-cori-2ivyao` (already restarted from master `89397de`, receipt `0e65165` pushed). Received, building.
2026-09-19 11:05 AEST | CHAT→CODE | **Plan-of-record + schedule↔quota-link brief (v3) received — received, not started; STOP at G0.** Objective: (A) a superseding user-scoped `training_plan` typed knowledge entry the coach reads every turn (relocates #270's plan-of-record from ROADMAP to a coach-readable store; reverses nothing); (B) an OPTIONAL `satisfies` link on `schedule_item` (`{capacity:<Capacity token>}` | `{load_window:"metabolic"}`, exactly one key, validated against the microcycle validator's OWN vocabularies — imported, one definition) + a consistency line computed on read (scheduled vs quota vs done; MISMATCH/UNPLACED/unlinked; `done` from `resolve()`, never recounted). Phase ledger untouched (actuals only, #223); days stay a PREFERENCE (#275 untouched); no calendar/planner/phase-change UI. **Non-schema EXPECTED but NOT asserted — S0(a) adjudicates: (i) can a `type='training_plan'` row be added with no migration (`knowledge_entries.type` free-text vs enum/CHECK; `value` JSON)? (ii) can `schedule_item` gain `satisfies` by validator change alone (`SCHEDULE_ITEM_FIELDS` refuses unknown keys today)? ANY schema need → HOLD (operator review, Merge disposition hold (a)).** S0(b) READER SWEEP is the standing gate (per #309→#311): every reader of `knowledge_entries` (+ `_section_knowledge`, `_section_schedule`, knowledge-update protocol text, MCP tools, frontend Knowledge page, exports) stated against a `training_plan` row AND a `satisfies`-carrying `schedule_item` — each shown-unchanged-by-test or ruled; the plan MUST render ONCE (own section), never also in the generic knowledge list. S0(c): token cost of rendering the macro plan every turn; propose the bound, don't pick it silently. **Maxima by identity: decisions #311, questions Q162 (master `89397de`) — numbers at merge; DECISIONS `#NEXT`, `Q#NEXT` resolved from master's max immediately before landing.** Branch `claude/awesome-cori-2ivyao` restarted from master `89397de` at build time (S1+), NOT now (S0 is read-only). Received, not started — running S0, stopping at G0 with the reader-sweep table.
2026-09-19 10:40 AEST | CHAT→CODE | **Finding on the merged #232: `cbti/replay` allow-list reasoning was WRONG since #309 — immediate follow-up PR (non-schema, self-merge on green; #232 merged, do NOT revert).** #232 allow-listed `cbti/replay.load_nights` on "twin stop_time differs by seconds — negligible". True for same-bout twins, FALSE since #309: `_TRAINING_SQL` has no ORDER BY and no source filter, and `load_nights` folds rows `{session_date: stop_time}` last-row-wins. LIVE path (`routers/checkin_v2` → `evaluate_live_cycle` → `load_nights`) feeding `training_end` → `training_constrained` (TRAINING_RECOVERY_MIN=90) → the night's exclusion from titration. Post-#309 a day can hold DIFFERENT bouts (morning walk, evening pilates/session), so a walk can overwrite the evening session (constraint vanishes) or an evening walk can constrain a night — ingest changing the sleep engine's inputs as a side effect (same class as Q160). **FIX (raw-SQL isolation kept — expressible in SQL): (1) deterministic per session_date `MAX(stop_time)`, never iteration order; (2) interim mirroring Q160 — `source <> 'health_connect'` EXCLUDED until ruled (preserves pre-#309 exactly); raise Q162 (which activities constrain a night — a walk almost certainly not, a hard evening Garmin/Samsung session arguably yes; do NOT pick); (3) update the drift-guard allow-list reason to what the read now guarantees.** GATES: {evening Polar + morning HC walk} → training_end = evening stop, asserted with rows inserted in BOTH orders; {HC evening walk only} → training_end None; existing CBT-I replay fixtures' verdicts byte-identical; §18 mutation (drop the MAX) → both-orders test fails. `CBTI_TITRATION_POLICY` is NOT in the tree (project-knowledge, unseeable) — cite operator-held if it defines "training session". Branch restarted from master `ebbe557` (post-#310). Maxima: decisions #310 → #311, questions Q161 → Q162. Non-schema; self-merge on green. Received, starting.
2026-09-19 09:04 AEST | CHAT→CODE | **PR #231 (#309) MERGED by operator. Next: the read-door follow-up PR (Q161) — non-schema, self-merge on green.** Scope exactly as Q161 routed: one read door per table (`reads.aerobic_reads.arbitrated_sessions` for aerobic; `reads.hevy_reads.counted_workouts` for hevy — both exist from #309); the two `mcp_server` readers (`get_readiness_snapshot`, `get_training_sessions`, raw SQL over `aerobic_sessions`, feed EXTERNAL MCP clients) moved onto the aerobic door; every other swept reader (cbti/replay.load_nights, engine/region_exercise.recent_template_ids, the two audit scripts) FIXED or allow-listed WITH A REASON; a drift-guard test that FAILS on any query of `aerobic_sessions`/`hevy_workouts` outside the allow-list (the structural fix — reader N+1 can't repeat this); the load transform (`load_events`) adopts `counted_workouts` behind a gate that `load_events` AND `load_metrics` are BYTE-IDENTICAL before/after for users 1 and 4 (they will be — every current dedup pair is adjudicated, operator-confirmed by prod pull; a sandbox can't touch prod, so I prove the equivalence-when-adjudicated as a TEST and the prod byte-identity rests on the operator's confirmation — unseeable-surface). Branch restarted from master `ab4c39a` (keeps `claude/awesome-cori-2ivyao`; #231 merged, so force-with-lease is clean). Maxima on new master: decisions #309, questions Q161 — no collision. Non-schema → self-merge on green (§ Merge disposition). Plan-of-record brief follows this. Received, starting.
2026-09-19 08:02 AEST | CHAT→CODE | **PR #231 review round 2 ADDENDUM — corrects the operator's OWN round-1 "hevy honours excluded_at + dedup_flag" instruction; MERGE CONDITION 2.** PROD FACT (user 1): `dedup_flag` is set on BOTH members of a suspected pair (`_recompute_dedup_flags`), `dedup_partner_ids` = the partner hevy_ids (verified: JSONB list, not self); `excluded_at` is the operator's adjudication of WHICH member is the artifact. So `dedup_flag IS NOT TRUE` drops the RETAINED performed log as well as the artifact — the round-1 psychological fix AND the resolver's #276 filter are WRONG for adjudicated pairs; the load transform (excluded_at only) is right. Five flagged/excluded rows (user 1): 89b8f0c8 EXCLUDED (artifact, partner a0f298a4) / a0f298a4 not-excl (performed, counts) / 0cd0cd00 EXCLUDED (artifact, partner f5d877a7) / f5d877a7 not-excl (performed, counts) / b3ebc404 not-flagged EXCLUDED (deleted upstream). **MERGE CONDITION 2:** ONE definition in a reads module, used by both callers this PR touches (psychological_reads, resolver): `counted(w) := excluded_at IS NULL AND (dedup_flag IS NOT TRUE OR every id in dedup_partner_ids is excluded)`. An unadjudicated flagged pair counts for NOTHING and is SURFACED — resolver uncounted reason `unadjudicated_duplicate`; the minutes read excludes it, docstring-noted; never silently counted twice, never silently dropped. Gates: the 5-row fixture → a0f298a4 + f5d877a7 count, the other 3 don't; an unadjudicated pair → neither counts + surfaced; §18 (revert to `dedup_flag IS NOT TRUE`) → fixture fails. Record in DECISIONS #309 as a correction: the resolver's #276 dedup filter was latently wrong for adjudicated pairs (historic windows only, no live effect — every current pair is adjudicated). The LOAD TRANSFORM is NOT touched here; the follow-up read-door PR adopts the helper behind a byte-identical gate for load_events/load_metrics (users 1, 4). Merge condition 1 (Q160 interim) stands. After both green: HOLD, operator merges. Received, folding into the round-2 build.
2026-09-19 07:59 AEST | CHAT→CODE | **PR #231 review round 2 — fix verified on `b031f4d`; ONE merge condition, then operator merges.** **Q160 INTERIM (merge condition):** `health_connect`-source rows contribute NO minutes and NO session tally to `_duration_min_by_day` until Q160 is ruled — one source filter + a test (an HC walk on a gym day leaves that day's minutes unchanged; §18 mutation: remove the filter → test fails). Rationale: the ingest must not change an existing metric as a side effect; pre-#309 only Polar + Hevy fed this term (no HC aerobic rows existed), so excluding HC preserves the series. Existing residual tests seed HC-source rows as generic duration providers → switch the shared `_aerobic` test helper to `polar_flow_export` (prod-faithful; a feeding source) so they stay paired. Update Q160 text: interim = HC excluded, decision pending, likely by declared sport. **Q161 ROUTING (operator's, not this session's code):** NOT the Hevy-deletion brief — a dedicated follow-up PR immediately after #231, before plan-of-record: one read door per table (`arbitrated_sessions` for aerobic; a single counted-workouts helper — `excluded_at`+`dedup_flag` — for hevy); `mcp_server.get_readiness_snapshot`/`get_training_sessions` restructured through them (correctness over query elegance); a drift-guard test failing when either table is queried outside a file allow-list; every other swept reader fixed or allow-listed with a reason. Update Q161 text accordingly. **Correction for the record:** the MCP readers feed EXTERNAL MCP clients, not the in-app coach (which has no tool runtime) — fix wherever I said "coach". **`load_events` `dedup_flag` divergence:** in NEITHER PR — operator is pulling the flagged prod rows and will rule it as its own load-input decision; note that in Q161, don't action it. After the Q160 interim lands green: HOLD, operator merges. Received, starting.
2026-09-19 07:36 AEST | CHAT→CODE | **PR #231 review received — one BLOCKING fix + Ruling-2 reconciled + a factual correction; stays HELD after the fix, operator merges.** **RULING 2 RECONCILED:** S0(d) exercise `source_package` distribution (all users) — withings 57 (2u), shealth 43 (2u), hevy 32 (2u), garmin 20 (2u), unknown 14 (1u), polarflow 11 (1u); every package fits the table or `unknown` class, NO misfit. Clear the OWED item (DECISIONS #309 divergence para, BRANCHES). **BLOCKING (fix in this PR):** `reads/psychological_reads.py` `_duration_min_by_day` (~L260-296) sums training minutes over ALL `aerobic_sessions` AND ALL `hevy_workouts` — no canonical filter, no excluded_at/dedup_flag filter — so post-ingest it double-counts a Garmin gym bout (HC row + Hevy workout), multiplies a bout by its non-canonical twins, and admits walks. Pre-existing for Polar twins; this PR makes it material. Fix: (1) aerobic side reads CANONICAL rows via `arbitrated_sessions` (not a second filter impl); (2) an HC session overlapping a COUNTED Hevy workout contributes no minutes — MOVE the resolver's `_overlaps_strength` to a shared home, two callers, don't copy; (3) Hevy side honours `excluded_at` + `dedup_flag`; (4) whether walk/pilates minutes belong here is NOT decided — report what the metric feeds and raise a Q#, don't pick. Gates: {Hevy + overlapping Garmin HC + Samsung twin + Withings mirror} → that day's minutes ONCE; excluded Hevy contributes nothing; §18 mutation (drop canonical filter → test fails). Then SWEEP every reader of `aerobic_sessions`/`hevy_workouts`, list canonical/excluded/dedup filtering per reader, report any other that doesn't (this list = S0(d) of the queued Hevy-deletion brief, do it once). **NON-BLOCKING (note):** ingest loads all HC rows per sync (bound by payload window if cheap); `sport_id=str(r.type)` when type is None (type is required-int, so defensive). **CORRECTION to the record (chat's error, not operator's):** S3b's "user 1 wears the Garmin in the gym" is FALSE — the 15 Sep Garmin row was a pilates class co-recorded with Samsung; amend the wording in DECISIONS #309, PR body, test docstrings — the `concurrent_strength` fixture STAYS, justified as a guard against ANY watch auto-detecting a gym bout, not a description of this user's practice (no code change). For the record: Withings is disabled for all but weight, its exercise mirrors are historical, the mirror rule stays. Received, starting the fix.
2026-09-19 04:52 AEST | CHAT→CODE | **G0 rulings received (PR #231, HC workout ingest stage 1) — starting the build; holds for operator review.** **SCHEMA FORK → Design 1:** add `source_package` AND `recording_method` (nullable int, actions Q118) to `aerobic_sessions` in one migration; HC record `id`→`source_session_id` as drafted; SCHEMA.md updated; operator merges after review (schema HOLD stands). **S0(a) accepted as a brief correction:** S3 is a REAL change to `arbitrate()` (within-`health_connect` comparison), not an extension — prove with a two-row same-source fixture AND the four-record-bout fixture. **RULING 0 WITHDRAWN:** no payload shim, no duration floor; countability is declared per slot by sport (activity-slot v2, queued) — ingest records what arrived. **RULING 1:** no per-device preference; writer classes wearable-native (`com.garmin.android.apps.connectmobile`, `com.sec.android.app.shealth`, `fi.polar.polarflow`) = EQUAL rank > aggregator/mirror (`com.withings.wiscale2`, `nl.appyhapps.healthsync`) > unknown; WITHIN a class the existing ladder decides (longest→earliest→lowest id); one declared table, provenance-row per #305. **RULING 2:** package not in the table → class `unknown`, named in the sync response; `com.hevy` → dropped at admission (counted, never ingested — Hevy connector owns those bouts). Carry the cross-PR `concurrent_strength` fixture in THIS PR (Garmin gym bout overlapping a Hevy workout → ingested, canonical, NOT conditioning). **DIVERGENCE flagged at the gate (FEEDBACK §44): the operator's message says "S0(d) output pasted below" but the `[S0(d) output here]` placeholder is EMPTY — nothing followed.** Ruling 2's confirm-the-table-is-total-and-STOP-on-a-misfit check therefore CANNOT be run; it is OWED pre-merge. Proceeding with the table built from the brief's EVIDENCE package strings — total by construction (unknown is the catch-all), so any unseen package still classifies; the only risk S0(d) guards is a real wearable mis-bucketed as `unknown`, a one-line table fix caught at the operator's review (they hold the S0(d) data). **Maxima by identity: decisions #308, questions Q158 — numbers at merge; DECISIONS #NEXT (ingestion built, mirror rule at admission, same-bout rule at read, zones deferred), Q#NEXT (stage-2 zones, blocked on HCA HR-lag).** Build S1–S3 + gates G1–G6 on `claude/awesome-cori-2ivyao` (harness-pinned branch), push to PR #231, hold for review. Plan-of-record brief is item #2, pasted when I report. Received, starting.
2026-09-19 04:35 AEST | CHAT→CODE | **HC workout ingest stage-1 brief received (closes #189's hold) — received, not started.** Makes a synced HC exercise record ONE `aerobic_sessions` row (`source='health_connect'`, start/stop/duration/sport, `z*_seconds` NULL), visible to the resolver session count + all `aerobic_sessions` readers, metabolic load fail-closed (INV-7). Zones = stage 2, out of scope (HCA HR-lag blocker). **Base divergence: brief cut from master `e8cd1a8`; master is now `222c221` (#308 merged) — adjudicating against the tree, not the brief's base.** **Maxima by identity: decisions #308, questions Q158 — numbers at merge.** **S0 established from the tree (Certain): (a) `arbitrate()` compares CROSS-source only (`aerobic_reads.py:118`, `y.source==x.source` skipped) — two `health_connect` rows (S3's independent detections) are same-source, so today BOTH stay canonical; S3 is a real change to `arbitrate()`, not a no-op. (c) NO writer-package priority table exists anywhere — `_capture_record_sources` captures identity but "does not itself filter" (#36/#37 enabler only), and `_SOURCE_RANK` ranks by SOURCE not writer; this brief MINTS the writer-class table. (e) `sport_name_for` CONFIRMED the mapping and currently test-only (`health_connect.py:232`); `_local_day` (`load_metrics`, Q42) is the reusable local-day helper for S2. (b) SCHEMA FORK — `aerobic_sessions` has NO writer column (`models.py:434` `source` is coarse) and `health_connect_record_sources` stores no record `id` (keyed `(user_id,record_type,record_start,source_package)`, `models.py:276`), so `source_session_id`=HC `id` has nothing to join for read-time writer recovery. S3 writer-class ranking therefore needs the writer on the row: EITHER a new `source_package` column (→ migration → HOLD, full human review) OR a read-time join to HCRS on `(user_id,'exercise',record_start==start_time)` (no schema, self-merge-eligible, but couples the read path + string-timestamp join + co-timestamped-mirror ambiguity). Intersects Q118 (persisting `id`/`recordingMethod`/`device`, accepted-but-dropped today). NOT decided by Code — routed to G0.** **BLOCKING at G0 (STOP per operator instruction): S0(d) distinct exercise `source_package` distribution (SQL emitted to chat); Ruling 0 real user-1 payload log of type/durationMinutes/recordingMethod/writer — none persisted today (Q118), needs a temp shim or S2-first; operator syncs and rules. Rulings 0/1/2 + the schema fork all owed.** Activity-slot brief v2 HELD (not re-aimed); plan-of-record brief is item #2 this session, pasted by operator at G0. Received, not started.
2026-09-17 21:53 AEST | CHAT→CODE | **S6b brief received — completes #307 Amendment 1 A2; corrects a #307 divergence I mislabelled as scoping. Received, starting.** A2 specified `_section_training_phase` render the RESOLVER POSITION from the same `resolve()` call the panel uses (current leg, per-slot done/quota, the due slot of either kind, uncounted). #307's S6 shipped only the DECLARED quota off the verbatim microcycle — no `resolve()`, no done/quota, no due, no uncounted, nothing for capacity slots — and I reported that narrowing as "a bounded shape" rather than flagging it as a divergence at the gate. The stated reason ("no `resolve()`/DB wiring") was wrong: `current_state()` has a live `db`/`user_id`/`today` (`current_state.py:63,114-125`), verified. **Plan:** (S1) `CurrentState.resolver_position = resolve(db,uid,today=today)`, try/except→None+log (G4); (S2) render the position under the phase section — window line, per-slot `{Label} · {done}/{quota}` + `◀ DUE` on `due_slot`, counted conditioning sessions (sport/duration/`unzoned`), four uncounted reasons in words, "all met" line; fold out `_conditioning_line`; (S3) one instruction sentence (authoritative count; don't infer from history; flag intent≠quota). Non-schema, read-only over `resolve()`; GUARD: no change to `resolve()`/validator/panel, no recompute. **Maxima by identity: decisions #307, questions Q158 → #308 at merge.** Wording corrections to carry (LOG): the HC-exercise ingest gap is NOT Q154 (Q154 = Polar ingest AUTOMATION) — it is #189's unbuilt Status, gets its own Q# from the stage-1 ingest brief; and the count is NOT "Polar-Flow-export only" — `polar_v4` rows are `aerobic_sessions` too (`connectors/polar.py:241`), canonical counting includes them. Candidate FEEDBACK §: a brief step narrowed in implementation is REPORTED as a divergence at the gate, not described as scoping (§19 family — prose/"done" has no totality check). Received, starting.
2026-09-17 21:33 AEST | CHAT→CODE | **GO on PR2 (frontend `QuotaWindow`, S5) + two rulings — written before acting, per #102.** PR1 (#307, backend) merged (`a5e04e3`). Operator review: (1) **G2 gate amended** — the "16 tests unchanged" wording accepted as too tight; two baseline exact-dict literals gained the additive `"due_slot": None`, behavioural assertions preserved (recorded on PR #228). (2) **Cross-PR sequencing rule** — on master `QuotaWindow.jsx` labels/keys by `s.capacity` and marks due by `due_capacity`, uncounted knows only untagged/off_plan; a conditioning slot renders undefined + the two new reasons show as off-plan. Nothing breaks from PR1 (no phase carries a load_window slot; response backward-compatible), but **the operator must NOT open the block-2 conditioning phase until PR2 is live and the served bundle renders it** (recorded on PR #228; folds into the closeout handoff). PR2 fixes it: `QuotaWindow` renders the `load_window` kind (`Conditioning · done/quota`), the due marker via the new `due_slot {kind,key}` on either kind, and the `concurrent_strength`/`untimed` uncounted reasons. Branch `feat/quota-window-conditioning` off master. #121 served-bundle grep is operator-side. Operator's own owed (not Code): plan-of-record v2 (pending knee-consult + club-start dates); the HCA HR-lag brief go-ahead. Received, starting PR2.
2026-09-17 20:46 AEST | CHAT→CODE | **GO on the metabolic slot-kind resolver (PR #228) — written before acting, per #102.** G0 ruled by the operator from the prod probe. **Amendment 1 applies, four points: (1) count CANONICAL `aerobic_sessions` directly (via `reads.aerobic_reads.arbitrated_sessions` `.canonical`), NOT `load_events` — so the count is not gated on qualifying/zones or on a formula version; a zoneless conditioning session still counts; (2) a new chat-context step S6 (surface the conditioning slot into `context_builder.py`); (3) `minutes` stays required on every slot (S0(f) unchanged); (4) half-timed (exactly one of start/stop NULL) → `untimed`, never silently counted.** OPEN CALL 1: **no floor in this PR** — P2 showed 2 sessions in the 6-week window (Fitness 30.1min/TRIMP117; unnamed 78.2min/TRIMP170), no short/easy session and so no basis; the floor question moves to the HC-ingest brief. OPEN CALL 2: **untimed fail-closed stands** (P3 = 0/2). P4 = 0 overlaps (`concurrent_strength` stays a fixture-proven guard, not prod-observed). P5: Garmin Connect DOES reach HC (1 exercise + 1204 heart_rate in 30d) but writes no `aerobic_sessions` row → invisible to the metabolic lane; the 2 counted events came via the Polar Flow export, not HC — confirms OPEN CALL 0 (Garmin→metabolic bridge is a separate brief; H10/Polar export is the cover). **Proceed S1–S6.** Maxima at merge: decisions #307 (from #306), questions Q159 if S6 mints one (from Q158) — resolved by re-reading master's max immediately before landing. GUARD holds: no new coefficient; no edits to `select_next`, `load_events_metabolic.py`, or the rollup (import `compute_metabolic_load`/`FORMULA_VERSION_METABOLIC`, never edit); non-migration (microcycle is JSON). Received, GO acknowledged, starting S1–S6.
2026-09-17 19:24 AEST | CHAT→CODE | **Metabolic slot-kind resolver brief (v1 test 2, Know) received — received, not started.** Adds a `load_window` slot kind to the phase microcycle so a conditioning block can declare and count metabolic sessions off the `load_events` metabolic lane (session counts only — no dose, no bands, no write-back). Branch `claude/metabolic-slot-resolver-0tx5q4` (operator-designated). **Maxima established by identity, not taken from the brief: decisions #306 (`DECISIONS_LOG.md`), questions Q158 (`OPEN_QUESTIONS.md`) — both match the brief.** Base note: brief cut from master `d687c29`; master is now `e8cd1a8` (the #305/#306 closeout merged after the cut) and this branch sits on it — maxima unchanged. **S0 code-facts established from the tree (Certain): (e) `microcycle` is `JSON` nullable (`models.py:960`), stored verbatim → a new slot key is a validator change, NOT a migration → this PR is not a HOLD on that axis; (f) `_slot_int` raises on a missing key (`profile.py:208`) and `validate_microcycle` calls it unconditionally for `minutes` (`training_phase.py:150`) → `minutes` is required on every microcycle slot today; (g-code) the only `aerobic_sessions` writers are Polar (`routers/polar.py:243`, `import_polar.py:196`, source `polar_flow_export`) — HC exercise is source-captured only (`health_connect.py:889` → `workouts: 0`, #189), so a Garmin→HC run writes no row, no load event: the brief's claim CONFIRMED.** **BLOCKING on the prod half: S0(a) resolver replay, (b) load_events distribution, (c) NULL start/stop counts, (d) Hevy overlap, and (g-prod) `health_connect_record_sources` cannot be run from this sandbox — no Railway CLI on PATH, Railway MCP has no SQL route (unseeable-surface rule: verify or STOP, never land). Exact operator queries emitted to chat at G0; the two OPEN CALLS (walk floor; untimed frequency) await the (b)/(c) distributions.** Received, not started.
2026-08-23 12:47 AEST | CHAT→CODE | **`schedule_item` schema brief (v2) received — received, not started.** Supersedes `brief_schedule_item_schema.md`, written but never run; four amendments folded in (A1 `supersedes` triggers on day overlap alone, A2 expiry precedence, A3 ownership-rule phrasing, A4 `expected_load` as the cost vocabulary any future axis resolves into). Single branch `feat/schedule-item-schema`, confirmed absent from origin. Scope: validate `schedule_item` at write in `backend/routers/knowledge.py` (closed key set, `validate_weekly_template`/`#221` as precedent), change the `context_builder.py` prompt template so a rejection is surfaced rather than a fact silently dropped, backfill 18 active rows (3 retired, 5 corrected, 10 conformed), add a `SCHEMA.md` section, and mint `#NEXT` + `Q#NEXT`. Resolver and calendar view are explicitly out of scope. **Maxima established by identity from master `aca2363`, not taken from the brief: decisions `#232` (brief correct), questions `Q115` (brief asserts `Q114` — STALE, corrected on operator instruction rather than halting; `Q115` landed earlier this session at `e834cc3`), `FEEDBACK` `§32` (brief left this unverified and asked Code to establish it; it moved this session at `aca2363`, so any `§#NEXT` here resolves to `§33`).** Row-level data in the brief is a 2026-08-23 chat read carried forward unverified — GUARD's stop-condition applies before any backfill. Received, not started.
2026-07-22 08:35 AEST | CHAT→CODE | **Post-close-out agreement, receipted at the moment of agreement per #98/#102 — second in a pair with §18.** A standing-rule candidate: **a resolution table handed from chat to Code is a hypothesis about the input space until Code enumerates it.** Before implementing, enumerate the space the table claims to cover and confirm every cell maps to exactly one outcome; report any input that falls through and treat fall-through as a defect in the table, not a case to resolve silently. Prose has no totality check — a table reads complete because nothing in the medium asks whether it is; code must return something for every input, which is what exposes the gap. Two instances this session: the censored `>0.30`-below-all-bands branch (#105) and the `news_gate` exact-dict constraint the brief did not see (#104). Fourth member of the family with §17 (don't trust a probe's success), #103 (don't let a check follow its own action) and §18 (don't trust a test that has never failed) — each is "the artefact that looks like verification isn't, until something can make it fail." **Canonical home is `FEEDBACK` §19 when a brief actions it; §18 (mutation rule) is §18, still owed. This line is the in-flight protection, not the landing.**
2026-07-22 06:52 AEST | CHAT→CODE | **GO — written before acting on it, per #102.** Merge authorisation for `feat/safety-threshold-gate` (`f078f1c`), given after chat verified the pushed branch **by identity** rather than from Code's report: `"is_moved"` × 0, `should_surface` × 2 and `safety_gate` × 6 in `producer.py`, the safety arm present as `basis.append(f"safety_band_{band_change}")` rather than a sibling key, asset pure ASCII with `thresholds` empty and `_deferred.haematocrit` holding the shape; control master README 200. Merge, push, delete, `/closeout`, regenerate. **Second item in the same entry, receipted so it is not lost a fourth time:** a standing rule agreed this turn — **a guard is verified by mutation, not by passing.** A test's value is what it does when the code is *wrong*, and the only way to know that is to make the code wrong on purpose. Same family as the identity-control and unpaired-negative rules (`FEEDBACK` §17) but constructive rather than cautionary. Earned here: the news_gate shape guard was confirmed by reimplementing the safety arm as a sibling key and observing 6 tests fail. **Canonical home is `FEEDBACK` §18 when a brief actions it — this ledger line is the in-flight protection, not the landing.**
2026-07-22 05:24 AEST | CHAT→CODE | Safety-threshold gate brief received — new `backend/reference/safety_thresholds.json` (schema + `_meta` + `_deferred.haematocrit`, no live entries), a schema test with a negative control, `gates.safety_gate()` as a third level-based gate, `news_gate` gaining a non-demotable second arm via optional keyword, and `_is_moved`/`is_moved` renamed to `_should_surface`/`should_surface` as a deliberate contract-surface change. Closes Q34; asset withheld under I1 pending citation capture (→ #104/#105/#106 + Q41 at merge; master `d22eeba`, maxima #103 / Q40 / §17 all verified by identity per §17 — `#104`/`Q41`/`§18` asserted absent and `#95`/`Q34`/`§16` asserted present, not inferred from a non-empty grep). Received, not started.
2026-07-21 21:56 AEST | CODE→CHAT | #99–#103 all landed (`8065135`). The session opened with a **blocking finding: `#99`/`#100`/`Q38`/`Q39` had never merged** — master sat at `1d49000`/#98 while they lived on an unmerged branch, so the incoming brief's supersession premise had already failed. Prerequisite merged first on a written go, then rebased. Landed: four erythroid RCV constants (0.08/0.08/0.08/0.02, Coşkun), superseding #99's haematocrit 0.12; #102 widening #98 rule 3 to any cross-lane instruction; #103 + `FEEDBACK` §17 landing the paired-control rule with two additions earned in-session. Q40 minted, Q38 appended. **Erythroid is now content-complete and delta-instrumented; `haemoconcentration_discriminator` can fail rather than self-confirm (mcv 0.30 → 0.02).** Still dark: the 0.50–0.54 band — **Q34 `safety_threshold`** is the only open item between the repo and the clinical concern that opened this fork; everything else on the 4b list is correctness. Re-sync CLAUDE.md, DECISIONS_LOG, OPEN_QUESTIONS, FEEDBACK, HANDOFF from disk.
2026-07-21 21:52 AEST | CHAT→CODE | **Two rules given post-merge, receipted per #102 before actioning.** (1) **Identity, not just function:** a control must discriminate on the identity of the thing under test, not merely prove the instrument works — where a probe could succeed against the wrong artefact (stale refs, cached CDN copies, reused branch names), pin to a SHA or assert on content only the intended version carries. Refines the paired-control rule, whose canonical home was flagged as `FEEDBACK` "when a brief actions it"; this actions it. (2) **A check whose failure cannot stop what follows is not a check** — chaining a verification to an action in one command is a reflex, so it needs a gate, not care. Both earned this session: three honest 200s described pre-rebase bytes, and a failed assertion was followed by `git rebase --continue` in the same command, committing conflict markers. Landing as `FEEDBACK` §17 + a `CLAUDE.md` line at #103.
2026-07-21 20:53 AEST | CHAT→CODE | Erythroid RCV-constants brief received — four constants from Coşkun et al. (`10.1515/cclm-2017-1155`), superseding #99's haematocrit 0.12. **Received, NOT started — two blocking findings, both verified.** (1) **The brief's base does not exist.** master is `1d49000`, DECISIONS max **#98**, questions max **Q37**. #99/#100/Q38/Q39 never merged; they sit on `feat/erythroid-constants-and-lever`, pushed but unmerged because the previous session held for a go that did not arrive. So there is no haematocrit 0.12 on master to correct, and no `erythroid.group_levers` for step 4 to leave alone. (2) **Step 2's self-check fails on one row:** haemoglobin/rbc/mcv reproduce exactly, haematocrit computes 8.0093 (2 d.p. 8.01) against published 8.00 — consistent with the source rounding its own CVA/CVI inputs rather than transport corruption, and the landed constant is 0.08 either way, but the brief says stop and report rather than reconstruct. Also verified: the asymmetrical-RCV question and two-sided convention did **not** land anywhere (only content hit for `asymmetric` on master is unrelated prose in Q34), so both need folding in as the brief anticipated.
2026-07-21 19:45 AEST | CHAT→CODE | Erythroid constants + lever brief received — land `haematocrit` `min_meaningful_delta` at 0.12 (Thirup 2003), promote `plasma_volume_status` out of `_deferred_levers` with verified DOIs, populate `erythroid.group_levers`; `haemoglobin` stays withheld under I1 because its per-parameter value was not read. Discharges the citable half of #96's withholding (→ #99/#100 + Q38/Q39 at merge; master `1d49000`, DECISIONS max #98 and questions max Q37 both verified at open — the brief's advisory figures confirmed, not assumed). Received, not started.
2026-07-21 15:56 AEST | CHAT→CODE | **Standing rule agreed, receipted at the moment of agreement per #98 rule 3 — canonical home is `FEEDBACK` when a brief actions it; this ledger line is the in-flight protection, not the landing.** **Any negative result offered as evidence — a 404, zero rows, an empty grep — must be paired with a positive control in the same command, and the control's output goes in the report alongside the negative. An unpaired negative is not a finding.** Rationale: a bare negative is uninterpretable, being equally consistent with "the thing is absent", "the probe was aimed wrong", and "the probe could not have succeeded". Chosen over "verify premises before acting" because that is a care-rule and this is checkable by a reader — a report either shows the control or it does not. Three instances it would have caught, all within 48h: the `feat/interpretation-view-skeleton` 404 (unreproducible on re-probe — paired master-vs-branch × 2 files returned 200 on all four, so the seed premise is falsified and its original cause is unknown, likeliest a stale CDN negative cache); the backfill dry run's expected zero (local SQLite has zero NULL rows, so the query could not have returned anything else — §11); and #95's brief asserting the ritual was "already struck". Note the pattern is not cured by naming it: §11 was correctly diagnosed on the backfill roughly twenty minutes before the same error was committed on the branch check.
2026-07-21 15:50 AEST | CHAT | O3 unblocked — carried BLOCKED since the fork on a false premise. `feat/interpretation-view-skeleton` is pushed and readable (paired probe, master and branch × 2 files, 200 on all four), so the re-verify against `{meta, groups[], ungrouped[]}` can proceed. Chat work, not Code. Board state lives on `ptb-tasks`, never mirrored here.
2026-07-21 14:55 AEST | CHAT→CODE | **Post-close-out agreement, receipted at the moment of agreement per #98 rule 3.** Luke owes the citation capture that unblocks the withheld constants: DOIs for the haematocrit RCV work and the plasma-volume papers. On landing they promote `plasma_volume_status` out of `_deferred_levers` and let the `haematocrit`/`haemoglobin` `min_meaningful_delta` constants land cited, discharging #96's withholding. **Owner: Luke, not Code.** This entry exists because Q37 took three sessions to land after being agreed twice — an agreement between sessions previously had nowhere to live.
2026-07-21 14:52 AEST | CODE→CHAT | #96/#97/#98 landed (`970224c`) — `erythroid` authored as structure only, `trt_erythrocytosis_watch` → `blocked_on_contract` with ONE blocker (the brief's second, `precondition_phase`, was checked and cleared: `hpg_gonadotropin_suppression` carries it, plus `driver`, so authored relations hold extra keys and it disqualifies nothing), Q36/Q37 minted, three standing guards. **Correction to a stated premise:** `feat/interpretation-view-skeleton` is NOT unpushed — verified present on origin and readable at 200 via all three raw URL forms, for both governance stores and both reference JSONs. O3's re-verify is not blocked. The genuinely unpushed branch was `feat/recovery-metrics-rhr`, now pushed. Re-sync CLAUDE.md, DECISIONS_LOG, OPEN_QUESTIONS, BRANCHES, HANDOFF from disk.
2026-07-21 11:27 AEST | CHAT→CODE | Erythroid group-authoring brief received — author `erythroid` in `marker_groups.json` as structure only (members, roles, 2 relations, no `group_levers`); correct `trt_erythrocytosis_watch`, which #95 wrongly cleared to `ready_to_promote`; read-constants and the `plasma_volume_status` lever withheld under #95's I1 extension pending verbatim DOIs (→ #96/#97 + Q36 at merge; master `fcb7530`, DECISIONS max #95 and questions max Q35 both verified at open, not taken from the brief). Received, not started.
2026-07-21 07:36 AEST | CODE→CHAT | #95 landed (`3bb200e`) — marker_canonical v0.3 (31→65), `_deferred` three-class taxonomy, I1 extended to read-constants; Q34/Q35 minted. **Minted #95, not #88 — the brief anchored to master #87 (`eed3c76`) but master was at #94.** **Ritual breach, recorded not hidden: no `CHAT→CODE` receipt was written before work began.** This brief carried no step 0 and the standing rule was not applied from memory; the omission is the receipt's entire purpose, so an interruption mid-session would have left nothing. **Two gates NOT claimed:** the backfill dry run ran against local SQLite with zero NULL rows and could not have returned anything but its expected zero; and I1's extension has no enforcement plus one live violation (`alt`). Re-sync DECISIONS_LOG, OPEN_QUESTIONS, CLAUDE.md, HANDOFF from disk.
2026-07-20 20:51 AEST | CODE | **Timestamp correction, not a restatement.** The two `CHAT→CODE` receipts below stamped `09:50` and `10:24` are each **10 hours early**; true local times were `19:50` and `20:24 AEST (+1000)`. Cause: Git Bash on this machine silently ignores `TZ` — `TZ='Australia/Brisbane' date` returns UTC and mislabels it (`10:44 GMT`), while the real local zone is `+1000`. Left in place per this ledger's append-only rule; corrected here rather than edited above. Any prior entry produced the same way carries the same error — unverified for entries predating 2026-07-20 19:50, which this session did not write. `scripts/gen_governance_view.py` now stamps a numeric offset, which neither Windows' verbose `%Z` nor Git Bash's TZ fallback can misrender.
2026-07-20 10:24 AEST | CHAT→CODE | Governance view generator brief received — `scripts/gen_governance_view.py` reading 8 stores across 2 repos at master via raw.githubusercontent, live provenance block, gitignored output (→ #94 at merge; DECISIONS max #93 and FEEDBACK max §15 both verified at open). Received, not started. **Format source `/mnt/project/CONSOLIDATED_GOVERNANCE_VIEW.md` is not reachable from Code — step 2 and the Gate-4 comparison are blocked on Luke supplying it.**
2026-07-20 09:50 AEST | CHAT→CODE | Return trip 2 brief received — Q25 close, FEEDBACK §14 append, ritual line 34, BRANCHES header pair (→ #93 at merge). Received, not started.
2026-07-20 07:55 AEST | CHAT→CODE | Vocabulary reconciliation brief received — strike CLAUDE.md line-60 status clause, sweep OPEN_QUESTIONS to four states, correct #90 gate claim via #91 (→ #91 at merge; master max confirmed #90 at line 2384, FEEDBACK max §13). Received, not started.
2026-07-20 07:38 AEST | CODE→CHAT | Session closure sweep Phase 1 LANDED to master at `05f0282` — minted **#90**, not #89 (master max was already #89; brief assumed #88). BRANCHES: all 22 rows four-state (11 DONE / 10 OWED / 1 UNSTARTED); 3 branches pushed to origin with dedicated rows + renumber debt recorded. Step-4 verdicts: hevy-exercise-template-resolver DONE, hevy-resolver-activation **OWED** (limb 2 — nonsense→refused — is a distinct path, never run), connector-error-policy **OWED** (brief's "See-all verified live" had no artifact — chat asserted an unseeable surface), probe-harness-fidelity + hrv-sleep-integrity Task 3 OWED. OPEN_QUESTIONS: 18 open / 1 verifying / 10 resolved — the brief's "no unresolved entries" was WRONG. FEEDBACK §13 added. 206 tests, unchanged. **Phase 2 (HCA) NOT started** — recorded OWED in #90 rather than asserted done. **Open for decision:** CLAUDE.md contradicts itself — canonical-stores row assigns open/verifying/resolved to OPEN_QUESTIONS, #88's vocabulary section says the four states apply to it; left unresolved because that text is in the verbatim-propagated shared block. Re-sync BRANCHES, OPEN_QUESTIONS, ROADMAP, DECISIONS_LOG, FEEDBACK, HANDOFF from disk.
2026-07-19 12:08 AEST | CHAT→CODE | Session closure sweep brief received — branch terminal states, vocabulary propagation, loop adjudication (→ #90 at merge; master max already #89, brief assumed #88 — next free number taken per PRECONDITION). Received, not started.
2026-07-19 11:20 AEST | CODE→CHAT | Q17 resolved on (A); minted #NEXT→#89 at merge (master max was #88; `feat/checkin-injury-probe` local #89–90 hardcodes renumber at their own merge). Q29 minted (PENDING). `feat/recovery-metrics-rhr` UNSTARTED. Landing `fix/q17-hrv-instrumentation` to master; SHA in closeout. **Step 5 (HCA #19→Q29 pointer) DEFERRED** to an HCA-rooted session per single-repo rule — a ready-to-paste HCA #20 entry was emitted to chat; not written here. Re-sync OPEN_QUESTIONS, BRANCHES, DECISIONS_LOG, HANDOFF from disk.
2026-07-19 11:07 AEST | CHAT→CODE | Q17 instrumentation-close brief received. Step 1 evidence adjudicated 4/4 CONFIRMED vs HCA master (`1db8833`/#19). Writing on `fix/q17-hrv-instrumentation`: resolve Q17 → (A); mint Q29 (phantom-stale reconciliation, install-segmentation prerequisite — brief's "Q5" was a stale-mirror mis-ref, real Q5 is dual-field acceptance); `feat/recovery-metrics-rhr` BLOCKED→UNSTARTED; DECISIONS #NEXT. HCA #19 pointer (step 5) HELD for confirmation — cross-repo write.
2026-07-19 10:01 AEST | CODE→CHAT | #88 governance built on gov/interruption-survival (unseeable-surface rule, state vocabulary, HANDOFF ledger, close-out git-log emission); landing to master. Re-sync CLAUDE.md ×2, FEEDBACK, DECISIONS_LOG, BRANCHES, HANDOFF from disk; master SHA in closeout.
2026-07-19 09:44 AEST | CHAT→CODE | Interruption-survival governance brief received — 4 changes, one concern (→ #88 at merge). Received, not started.
2026-07-19 09:02 AEST | CODE | #87 landed (eed3c76) — oracle fixture group display_name re-sync.
